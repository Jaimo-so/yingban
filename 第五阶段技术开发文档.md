# 第五阶段技术开发文档

> AI 读取定位：这是影伴当前最新、当前权威的工程交接文档。后续迭代必须先读仓库根目录 `AGENTS.md` 和本文件；只有追溯邀请码、双 Agent、TMDB、早期 Brave Search 或豆包语音的设计来源时，才读取第一至第四阶段文档。当前代码、数据库和真实运行状态高于历史快照；发现不一致时先只读验证，再在新的阶段文档中记录。

## 0. AI 快速接手卡

```text
DOCUMENT_ROLE=current_authoritative_handoff
CURRENT_STAGE=5
STATUS=completed_code_and_local_verification_waiting_for_tavily_and_voice_credentials
CODE_ROOT=products/yingban
PRODUCT_START=products/yingban/app.py
HTTP_ROUTING=products/yingban/server.py
AGENT_RUNTIME=products/yingban/agent.py
PERSISTENCE=products/yingban/storage.py
OFFLINE_MOVIE_RETRIEVAL=products/yingban/catalog.py
LIVE_MOVIE_AND_WEB=products/yingban/integrations.py
VOICE_RUNTIME=products/yingban/voice.py
USER_UI=products/yingban/web/index.html + app.js + styles.css
ADMIN_UI=products/yingban/web/admin.html + admin.js + admin.css
TESTS=products/yingban/tests/test_product.py
START_COMMAND=cd products/yingban && python3 app.py serve
TEST_COMMAND=cd products/yingban && python3 -m unittest discover -s tests -v
EXPECTED_TEST_BASELINE=19 tests passing
CURRENT_URL=http://127.0.0.1:8765/
CURRENT_DATABASE_MODE=model
CURRENT_MODEL_PROTOCOL=anthropic
CURRENT_MODEL_ID=deepseek-v4-flash
CURRENT_TMDB_MODE=enabled
CURRENT_WEB_SEARCH_PROVIDER=tavily
CURRENT_WEB_SEARCH_MODE=disabled_missing_credential
CURRENT_WEB_READER_MODE=enabled_douban_and_zhihu_public_pages_only
CURRENT_VOICE_PROVIDER=doubao
CURRENT_VOICE_MODE=disabled_missing_credential
CURRENT_MOVIE_COUNT=24
RUNTIME_EXTRA=ffmpeg required when browser recording is WebM/MP4
NEXT_DOCUMENT=products/yingban/第六阶段技术开发文档.md
SUGGESTED_FIRST_CHECK=run 19 tests, then inspect /api/health and masked admin configuration without exposing secrets
```

### 0.1 当前实现一句话

影伴是邀请码制 AI 电影伙伴：双 Agent 共享真实电影资料、安全和长期观影记忆边界；TMDB 负责新电影与真实海报；Tavily `basic` Search 作为新的默认 Web Search，负责全网及豆瓣、知乎域名限定检索；已有 Brave Search 配置仍兼容；豆包语音入口和后端适配已存在，但真实语音仍等待凭据。

### 0.2 当前完成状态

- Web Search 默认供应商由收费门槛不合适的 Brave Search 改为 Tavily。
- Tavily Researcher 免费方案在 2026-08-21 的官方说明中为每月 1,000 API credits、无需绑定信用卡。额度和价格是外部事实，后续上线前必须重新核对，不能把当前数字视为永久合同。
- 影伴 Tavily 请求固定使用 `search_depth=basic`，每次搜索按当前官方规则消耗 1 credit；没有启用 `auto_parameters`，避免自动升级到可能消耗 2 credits 的 `advanced`。
- 豆瓣和知乎检索在 Tavily 请求中使用 `include_domains`，不读取 Cookie、不自动登录、不绕过验证码。
- Brave 适配器仍保留，已有 `https://api.search.brave.com/res/v1` 配置会自动推断为 `brave`，不会被静默破坏。
- 切换搜索供应商但没有同时输入新密钥时，后端会清空旧供应商密钥，防止跨供应商误发凭据。
- 管理后台新增原生 `select#web-search-provider`，可在 Tavily 与 Brave 间切换；字段名称、帮助文案和默认根地址随供应商变化。
- 当前真实数据库已保存 `web_search_provider=tavily` 和 `web_search_base_url=https://api.tavily.com`，`web_search_api_key` 为空；因此 Web Search 暂未启用，也不会产生外部搜索费用。
- 当前真实 TMDB 凭据仍配置有效，管理页状态为“电影与海报已接入”；切换 Web Search 没有删除 TMDB 凭据。
- 19 项自动测试全部通过；后台页面实机检查通过；Tavily 选择器、动态标签、保存状态和响应式布局均正常。
- 本地服务已加载当前代码并在 `http://127.0.0.1:8765/` 运行；大模型模式为 `model`，模型 ID 为 `deepseek-v4-flash`。

### 0.3 不可破坏的产品约束

- 一个邀请码只对应一个长期账户；同一码再次登录必须返回原账户。
- 邀请码完整正文不得写入数据库日志、测试输出或阶段文档。
- 用户确认看过电影后自动写入 `watched`，不重复询问；推荐数据层必须硬性排除 `watched` 和近期拒绝项。
- TMDB 动态电影必须先转为统一电影记录并写入 `movies`，才能进入观影状态、推荐、曝光和硬排除链路。
- “聊电影”和“找电影”使用独立系统提示词，但共享身份、安全、隐私、事实与长期记忆边界。
- Agent 不得冒充真人，不得虚构童年、朋友、恋爱、工作或影院观影等真人经历；用户明确询问身份时必须说明自己是 AI。
- 现实烦恼、心理状态和敏感经历不得自动写入长期记忆。
- 高风险自伤或伤人文本必须在普通电影 Agent 和外部工具调用前由产品代码短路。
- Web 搜索摘要只能作为公开资料线索；电影推荐最终仍必须落到产品的真实候选记录，不能凭摘要补造电影。
- 豆瓣和知乎能力只处理公开页面；不得登录、提交 Cookie、绕过验证码、绕过付费墙、绕过访问控制或读取私密内容。
- 管理读取接口不得返回完整 API 密钥、Access Token、管理员口令、邀请码或会话令牌。
- 不同 Web Search 供应商的密钥不能交叉复用；供应商变更且未输入新密钥时必须清除旧密钥。
- Tavily 必须保持 `basic` 搜索，除非用户明确接受更高额度消耗并完成新的产品决策。
- 联网服务不可用时，种子片库、观影历史、推荐硬去重和已配置的大模型对话仍应继续工作。
- 录音、Base64 音频、转换后的 WAV 和合成音频不得写入长期数据库或磁盘。
- 浏览器不得自动播放 AI 语音；必须由用户主动点击播放。
- 麦克风入口不能因服务未配置而完全消失；不可用原因必须可发现、可读。
- 旧账户、邀请码绑定、观影历史、曝光记录、提示词、模型配置、Brave 配置和 OpenAI 兼容语音配置升级时不得丢失。

### 0.4 下一阶段建议的第一步检查

1. 执行 19 项测试，确认不是只相信本文档的时间点快照。
2. 请求 `GET /api/health`，确认返回 `mode=model`。
3. 登录 `/admin#internet-settings`，只检查供应商、根地址和密钥是否配置，不输出任何完整密钥。
4. 用户在 [Tavily Platform](https://app.tavily.com) 注册后，复制 API Key 到“Tavily API Key”，先点“测试 Web Search”，再保存。
5. 用 `source=douban` 和 `source=zhihu` 分别完成一次真实搜索，核对结果域名、中文摘要质量和每月额度消耗。
6. 若真实搜索质量不足，先优化查询和域名限定，不要直接切换 `advanced`；任何额度升级都需要用户明确决定。

## 1. 阶段信息、需求背景与完成定义

- 产品名称：影伴。
- 阶段名称：第五阶段——Web Search 免费方案替换与 Tavily 适配。
- 完成日期：2026-08-21。
- 继承阶段：第四阶段——豆包语音正式适配与麦克风入口修复。
- 下一阶段文档：`第六阶段技术开发文档.md`。
- 本阶段边界：用具有明确免费月度额度的 Tavily 替换后台默认 Brave Search，同时保留 Brave 兼容；不替用户注册第三方账号，不生成或保存虚假密钥，不改变现有视觉语言。

用户在获取 Brave Web Search API 密钥时发现需要付费，希望改用免费方案。选择标准不是“永久免费”承诺，而是：当前官方有清晰免费额度、无需绑卡、提供结构化搜索 API、支持中文通用搜索、能够限制豆瓣与知乎域名，并允许后端安全保存密钥。

官方依据：

- [Tavily Quickstart](https://docs.tavily.com/documentation/quickstart)：创建账号后可从控制台复制 API Key；当前说明为每月 1,000 免费 credits、无需信用卡。
- [Tavily Credits & Pricing](https://docs.tavily.com/documentation/api-credits)：`basic` Search 每次 1 credit，`advanced` 每次 2 credits。
- [Tavily Search API](https://docs.tavily.com/documentation/api-reference/endpoint/search)：`POST /search`、Bearer 认证、`include_domains`、`max_results` 等契约。
- [Tavily API Introduction](https://docs.tavily.com/documentation/api-reference/introduction)：根地址 `https://api.tavily.com`，鉴权头为 `Authorization: Bearer <API_KEY>`。

完成定义：

```text
新安装或无旧搜索配置
→ 后台默认显示 Tavily
→ 用户输入 Tavily API Key
→ 测试连接调用 POST https://api.tavily.com/search
→ 固定 basic + max_results
→ 全网搜索不限制域名
→ 豆瓣搜索 include_domains=[movie.douban.com]
→ 知乎搜索 include_domains=[zhihu.com]
→ 统一返回 title/url/snippet/source
→ Agent 继续通过原 search_web 工具消费结果
```

兼容完成定义：

```text
数据库只有旧 web_search_base_url，尚无 provider 字段
→ URL 包含 api.search.brave.com 时推断 brave
→ URL 包含 api.tavily.com 时推断 tavily
→ 旧 Brave key 和请求协议继续可用
→ 用户主动切换 provider 时使用对应新根地址
→ 未同时提供新 key 时清空旧 key
```

## 2. 当前总体架构与关键决策

影伴仍是 Python 标准库单进程 HTTP 服务、SQLite 持久化和无前端构建步骤的原生 HTML/CSS/JavaScript 应用。

```text
浏览器用户端
  ├─ 邀请码登录
  ├─ 聊电影 / 找电影
  ├─ 电影卡片、海报、观影记录
  └─ 文字输入 / 可发现的语音输入 / 主动播放语音
          │
          ▼
server.py
  ├─ 用户与管理员 Cookie 会话
  ├─ API 参数、大小、错误和安全边界
  ├─ 高风险文本短路
  └─ 静态资源
          │
          ├─ agent.py：双提示词、模型协议、Agent Loop、工具注册
          ├─ catalog.py：本地检索、匹配、排序、硬排除
          ├─ integrations.py：TMDB + Tavily/Brave + 公开页面阅读
          ├─ voice.py：豆包 + OpenAI 兼容 STT/TTS
          └─ storage.py：SQLite、邀请码、会话、电影、记忆、配置
```

关键决策：

1. Web Search 继续通过影伴自己的 `search_web` 领域工具暴露给 Agent，供应商细节不泄漏到提示词和模型工具 schema。
2. Tavily 和 Brave 在 `InternetRuntime.web_search()` 内适配成统一结果，避免 Agent 同时理解两个第三方协议。
3. Tavily 固定 `search_depth=basic`、`include_answer=false`、`include_raw_content=false`、`include_images=false`，控制额度、延迟和响应体；电影海报仍以 TMDB 为权威来源。
4. Tavily 豆瓣与知乎使用结构化 `include_domains`，Brave 保留旧的 `site:` 查询语法。
5. `web_search_provider` 写入 `app_settings`，没有数据库建表或加列；SQLite `app_settings` 的键值模型天然兼容新增配置。
6. 供应商切换不自动迁移密钥，因为不同供应商的凭据无兼容意义；安全默认是清空。
7. UI 使用原生 `<select>`、显式 `<label for>` 和帮助文本，保持键盘导航、读屏器命名和现有视觉风格。

## 3. 继承的已有能力

### 3.1 账户、邀请码与会话

- 邀请码以摘要保存，完整邀请码只在生成当次显示。
- 第一次登录创建账户并绑定邀请码；同一码后续登录回到同一账户。
- 邀请码可停用或换发；换发保留账户但旧码和旧会话失效。
- 普通用户与管理员使用独立 HttpOnly Cookie 会话。

### 3.2 双 Agent 与模型运行

- `discussion` 对应“聊电影”：回应具体观后感、人物、主题和情绪，服从剧透选择。
- `recommendation` 对应“找电影”：理解当下需求，只从真实候选中选择，并说明适合、不适合和必要内容提醒。
- 模型支持 Anthropic Messages 与 OpenAI-compatible Chat Completions 两种协议。
- 当前数据库配置为 Anthropic 协议、`deepseek-v4-flash`、`model` 模式。
- 管理员可编辑两套完整提示词；保存后立即生效。

### 3.3 电影、推荐、长期记忆与海报

- 内置 `data/movies.json` 种子片库；当前数据库共有 24 条电影记录。
- TMDB 搜索和发现结果映射为 `tmdb-{external_id}`，写入 `movies`。
- `poster_url` 来自配置的 `TMDB_IMAGE_BASE_URL + poster_path`；前端电影卡片展示真实海报。
- `user_movie_states` 保存 `watched`、`watchlist`、`disliked`；现实烦恼不自动持久化。
- `catalog.recommend()` 在数据层硬排除看过和近期拒绝电影。
- `recommendation_impressions` 记录曝光，降低短期重复。

### 3.4 语音

- 用户输入区始终可看到语音模式入口。
- 浏览器通过 `MediaRecorder` 录音；豆包不支持的 WebM/MP4 可由后端使用 `ffmpeg` 内存转换为单声道 16 kHz WAV。
- 豆包默认 ASR 资源 ID：`volc.bigasr.auc_turbo`。
- 豆包默认 TTS 资源 ID：`seed-tts-2.0`。
- 默认音色：`zh_female_xiaohe_uranus_bigtts`。
- OpenAI 兼容模式调用 `/v1/audio/transcriptions` 与 `/v1/audio/speech`。
- 原始录音、转换音频和合成音频均不落盘；浏览器只在用户主动点击后播放。

## 4. Agent 行为、运行时上下文与工具限制

`AgentRuntime.respond()` 根据 `mode` 选择独立提示词，并注入当前电影、候选片单、剧透开关、观影记录等运行时上下文。当前模型最多进行受控工具循环，工具异常被转换为结构化 `error` 返回，避免服务进程崩溃。

当前领域工具共七个：

1. `search_movie(query)`：先查本地真实电影；无结果且 TMDB 可用时查 TMDB 并入库。
2. `get_movie_detail(movie_id)`：读取已收录电影的事实、主题、情绪和内容提醒。
3. `list_watched_movies()`：只读取当前账户的看过列表。
4. `mark_movie_watched(movie_id, source)`：只允许已存在电影；`source` 为 `discussion`、`recommendation_feedback` 或 `chat`。
5. `recommend_movies(request, limit)`：可先调用 TMDB discover 补候选，再由本地推荐器硬排除和排序；`limit` 为 1 至 5。
6. `search_web(query, source, limit)`：`source` 只允许 `web`、`douban`、`zhihu`，`limit` 为 1 至 10；当前没有 Tavily Key 时返回配置错误，不伪造搜索结果。
7. `read_public_page(url)`：只允许公开的 `douban.com`、其子域名、`zhihu.com` 和其子域名；拒绝其他域名、带用户名 URL 和非 HTTP(S) 地址。

搜索结果统一契约：

```json
{
  "title": "公开结果标题",
  "url": "https://来源页面",
  "snippet": "去除 HTML 标签后的公开摘要，最多 1200 字符",
  "source": "web | douban | zhihu"
}
```

安全降级：

- Web Search 未配置：本地电影、TMDB、历史和大模型继续工作；需要公开网页资料时工具返回明确错误。
- TMDB 未配置：本地 24 部电影继续可用；不假装已查到新片，也不伪造海报。
- 模型未配置：切到演示 Agent；账户、片库、历史和管理能力继续工作。
- 语音未配置：麦克风入口仍显示，但说明需要管理员配置；文字对话正常。
- 高风险文本：在模型和外部工具前短路，记录最小化 `safety_events`，返回安全支持文本。

## 5. Tavily 与 Brave 接口契约

### 5.1 `InternetConfig`

```text
tmdb_api_key: str
tmdb_base_url: str
tmdb_image_base_url: str
web_search_provider: "tavily" | "brave"
web_search_api_key: str
web_search_base_url: str
web_reader_enabled: bool
web_reader_base_url: str
```

`movie_enabled` 定义为 `bool(tmdb_api_key)`；`search_enabled` 定义为 `bool(web_search_api_key)`。供应商、根地址和密钥是独立字段；仅有根地址不代表搜索已启用。

### 5.2 Tavily 请求

```http
POST {web_search_base_url}/search
Authorization: Bearer {web_search_api_key}
Content-Type: application/json
Accept: application/json
```

```json
{
  "query": "用户查询，最多 300 字符",
  "topic": "general",
  "search_depth": "basic",
  "max_results": 1,
  "include_answer": false,
  "include_raw_content": false,
  "include_images": false,
  "include_domains": ["movie.douban.com"]
}
```

`include_domains` 只在 `source=douban` 或 `source=zhihu` 时添加。`source=web` 不添加域名限制。响应读取 `results[].title`、`results[].url` 和 `results[].content`。

### 5.3 Brave 兼容请求

```http
GET {web_search_base_url}/web/search?q=...&count=...&search_lang=zh-hans
X-Subscription-Token: {web_search_api_key}
Accept: application/json
```

豆瓣与知乎在 Brave 查询前分别添加 `site:movie.douban.com` 和 `site:zhihu.com`。响应读取 `web.results[].title`、`url` 和 `description`。

### 5.4 通用外部 JSON 请求

`_json_request(url, headers, body=None)` 当前同时支持：

- `body is None`：GET，用于 TMDB 和 Brave。
- `body is dict`：UTF-8 JSON POST，用于 Tavily；自动设置 `Content-Type: application/json`。
- 最大读取 2,000,000 bytes。
- HTTP 错误只截取有限外部详情；URL 错误映射为“无法连接外部服务”；非 JSON 或非对象响应拒绝。

## 6. 数据结构、迁移与兼容性

本阶段没有新增表、字段或索引。当前 SQLite 表及其意义：

- `accounts`：长期账户与状态。
- `invite_codes`：邀请码摘要、提示片段、状态、账户绑定和换发关系。
- `sessions`：普通用户会话摘要、账户与过期时间。
- `admin_sessions`：管理员会话摘要与过期时间。
- `movies`：统一电影事实记录，包含别名、年份、主创、地区、类型、主题、情绪、内容提醒、摘要、来源、海报、来源链接和外部 ID。
- `user_movie_states`：账户级 `watched`、`watchlist`、`disliked` 状态、来源、评分和备注。
- `recommendation_impressions`：推荐曝光，支持短期去重。
- `safety_events`：最小化高风险事件类别，不保存完整敏感对话。
- `app_settings`：运行时配置键值；密钥也保存在这里，因此数据库文件必须只允许当前系统用户读取。

本阶段新增 `app_settings` 键：

```text
web_search_provider=tavily | brave
```

既有键继续使用：

```text
web_search_api_key
web_search_base_url
web_reader_enabled
web_reader_base_url
```

迁移方式：惰性兼容，无 SQL migration。读取配置时：

1. 有 `web_search_provider` 时直接校验并使用。
2. 无 provider 且根地址包含 `api.search.brave.com` 时推断 `brave`。
3. 无 provider 且根地址包含 `api.tavily.com` 时推断 `tavily`。
4. 两者均不匹配时回退 `Settings.web_search_provider`。

回滚风险：如果简单删除新代码但数据库已保存 Tavily 根地址，旧版本会把 Tavily 当 Brave 用 GET `/web/search`，连接会失败。因此回滚到第四阶段前必须先在后台切回 Brave 根地址和 Brave 密钥，或同时回滚数据库中的 Web Search 配置。账户、电影和观影历史本身不受影响。

## 7. 接口变化

既有 HTTP 路径未新增；响应和请求字段向后兼容扩展。

### `GET /api/admin/agent-config`

`internet.web_search` 新增：

```json
{
  "provider": "tavily",
  "base_url": "https://api.tavily.com",
  "has_api_key": false,
  "api_key_hint": "",
  "enabled": false
}
```

完整密钥永不返回。

### `POST /api/admin/internet-config`

新增可写字段：

```json
{
  "web_search_provider": "tavily"
}
```

原字段 `web_search_api_key`、`clear_web_search_api_key`、`web_search_base_url` 继续有效。供应商变化且新 key 为空时清除旧 key。

### `POST /api/admin/internet-config/test`

`target=web_search` 仍调用统一 `web_search("电影", "web", 1)`。当前缺少 Tavily Key 时返回“请先填写 Web Search API 密钥”；配置后才产生一次真实 `basic` 搜索消耗。

其余现有主要接口保持：`/api/health`、`/api/me`、`/api/auth/login`、`/api/auth/logout`、`/api/chat`、`/api/movies`、`/api/history`、语音接口、模型/提示词配置、邀请生成/停用/换发。

## 8. 页面变化与可访问性

只修改管理员后台“联网检索与电影海报”区，不改变产品主视觉语言：米白背景、纸张卡片、深红强调色、现有字体层级和圆角继续保留。

新增控件：

```html
<select id="web-search-provider">
  <option value="tavily">Tavily（推荐 · 每月免费额度）</option>
  <option value="brave">Brave Search（兼容保留）</option>
</select>
```

交互：

- 选择 Tavily：根地址变为 `https://api.tavily.com`，标签变为“Tavily API Key”，说明免费额度与 `basic` 消耗。
- 选择 Brave：根地址变为 `https://api.search.brave.com/res/v1`，恢复 Brave 标签与说明。
- 切换时清空输入框，但不在前端错误地强制勾选“清除密钥”；真正的跨供应商清理由后端契约保证。
- 保存后 `renderInternetConfig()` 使用响应重新渲染权威状态。

可访问性：

- 使用原生 `select`，支持键盘上下键和系统辅助技术。
- `label[for=web-search-provider]` 提供可访问名称。
- `aria-describedby=web-search-provider-help` 关联额度说明。
- 动态字段标签保留明确 `for` 关系。
- 保存与测试结果继续使用 `role=status` 和 `aria-live=polite`。

## 9. 配置变化

新安装 `.env` 默认：

```env
WEB_SEARCH_PROVIDER=tavily
WEB_SEARCH_API_KEY=
WEB_SEARCH_BASE_URL=https://api.tavily.com
WEB_READER_ENABLED=true
WEB_READER_BASE_URL=https://r.jina.ai
```

兼容旧变量：

- `WEB_SEARCH_API_KEY` 缺失时仍读取 `BRAVE_SEARCH_API_KEY`。
- `WEB_SEARCH_BASE_URL` 缺失时仍读取 `BRAVE_SEARCH_BASE_URL`。
- 如果旧 Brave 环境变量存在但没有显式 provider，根地址推断会把运行时识别为 Brave。

管理员后台保存值优先于 `.env` 启动默认值。

## 10. 安全与隐私边界

- Tavily、Brave、TMDB、模型和语音密钥只由后端发往各自供应商。
- 浏览器管理页只看到“未配置”或末四位掩码。
- URL 校验只允许 HTTP(S)、必须有主机、禁止 URL 用户名；这降低凭据被嵌入 URL 的风险。
- 外部响应长度受限；异常不会回显内部堆栈给普通用户。
- 搜索输入截断为 300 字符，结果标题、URL、摘要分别限长。
- 豆瓣与知乎阅读器严格检查最终输入 URL 的 hostname，不能用相似域名绕过。
- Tavily `include_domains` 是搜索范围，不扩大公开页面阅读器的允许域名。
- 不把网页观点当电影事实；回答引用公开资料时应给出来源链接并保留不确定性。
- 免费额度耗尽属于可预期降级：返回外部服务错误，不回退到未经授权的爬虫、不自动付费、不切换到付费套餐。

## 11. 修改文件清单

- `products/yingban/settings.py`：新增 `web_search_provider`，新默认改为 Tavily，增加通用 Web Search 环境变量并兼容旧 Brave 变量。
- `products/yingban/integrations.py`：新增 provider 校验、惰性迁移、跨供应商密钥清理、Tavily POST 适配、统一 GET/POST JSON 请求和结果映射。
- `products/yingban/web/admin.html`：新增 Web Search 服务商选择器、动态标签 ID、免费额度帮助文本。
- `products/yingban/web/admin.js`：渲染和提交 provider，推断旧配置，切换供应商字段与默认地址。
- `products/yingban/.env.example`：将新安装示例改为 Tavily 通用变量。
- `products/yingban/README.md`：更新联网搜索说明、免费额度、Key 获取入口和目录说明。
- `products/yingban/tests/test_product.py`：旧 Brave 测试显式指定 provider；新增 Tavily Bearer、`basic`、域名过滤和结果映射测试。
- `products/yingban/第五阶段技术开发文档.md`：本权威交接文档。

本阶段没有修改：`storage.py` 数据库 schema、用户端 HTML/CSS/JS、电影推荐算法、模型提示词正文、语音协议实现。

## 12. 测试与验证结果

执行命令：

```sh
cd products/yingban
python3 -m unittest discover -s tests -v
```

结果：

```text
Ran 19 tests in 4.003s
OK
```

新增重点测试 `test_tavily_search_uses_basic_search_and_domain_filter` 验证：

- 保存后的公开配置 provider 为 `tavily`。
- URL 精确为 `https://api.tavily.com/search`。
- Header 为 `Authorization: Bearer <key>`。
- `search_depth=basic`。
- `max_results` 与调用限制一致。
- 豆瓣 source 使用 `include_domains=["movie.douban.com"]`。
- `results[].content` 正确映射为统一 `snippet`。

回归测试继续覆盖：邀请码与账户绑定、停用、换发、账户隔离、推荐硬排除、观后讨论自动记忆、AI 身份透明、模型配置与密钥掩码、两套提示词独立、TMDB 新电影和真实海报入库、豆包原生协议、麦克风入口常显、管理接口保存联网和语音配置。

静态验证：

```sh
python3 -m py_compile products/yingban/integrations.py products/yingban/settings.py products/yingban/tests/test_product.py
node --check products/yingban/web/admin.js
```

浏览器实机验证：

- 后台新选择器可发现并可操作。
- 从旧 Brave 配置切换 Tavily 后，根地址与标签立即变化。
- 保存后页面显示 Tavily 为选中值，状态提示“联网配置已保存，并已立即生效”。
- TMDB 仍显示“电影与海报已接入”。
- 784 px 宽度下表单保持现有响应式布局，没有引入新的横向溢出。
- 没有使用真实 Tavily Key，因此未执行会消耗额度的最终连接测试。

## 13. 启动与使用方法

启动：

```sh
cd products/yingban
python3 app.py serve
```

访问：

- 用户端：`http://127.0.0.1:8765/`
- 管理端：`http://127.0.0.1:8765/admin`
- 联网页锚点：`http://127.0.0.1:8765/admin#internet-settings`

配置 Tavily：

1. 打开 [Tavily Platform](https://app.tavily.com) 并登录或注册。
2. 在控制台复制 API Key；不要把 Key 发到聊天、截图、提交记录或阶段文档。
3. 管理后台选择“Tavily（推荐 · 每月免费额度）”。
4. 保持根地址 `https://api.tavily.com`。
5. 把 Key 粘贴进“Tavily API Key”。
6. 点击“测试 Web Search”。这会产生一次真实 `basic` 搜索，按当前规则消耗 1 credit。
7. 测试成功后点击“保存联网配置”。

## 14. 尚未完成事项、已知风险与阻塞

尚未完成：

- 缺少真实 Tavily API Key，尚未做真实中文、豆瓣、知乎搜索质量和额度仪表盘验收。
- 豆包语音仍缺凭据，尚未完成真实 STT/TTS 端到端验收。
- 当前没有在管理页显示 Tavily 剩余 usage；只能在 Tavily 控制台查看。
- 当前搜索没有缓存；相同问题会重复消耗 credit。
- 当前没有结果去重、来源可信度标签和网页发布日期标准化。
- 当前公开页面正文阅读依赖外部 `r.jina.ai`，可用性和站点政策变化会影响结果。

已知风险：

- 免费额度和套餐规则可能变化；所有“1,000/月、无需绑卡、basic 1 credit”均应在上线前重新查官方文档。
- Tavily 对中国站点的收录、更新速度和摘要质量需要真实数据验证，不能仅由协议测试证明。
- 免费额度耗尽会返回 429 或供应商错误；当前不会自动切换备用服务商。
- `app_settings` 中密钥是后端明文配置，虽文件权限收紧但不等同专用密钥管理服务；公开部署应迁移到 Secret Manager。
- 从 Tavily 回滚旧版代码前必须处理根地址兼容，否则旧版会以 Brave 协议调用 Tavily。
- 搜索摘要可能不准确、过时或缺少上下文，Agent 必须引用来源并保留不确定性。

当前真实阻塞：

- Tavily API Key 未提供。
- 豆包语音 App Key 或旧版 App ID + Access Token 未提供。
- 是否需要在后台增加“本月已用/剩余额度”尚未由用户决定。

## 15. 下一阶段建议

建议第六阶段优先级：

1. 用户配置真实 Tavily Key 后，做全网、豆瓣、知乎三类查询的端到端验收。
2. 增加短期搜索缓存，例如按 `provider + source + normalized_query` 缓存 6 至 24 小时，避免重复消耗免费额度；必须定义失效与隐私边界。
3. 可选调用 Tavily `/usage`，在后台显示 key/account usage、limit 和剩余额度；不得在前端返回完整 key。
4. 对同 URL 搜索结果去重，并明确标记来源域名与外部链接。
5. 取得豆包凭据后完成真实麦克风录音、转写、文本回复和用户主动播放语音闭环。
6. 在真实使用数据出现后再评估是否需要更多免费备用供应商；不要提前堆叠未验证适配器。

## 16. AI 接手入口

下一位 AI 必须依次：

1. 读取根目录 `AGENTS.md`。
2. 读取本文件，不以第四阶段的 Brave 默认状态覆盖当前 Tavily 状态。
3. 只读检查 `settings.py`、`integrations.py`、`admin.html`、`admin.js` 和测试中 provider 契约。
4. 运行 19 项测试。
5. 通过只返回掩码的管理员接口或后台 UI 检查是否已配置 Tavily；禁止查询或输出完整 `app_settings` 密钥值。
6. 如果没有真实 Key，只能完成离线、协议模拟和页面验证，不能声称真实搜索已通过。
7. 完成下一次边界明确的开发后，创建 `products/yingban/第六阶段技术开发文档.md`，不得覆盖本文件。
