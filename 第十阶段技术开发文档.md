# 影伴第十阶段技术开发文档

> 文档日期：2026-08-22
> 阶段主题：P1 留存、连续性与主动服务
> 开发分支：`feature/yingban-stage-9`（用户明确要求继续在该分支开发；未切换、未合并、未改动主分支）
> 文档性质：下一次版本迭代 AI 的工程交接上下文和当前状态快照，不是宣传稿。

## AI 快速接手卡

- **当前阶段**：第十阶段，P1“留存、连续性与主动服务”。
- **完成状态**：P1-01 至 P1-07 的代码、幂等迁移、API、页面、自动化、桌面和 390×844 窄屏真实浏览器验收均已完成；没有合并主分支，也没有创建提交。
- **权威代码目录**：`/Users/Admin/Tool/agent-blueprint/products/yingban/`。
- **当前分支**：`feature/yingban-stage-9`。名称仍含 stage-9 是用户明确决定，不代表代码只到第九阶段。
- **启动入口**：`cd /Users/Admin/Tool/agent-blueprint/products/yingban && python3 app.py serve`。
- **用户端**：`http://127.0.0.1:8765/`。
- **管理端**：`http://127.0.0.1:8765/admin`。
- **公共分享页**：`http://127.0.0.1:8765/share?token=<随机令牌>`。
- **测试命令**：`cd /Users/Admin/Tool/agent-blueprint/products/yingban && python3 -m unittest discover -s tests -v`。
- **当前测试基线**：49 项，2026-08-22 全部通过；第一至第九阶段的 36 项意义未删除。
- **当前运行模式**：主配置只读检查为真实模型 `model` 模式，模型标识 `deepseek-v4-flash`；交接时服务未保持运行。真实浏览器验收使用 `/private/tmp` 独立数据库和 `demo` 模式，未向外部模型发送验收聊天，也未向主数据库写入测试用户数据。
- **当前主数据库时点**：账户 2、电影 60、电影状态 8、账户画像 1、电影反馈 5、推荐曝光 21、推荐反馈 0、观后感版本 1、产品事件 33、用量事件 9；本阶段五张新表均为 0，`PRAGMA foreign_key_check` 为空。
- **核心代码入口**：`app.py` 组装与启动；`server.py` HTTP、分享校验、短观感信号识别、月度分享快照、后台任务和短时音频缓存；`storage.py` schema、迁移、账户隔离和状态语义；`catalog.py` 推荐硬排除；`agent.py` 双 Agent、观后感上下文取样和整理；`web/app.js` 本机草稿与用户流程；`web/share-card.js` 安全 DOM 视觉渲染；`web/share.js` 公共令牌加载；`tests/test_product.py` 49 项契约。
- **当前运行架构**：Python 标准库 `ThreadingHTTPServer` + SQLite + 浏览器原生 HTML/CSS/JavaScript；后台任务由当前服务进程的守护线程执行，不是跨进程持久队列。
- **不可破坏的产品约束**：推荐在数据层硬排除已看；看过事实、观看评价、当下不想看和持续不感兴趣分离；AI 身份透明；账户数据隔离；原始聊天和音频不进长期数据库；本机草稿默认 7 天且可清空；服务端电影摘要必须显式同意；会话摘要与观后感独立；未确认内容不可分享；语音、观后感和海报不得阻塞文字主回复；不开发商业化。
- **已确认的第十阶段决策**：本机草稿保留 7 天，并保留未来替换为云端保存适配器的可能；本周建议首先显示在首页，卡片可关闭本周，设置页可永久停用或重新开启。
- **已知阻塞**：正式成本价格表仍需管理员依据供应商账单填写；当前后台 worker 无跨进程恢复；公网部署、正式账户、数据导出/删除和多人权限属于第十一阶段。
- **下一阶段文档名**：`第十一阶段技术开发文档.md`，不得覆盖本文档。
- **建议第一步检查**：完整读取根 `AGENTS.md`、`影伴产品开发总文档.md` 和本文档；确认分支及工作区；复跑 49 项测试；只读核对 `/api/health`、schema、数据库数量、外键、任务状态和真实第三方配置。

## 1. 需求背景

第九阶段已经完成 5 部电影冷启动、证据型口味画像、曝光级反馈、版本化观后感和非敏感产品健康汇总，但用户刷新页面后仍缺少讨论连续性，首页没有克制的回访理由，想看列表没有后续动作，观后感与语音仍可能增加等待，历史列表没有规模化分页，也没有用户控制的回顾和分享资源。

第十阶段解决的核心不是“增加通知”或“长期保存聊天”，而是在不扩大隐私收集的前提下形成以下 P1 闭环：

`7 天本机草稿 / 显式电影摘要 → 首页本周建议 → 想看跟进 → 月度电影回顾 → 用户确认内容 → 可过期、可撤回分享`。

同时把语音、观后感和海报从主请求中拆到可观测任务，使文字主回复和历史列表优先可用。

## 2. 阶段目标

1. 在不长期保存原始聊天的前提下，让用户恢复当前设备上的进行中对话，并显式选择是否保存电影讨论摘要。
2. 让首页每周提供最多一份克制建议，优先使用想看列表和已确认口味，并让用户随时关闭。
3. 让想看列表拥有四种语义明确的后续动作，不把“暂时不看”误记为长期不喜欢。
4. 用当月真实电影状态和已确认观后感组织月度回顾，不生成用户人生叙事。
5. 让用户只分享自己确认的内容，分享前可预览，链接随机、有限期、可撤回且公共响应脱敏。
6. 让语音、观后感和海报进入账户隔离、可重试、可过期、可观测的后台任务，不阻塞文字主路径。
7. 让历史列表稳定分页并返回总数，补图不改变账户状态、反馈、观后感、时间和来源。

## 3. 进入条件验证

开发前完成了以下只读基线检查：

- 分支为 `feature/yingban-stage-9`，工作区起始干净。
- 主配置为 `model` 模式，模型标识 `deepseek-v4-flash`。
- 第九阶段基线测试 `36/36 OK`，耗时约 6.24 秒。
- 主数据库迁移前为账户 2、电影 60、电影状态 8、画像 1、反馈 5、推荐曝光 21、推荐反馈 0、观后感 1、产品事件 33、用量事件 9；外键检查为空。
- 第九阶段遗留的真实窄屏验收缺口在本阶段已补齐。
- 用户明确确认草稿保留 7 天、本周建议首页出现和两级关闭位置；因此没有用代码默认替代产品决策。

## 4. 继承的已有能力

以下能力继续有效，不得因第十阶段重构而丢失：

- 邀请码首次使用创建账户，同一码回到同一账户；停用与换发使旧凭证和会话失效但保留账户数据。
- 空白账户必须选择恰好 5 部真实看过电影，以 `positive/neutral/negative` 三档反馈建立证据型画像；负向反馈不覆盖 `watched` 事实。
- 口味画像可见、可修正、有版本，只描述电影偏好，不推断人格、心理、医疗、家庭、工作或身份。
- `discussion` 与 `recommendation` 使用独立系统提示词、三套独立开场白和最多 8 轮受限电影工具。
- 推荐候选在目录层硬排除已看和持续不感兴趣，不依赖模型自觉去重。
- 推荐卡拥有账户隔离 `impression_id`，反馈必须校验曝光归属；`not_now` 与 `not_interested` 语义分离。
- 观后感以 `(account_id, movie_id, version)` 追加版本；AI 先生成 `draft`，用户可确认、编辑、锁定、解锁和删除；删除观后感保留已看。
- 电影目录支持种子片、TMDB 外部片持久化、Tavily/Brave 公开搜索、受限豆瓣/知乎阅读和海报文字降级。
- 原始录音和合成音频不长期保存；文字主回复始终保留。
- 产品事件过滤消息、正文、密钥、令牌和原始查询；用量事件只记录服务、成败、延迟、单位、价格版本和估算金额。
- 管理接口不返回模型、联网、语音完整密钥；SQLite 文件仍尝试收紧为当前系统用户可读写。
- 高风险安全降级优先于电影任务，安全事件只记录类别，不保存原话。

## 5. 范围内功能

### 5.1 P1-01：电影会话连续性

- 浏览器本机草稿默认保留 7 天，页面刷新和重新进入后可恢复。
- 用户可清空当前草稿；退出登录时自定义对话框提供“保留草稿并退出”和“清空草稿并退出”。
- 只有用户点击“下次继续聊”、查看预览并保存后，服务端才写入电影会话摘要。
- 服务端摘要只保存电影 ID、讨论进度、主题、未完问题、剧透许可、同意状态和时间。
- 摘要可查看、更新和删除；删除摘要清空摘要正文，但不删除观后感。
- 打开聊天时先恢复本机草稿；没有本机草稿时才使用服务端电影摘要作为明确标识的上下文提示。

### 5.2 P1-02：本周电影建议

- 首页是第一入口，完成冷启动后自动读取本周资源。
- 每个账户、每个 ISO 自然周默认只有一份资源；重复 GET 不重复生成。
- 候选最多 3 部，优先想看列表，不足时回退目录推荐；保存前再次检查已看硬排除。
- 数据不足时显示真实空状态，请用户补充本周方向，不伪装高确定个性化。
- 用户可输入最多 300 字符的方向并“换个方向”。显式刷新每账户每小时最多 3 次，第 4 次返回 HTTP 429 和解释。
- 卡片内“本周不再显示”只关闭当前周；设置中可永久停用，停用后后续周不自动生成，并可重新开启。

### 5.3 P1-03：想看列表跟进

- `keep`：保留 `watchlist`。
- `watched`：转换为 `watched`，来源 `watchlist_follow_up`，可继续进入讨论。
- `not_now`：删除当前 `watchlist` 状态，不写 `disliked`，不进入持续排除。
- `not_interested`：转换为 `disliked`，后续推荐持续排除，可附结构化 `reason_code`。

### 5.4 P1-04：月度电影回顾

- 用户在“我的电影”显式打开和生成当前 `YYYY-MM` 回顾。
- 回顾使用当前账户当月 `user_movie_states.updated_at` 对应电影状态。
- 观后感来源只允许当月 `confirmed/locked`，并保存 `source_reflection_ids` 回链。
- 主题来自对应真实电影的主题字段，并保留来源观后感 ID；不总结现实人生。
- 草稿确认时用户可填写最多 300 字符的下月观影方向。
- 数据不足时保存和显示结构化空列表，不生成虚构结论。

### 5.5 P1-05：用户控制分享卡

- 来源只允许已确认/锁定观后感、已确认月度回顾或当前可见口味维度。
- 服务端根据当前账户重新读取和校验来源，不信任前端只传的 `source_id` 或正文。
- 创建前显示最终预览，用户可编辑 1–1200 字符正文或取消。
- 月度回顾本身就是创建前的确认预览；确认后点击“生成精美分享卡”会显示忙碌状态，等待 `POST /api/share-cards` 成功，再自动 `window.location.assign(data.share_path)` 打开 HTML 成品，不再重复打开纯文字对话框。
- 月度回顾分享创建时由服务端重新读取所有当月 `watched` 电影，并固化标题、原名、年份、最多 3 个类型和封面 URL；同时固化电影数量、确认观后感数量、真实电影主题、用户确认的下月方向，以及仅由这些字段确定性组合的故事标题和故事正文。
- 公共 HTML 使用上述结构化快照渲染完整片单；封面 URL 失败或为空时保留酒红色文字封面，不隐藏电影条目。
- 内容固定包含“由影伴 AI 协助整理”。
- 分享令牌使用 `secrets.token_urlsafe(24)`，默认 7 天，可选 1–30 天；用户可立即撤回。
- 公共分享页不要求登录，响应删除 `account_id` 和令牌本身，HTML 声明 `noindex,nofollow,noarchive`。
- 过期、撤回和不存在统一显示不可访问，不泄露账户或资源枚举信息。

### 5.6 P1-06：语音与观后感性能

- `compact_voice_text()` 默认把长回复压缩到约 140 个字符，对应约 15–30 秒摘要；完整朗读必须由用户次级点击触发。
- 前端语音主路径调用 `POST /api/jobs/voice` 并轮询任务；文字回复先可用。
- 前端观后感整理使用 `async=true` 创建任务并轮询；旧同步接口保留兼容。
- 默认任务 `max_attempts=2`；每次进入 `running` 增加 `attempt_count`。第一次瞬时错误后立即进行第二次尝试，最终失败只记录错误类别。
- 到达 `expires_at` 的 `queued/running` 任务在读取时改为 `expired`，不继续作为成功任务暴露。
- 合成音频只存放在进程内 `EPHEMERAL_AUDIO`，随机任务 ID 作为访问边界，10 分钟后不可获取；数据库不保存音频或 base64。

### 5.7 P1-07：历史分页和海报任务

- `GET /api/history` 支持 `cursor` 和 `limit`；limit 被约束在 1–50，返回 `items/total/cursor/next_cursor`。
- 分页沿用 `movie_states()` 的稳定排序，相邻页不重复。
- 当前页缺失海报时只创建 `poster_hydration` 任务并返回 `poster_job_id`，不再在历史响应内串行网络补图。
- 后台补图只更新电影目录海报数据，不修改当前账户电影状态、反馈、观后感、创建时间或来源。

## 6. 明确不做

- 不开发会员、订阅、支付、广告、金币、积分或机构计费。
- 不默认发送邮件、短信、系统通知或站外提醒；当前账户没有这些联系方式。
- 不保存全量原始聊天，不把 7 天本机草稿自动上传云端。
- 不创建公开影评广场、关注关系、评论区或算法信息流。
- 不创建无限期分享链接，最长 30 天；不分享未确认草稿或账户身份。
- 不做可下载的 PNG/JPEG 图片或音频导出；当前交付是服务端托管、可直接观看和分享链接的 HTML 电影月刊，月度版本包含电影封面但不是图片文件导出。
- 不接入“在哪里看”。当前没有经确认、包含地区和更新时间的合规稳定片源数据源，禁止让模型猜测。
- 不在本阶段实现跨进程消息队列、正式用户账户、数据导出/整账户删除、多人共享或公网部署。

## 7. 架构与关键决策

### 7.1 本机草稿是可替换适配器，不是服务端记忆

`web/app.js` 的 `localDraftStore` 固定写入：

- `schema_version: 1`
- `storage: "local-browser"`
- `account_hint`
- `mode`
- `movie_id`
- `input`
- `history`
- `updated_at`
- `expires_at`

键由账户提示、模式和电影组成，避免两个账户或两部电影互相恢复。读取时校验版本、存储类型、账户、模式、电影和过期时间；无效项立即忽略并清除。

保留未来云端可能的意义是：调用方只依赖 `read/write/remove/clearAll`，以后可替换实现。它不意味着用户已同意云同步；云端版必须重新定义认证、端到端删除、冲突、设备列表、加密和退出时清理。

### 7.2 会话摘要与观后感分表、分同意、分删除

`conversation_summaries` 回答“下次从哪里继续”，`movie_reflections` 回答“用户希望留下什么”。前者可以被更新为当前讨论进度，后者使用追加版本和确认/锁定。删除会话摘要只软删除并清空摘要正文；删除观后感不触碰摘要。任何自动合并会让用途和同意范围混淆，因此明确禁止。

### 7.3 主动建议使用周资源和账户偏好两层状态

当前周的 `dismissed` 不等于永久停用。永久偏好保存在 `account_profiles.weekly_recommendations_enabled`。同周资源唯一约束避免每次首页刷新重复生成；用户显式换方向才允许同周 upsert。近一小时刷新次数复用隐私过滤后的 `product_events`，无需额外存储用户方向正文。

### 7.4 后台任务只保存引用，正文由调用现场持有

`background_jobs.input_reference_json` 只保存电影 ID、电影 ID 列表、消息条数、字符数和摘要/完整模式。观后感近期 `history` 和语音正文只在创建线程的闭包中存在，不写通用队列表。这一选择减少数据库正文复制，但也意味着当前守护线程不能在服务重启后恢复执行。

### 7.5 分享来源由服务端重新校验

前端预览文字可编辑，但服务端仍先读取当前账户来源并验证状态：观后感必须 `confirmed/locked`，回顾必须 `confirmed`，口味维度必须存在且未隐藏。月度回顾使用已确认回顾对话框作为最终预览，创建时服务端再次水合电影目录并生成 `content.visual`，因此公开页不需要账户认证也不会在每次打开时读取账户私有记录。服务端把传入来源 ID 规范化为稳定 ID，再创建随机令牌。公共读取与拥有者读取使用不同方法，公共对象移除账户和令牌。

## 8. 已实现功能

### 8.1 用户端

- 首页新增“这周，可以从这里开始”，支持方向输入、3 张电影卡、本周关闭和空状态。
- 首页新增“连续性与主动建议设置”，解释本机 7 天、服务端摘要和周建议边界。
- 聊天页新增草稿恢复提示、清空和“下次继续聊”。
- 退出登录不再使用浏览器原生 `confirm`，改为产品内对话框，区分保留和清空本机草稿。
- “我的电影”新增加载更多、当月回顾和想看四动作。
- 观后感继续使用独立文字预览；月度回顾在已确认对话框直接生成，按钮显示“正在生成分享卡…”，成功后自动跳转到 HTML 成品，失败时恢复按钮并显示原因。
- 语音胶囊默认请求摘要；“完整朗读”是明确次级按钮。
- 新增公共分享页和撤回控制；月度分享页是电影月刊式长页面，包含故事首屏、全部电影封面片单、观后感计数、主题余韵、下月方向和 AI 协助标识。
- 修复冷启动完成后关闭口味画像仍停留在冷启动页的问题；现在完成后先导航首页，再显示画像。

### 8.2 服务端与数据层

- 新增五张表、一列账户偏好和对应索引。
- 新增电影会话摘要、周建议、想看跟进、月度回顾、分享、后台任务和分页方法。
- 新增周建议每小时 3 次持久限流。
- 后台任务支持 `queued/running/succeeded/failed/expired`、默认两次自动尝试、账户隔离、过期状态和非正文结果。
- 管理健康汇总新增 `continuity` 分组，包含会话摘要、周建议、回顾、任务和分享各状态数量。

## 9. 数据结构与迁移

### 9.1 `account_profiles` 新列

- `weekly_recommendations_enabled INTEGER NOT NULL DEFAULT 1 CHECK(... IN (0,1))`。
- 旧库通过 `PRAGMA table_info(account_profiles)` 检查后执行幂等 `ALTER TABLE`。
- 旧账户没有 profile 行时，读取仍合成默认开启；首次写偏好时 `_ensure_profile()` 建行。

### 9.2 新表 `conversation_summaries`

- 主键 `id`，外键 `account_id/movie_id`，账户或电影删除时级联。
- `summary/topics_json/open_questions_json/spoilers_allowed/consent_status`。
- `created_at/updated_at/deleted_at`。
- 唯一约束 `(account_id, movie_id)`。
- 删除时正文、主题和问题清空，`consent_status='revoked'`，保留最小审计时间。

### 9.3 新表 `weekly_recommendations`

- `id/account_id/week_key/movie_ids_json/reason_summary/source_inputs_json`。
- `status`：`ready/viewed/dismissed/expired`。
- `created_at/viewed_at/expires_at`。
- 唯一约束 `(account_id, week_key)`；索引 `idx_weekly_recommendations_account`。

### 9.4 新表 `monthly_recaps`

- `id/account_id/month_key/content_json`。
- `source_movie_ids_json/source_reflection_ids_json`。
- `status`：`draft/confirmed/deleted`。
- `created_at/updated_at`。
- 唯一约束 `(account_id, month_key)`；索引 `idx_monthly_recaps_account`。

### 9.5 新表 `background_jobs`

- `id/account_id/job_type/status`。
- `input_reference_json/result_json`。
- `attempt_count/max_attempts/available_at/error_category`。
- `created_at/started_at/finished_at/expires_at`。
- 状态：`queued/running/succeeded/failed/expired`；索引 `idx_background_jobs_account`。

`result_json` 是总文档基线之外的必要实用字段，用来让轮询端获得观后感版本、补图电影 ID 或音频是否仍可取；禁止存正文或音频。

### 9.6 新表 `share_cards`

- `id/account_id/public_token/source_type/source_id/content_json`。
- `source_type`：`reflection/monthly_recap/taste_dimension`。
- `status`：`active/revoked/expired`。
- `created_at/expires_at/revoked_at`。
- `public_token` 唯一；索引 `idx_share_cards_account`。

### 9.7 迁移方式、兼容性与回滚

`Store.initialize()` 使用 `CREATE TABLE IF NOT EXISTS`、`CREATE INDEX IF NOT EXISTS` 和列存在性检查幂等迁移，无单独迁移工具。旧表和旧字段不删除，旧接口的同步语音/观后感仍兼容。

实际迁移没有改变账户、电影、状态、画像、反馈、曝光和观后感数量；五张新表在主库为空，外键检查为空。

回滚风险：第九阶段旧代码会忽略新表和新列，但不能提供连续性、周建议、回顾、分享和任务轮询。不得通过删除新表回滚，因为上线后其中可能含用户授权摘要、确认回顾和有效分享撤回状态。代码回滚时应停止新写入并保留数据；若分享路由回滚，应先撤回或隔离公共访问，不能留下不可管理链接。

## 10. API 变化

### 10.1 新增公共和用户接口

| 方法 | 路径 | 责任与边界 |
|---|---|---|
| GET | `/api/public/share-cards/{token}` | 无认证读取有效卡；不返回账户和令牌 |
| GET/POST/DELETE | `/api/conversations/{movie_id}/summary` | 读取、显式保存/更新、独立软删除电影摘要 |
| GET | `/api/weekly-recommendation` | 读取或首次生成本周建议；标记已查看 |
| POST | `/api/weekly-recommendation/refresh` | 同周换方向；每小时 3 次，超过返回 429 |
| POST | `/api/weekly-recommendation/dismiss` | 关闭本周；可同时永久停用 |
| POST | `/api/weekly-recommendation/preferences` | 永久停用或重新开启后续周建议 |
| POST | `/api/watchlist/{movie_id}/follow-up` | `keep/watched/not_now/not_interested` |
| GET | `/api/recaps/monthly/{YYYY-MM}` | 回顾或 `empty=true` |
| POST | `/api/recaps/monthly/{YYYY-MM}/generate` | 生成当月结构化草稿 |
| POST | `/api/recaps/monthly/{YYYY-MM}/confirm` | 确认草稿和下月方向 |
| POST | `/api/share-cards` | 服务端校验来源后创建卡，返回 201 和 `share_path` |
| DELETE | `/api/share-cards/{id}` | 当前账户撤回，其他账户不能操作 |
| POST | `/api/jobs/voice` | 创建摘要或完整朗读任务，返回 202 |
| GET | `/api/jobs/{id}` | 当前账户轮询任务和短时语音结果 |

### 10.2 修改接口

- `GET /api/me` 增加 `weekly_recommendations.enabled`。
- `GET /api/history?state=&cursor=&limit=` 返回分页对象，并在需要时增加 `poster_job_id`；不再同步补图。
- `POST /api/reflections/{movie_id}/generate` 支持 `async=true`，返回 202 任务；同步模式仍兼容。
- `GET /api/admin/metrics/summary` 增加 `continuity.conversation_summaries/weekly_recommendations/monthly_recaps/background_jobs/share_cards` 状态汇总。
- 静态路由新增 `/share`、`/share/`、`/share.html`。

## 11. 页面与交互变化

### 11.1 `web/index.html`

- 首页本周建议卡、连续性设置入口。
- 聊天草稿提示、清空和“下次继续聊”。
- 历史加载更多、月度回顾。
- 观后感分享。
- 电影会话摘要、月度回顾、分享预览、连续性设置、退出选择五类新对话框。

### 11.2 `web/app.js`

- `localDraftStore` 负责 7 天本机草稿和未来适配边界。
- 周建议、摘要、回顾、分享、想看动作、任务轮询和分页状态进入集中 `state`。
- `pollJob()` 统一轮询语音和观后感任务。
- `completeOnboarding()` 完成后先 `navigate("home")`，再显示画像。
- 自定义退出对话框取代原生确认，避免浏览器自动化和可访问性问题。
- `createMonthlyRecapShare()` 负责按钮忙碌态、7 天分享创建、错误恢复和成功后的 HTML 自动跳转；月度回顾不再经过 `openSharePreview()`。

### 11.3 `web/styles.css`

- 新增周建议、草稿提示、工具型对话框、分享页、完整朗读和窄屏样式。
- 月度 HTML 分享采用深色影院背景、酒红故事首屏、2:3 封面网格、真实主题标签、下月方向与完整页脚；桌面 4 列、900px 以下 3 列、680px 以下 2 列。
- 观后感“离开有效电影讨论时自动生成草稿”从受全局 `input { width:100%; height:52px }` 影响的大块选项修正为内容宽度的轻量设置行；复选框固定为 16×16px，整行最小点击高度 34px，文字保持 11px 次级层级并保留键盘焦点样式。
- 390×844 下导航、首页卡、对话框按钮和内容均无横向溢出；`innerWidth=390`，文档宽 375。

### 11.4 `web/share.html`、`web/share.js`、`web/share-card.js`

- `share.js` 只负责读取公共令牌、更新文档标题并把安全内容交给渲染器。
- `share-card.js` 只用 `createElement` 和 `textContent` 渲染，不用 `innerHTML`；月度变体完整显示全部电影，其他观后感/口味分享继续使用兼容的精简卡片。
- 只读显示标题、故事、片单、封面、电影元数据、主题、下月方向和 AI 协助标识；封面加载失败自动回退到同尺寸文字封面。
- 公共分享页顶部提供明确的“← 返回影伴”按钮，链接到 `/`；移动端隐藏说明文字但保留品牌与返回按钮，不能要求用户猜测品牌标识可以点击。
- 令牌缺失、已撤回、已过期或不存在显示统一空状态。
- 不包含登录、评论、关注、再分享或账户入口。

分享渲染脚本资源版本为 `v=12`；样式资源更新为 `v=14`，用于避免浏览器继续使用没有明确返回按钮或仍带超大观后感复选框的缓存样式。

## 12. Agent 行为变化

- `discussion` 和 `recommendation` 的系统提示词、工具数量、8 轮限制、AI 身份、剧透和安全边界没有扩大。
- 原始聊天仍只作为当次请求和本机草稿上下文；服务端摘要不是 Agent 自动长期写入。
- 恢复服务端摘要时，前端把它作为明确的“上次电影讨论摘要”提示，不伪装成原始历史消息。
- 观后感整理仍由 `summarize_reflection()` 窄职责调用完成，输入边界与第九阶段相同；第十阶段只改变调度为后台任务，不授权自动确认或覆盖。
- 任务第一次异常后最多重试到 `max_attempts`；演示模式不伪造观后感草稿。
- 月度回顾当前是确定性数据组织，不调用自由文本人生总结模型。
- 本周建议当前使用想看列表和目录推荐，不新增主动心理关怀提示词，也不保存用户当周方向原文到事件。

## 13. 配置与第三方变化

- `.env` 没有新增必须配置。
- 模型、TMDB、搜索、阅读器和语音仍以管理页写入 `app_settings` 后为权威来源。
- 本机草稿保留 7 天当前是前端产品常量 `retentionMs: 7 * 24 * 60 * 60 * 1000`，不是服务端环境变量。
- 音频进程缓存固定 600 秒；后台任务默认两次尝试，创建方法允许 1–5 次。
- 周建议显式刷新固定每账户每小时最多 3 次；限制基于 SQLite 产品事件，可跨服务重启保持。
- 没有新增第三方网络依赖。公共分享托管在同一影伴服务。
- 正式成本价格仍为管理员配置；当前默认 `unpriced-v1 / ¥0` 仅表示未定价。

## 14. 安全、隐私与合规边界

- 本机草稿不上传，但共用设备同一浏览器配置文件中的其他系统用户可能看到；界面明确 7 天并提供清空。
- 完整邀请码、会话令牌、管理员口令、API 密钥和真实测试凭据不得进入本文档、Git 或公开分享。
- 会话摘要保存前需要用户按钮动作和预览；字段长度、列表数量、电影存在性和当前账户均由服务端验证。
- 摘要不得包含现实身份、家庭、工作、学校、联系方式、心理、医疗、危机原话或无关生活记录；当前前端用电影消息生成预览，仍需上线前增加系统化敏感样例评测。
- 所有新私有表查询都带 `account_id`；任务、分享撤回、回顾、摘要和想看动作均有跨账户测试。
- 分享只允许已确认来源；公共响应不含账户、邀请码、聊天原文、未确认草稿和令牌回显。
- 分享页面声明禁止搜索索引，但 `noindex` 不是访问控制；真正边界是高熵随机令牌、过期和撤回。
- 音频 base64 只在当前账户轮询成功任务时注入响应，数据库不保存。
- 后台错误只保存异常类别，产品事件属性过滤正文和凭据。
- 观后感整理的有效输入不再只看“用户消息至少 12 字”；`server.has_reflection_signal()` 对长句继续放行，同时显式接受“还好、还行、一般、没感觉、喜欢、不喜欢”等短而真实的电影反应，裸“是的、好的”仍不构成观后感。
- `AgentRuntime._reflection_context()` 从清洗后的完整本机对话中最多保留 24 条：前 12 条用于保留最初的目标电影语境，后 12 条用于保留最近补充；用户消息最多 1200 字符，AI 消息最多 600 字符。该上下文只随当次模型请求存在，不写入长期数据库。
- 观后感系统提示词明确：只有 `role=user` 是用户观感证据，`role=assistant` 只能帮助理解问答关系；必须忽略用户向 AI 提问但未表达自身观感的句子、明显属于其他电影的内容和 AI 自己的判断。
- 观后感草稿允许 20–500 个中文字符；内容少时必须短而忠实，不能为了旧有 80 字下限扩写。没有任何安全可保存电影感受时仍返回 `NO_UPDATE`，不会伪造草稿。
- 当前 SQLite、标准库 HTTP、同进程线程和本地密钥存储只适合本地或受控环境，不构成公网生产安全。

## 15. 修改文件清单

- `products/yingban/storage.py`：账户周建议偏好、五张新表、幂等迁移、摘要、周建议、想看跟进、回顾、分享、任务、分页、限流事件计数和连续性指标。
- `products/yingban/server.py`：新 GET/POST/DELETE 路由、分享来源校验、异步任务、两次自动尝试、任务过期、短时音频和分享静态路由。
- `products/yingban/web/index.html`：周卡、连续性、草稿、回顾、分享和退出对话框。
- `products/yingban/web/app.js`：7 天本机适配器、全部第十阶段用户流程、任务轮询、分页和冷启动落点修复。
- `products/yingban/web/styles.css`：第十阶段页面、分享和响应式样式。
- `products/yingban/web/share.html`：新增公共分享页结构与 robots 边界。
- `products/yingban/web/share.js`：新增公共令牌读取、显示和不可访问状态。
- `products/yingban/web/share-card.js`：新增无 `innerHTML` 的月度 HTML 电影月刊和兼容型普通分享渲染器。
- `products/yingban/tests/test_product.py`：第十阶段单元、HTTP、隔离、限流、重试、过期和 UI 契约测试。
- `products/yingban/README.md`：能力、隐私、测试数量、目录和生产化缺口。
- `products/yingban/影伴产品开发总文档.md`：当前状态、数据字典、API、Agent、实际完成、风险、测试和决策。
- `products/yingban/第十阶段技术开发文档.md`：本文档。

## 16. 测试与验收结果

### 16.1 自动化

执行：

```sh
cd /Users/Admin/Tool/agent-blueprint/products/yingban
python3 -m py_compile storage.py server.py tests/test_product.py
node --check web/app.js
node --check web/share.js
node --check web/share-card.js
python3 -m unittest discover -s tests -v
```

结果：Python 编译、三个 JavaScript 语法检查通过；完整自动化 `49/49 OK`。

第十阶段新增 13 项测试意义：

1. 电影会话摘要显式保存，与观后感独立删除。
2. 本周建议周内唯一，保存前硬排除已看，关闭和永久停用分离。
3. 想看四动作状态语义正确。
4. 月度回顾账户隔离，只引用确认/锁定观后感。
5. 分享随机、过期、撤回，公共对象不含账户和令牌；月度分享固化完整电影视觉快照和故事字段。
6. 后台任务只保存引用、读取按账户隔离、结果可观测。
7. 到期排队/运行任务转换为 `expired`。
8. 历史分页返回总数，相邻页不重复。
9. UI 包含 7 天本机、摘要、周卡、回顾、分享和异步控制。
10. HTTP 连续性、周建议、想看、回顾、分享和撤回闭环。
11. 每小时 3 次周建议刷新限流，以及观后感任务第一次失败后第二次自动成功。
12. “还好”“感觉一般”等短而明确的电影评价可触发整理，裸“是的”“好的”仍被拒绝。
13. 长对话跨到其他电影后，观后感输入仍保留早期短评价、最多 24 条，并在系统约束中声明 AI 文字不能作为用户证据、短内容允许 20 字以上忠实草稿。

### 16.2 真实浏览器

验收使用独立端口 `127.0.0.1:8876`、`/private/tmp` 独立 SQLite、临时邀请码和 `demo` 模式，完成后服务已停止。没有修改主数据库用户数据。

桌面端通过：

- 新账户冷启动、首页、本周方向和 3 张建议卡。
- 讨论《无间道》后本机草稿刷新恢复。
- 电影会话摘要预览、显式保存和继续上下文。
- 月度回顾生成、确认和下月方向。
- 观后感分享预览、创建、公共无认证访问、撤回后立即 404。
- 在主数据库当前已确认的 2026-08 回顾上点击“生成精美分享卡”，按钮等待服务端创建后自动跳转到 `/share?token=...`；公开 HTML 实际显示 7 部电影的全部封面与元数据、1 篇观后感、4 个真实主题和用户填写的“科幻”下月方向。
- 桌面可见首屏实际检查通过：深色影院背景、酒红故事主视觉、标题“和 7 部电影，一起走过这个月”、故事正文和统计均正常；公共 DOM 中 7 部电影条目完整，没有退化成纯文字段落。
- 分享页和应用页控制台无警告或错误。
- 在当前账户《流浪地球》真实本机草稿上复现旧失败：原任务两次模型调用均以 `ValueError` 结束。修复后用相同对话点击“整理这次观后感”，任务成功生成第 1 版 AI 草稿“你对《流浪地球》的感觉是‘还好’‘还行’……”，没有混入后来《牛来》的 AI 自述；草稿仍未自动确认。

窄屏 390×844 通过：

- 第二个新账户从邀请码登录到 `0/5 → 5/5`，生成画像后首页正确位于画像对话框下方，关闭后 `home-view` 激活。
- 首页卡片、导航、文字和滚动正常，`innerWidth=390`，`documentElement.scrollWidth=375`，无横向溢出。
- 无打开残留对话框，控制台 warning/error 为空。
- 临时视口覆盖在验收后已调用 reset；浏览器能力报告重置成功。

### 16.3 主数据库只读核对

- 账户 2、电影 60、电影状态 8、画像 1、电影反馈 5、曝光 21、推荐反馈 0、观后感 1、产品事件 33、用量事件 9。
- `conversation_summaries/weekly_recommendations/monthly_recaps/background_jobs/share_cards` 均为 0。
- `PRAGMA foreign_key_check` 无输出。

## 17. 启动与使用方法

要求 Python 3.11+，核心服务只使用 Python 标准库。已有 `.env` 和数据库时不要覆盖。

```sh
cd /Users/Admin/Tool/agent-blueprint/products/yingban
cp .env.example .env              # 仅首次、尚无 .env 时
python3 app.py generate-invites 1 # 完整邀请码只显示一次
python3 app.py serve
```

用户使用顺序：邀请码登录 → 新账户完成 5 部电影 → 首页查看/关闭周建议 → 讨论或推荐 → 需要时保存“下次继续聊”摘要 → 在“我的电影”跟进想看或生成回顾 → 只从确认内容创建分享。

管理员使用顺序：以部署环境 `YINGBAN_ADMIN_TOKEN` 登录 `/admin` → 检查模型、联网、语音配置 → 查看不含正文的产品健康 → 依据真实供应商账单填写价格版本。价格保存只作用于后续事件。

## 18. 实际实现与总文档计划的差异

1. 总文档 `background_jobs` 基线没有列 `result_json`；实际增加该字段，以便轮询返回版本号、电影 ID 和音频可用状态。字段明确禁止正文和音频，隐私边界没有扩大。
2. 总文档要求后台任务“可超时”。当前通过 `expires_at` 和读取时状态转换实现产品级过期，但不能强制中断正在第三方调用中的 Python 线程；结果完成后若任务已过期不会再作为成功暴露。生产级强制取消属于第十一阶段 worker。
3. 总文档允许未来片源接口设计；本阶段没有合规稳定片源数据，因此没有显示“在哪里看”，也没有伪造模型猜测。
4. 总文档分享范围提到图片、文本或音频标识；本阶段实现服务端托管 HTML 分享页，月度版本包含电影封面和结构化故事，但没有实现 PNG/JPEG 下载或音频导出。
5. 本阶段仍在 `feature/yingban-stage-9` 开发，而不是新建 stage-10 分支，这是用户明确要求；主分支未动。

## 19. 尚未完成事项

- 管理员根据真实供应商账单填写正式成本价格表；当前 `unpriced-v1 / ¥0` 不代表免费。
- 生产级跨进程任务队列、租约、幂等执行、服务重启恢复、延迟重试和死信处理。
- 本机草稿未来是否云端同步尚未授权；只能保留架构可能，不得直接开发上传。
- 正式账户登录、邀请码资格与登录凭证分离、账户找回。
- 整账户数据导出和删除、数据保留期、备份恢复和审计。
- 双人/小组共同观影、成员权限、条目可见性和默认过期。
- 生产数据库、反向代理、HTTPS、CSRF、持久安全限流、密钥服务和小范围真实上线。
- 分享图片/音频导出和相应隐式 AI 标识；如开发需按上线地区规则重新评估。
- 片源数据；只有确认合规提供方、地区、更新时间和授权后才能进入产品。

## 20. 已知风险与回滚风险

- `localStorage` 在共用浏览器配置文件上不是强隔离，7 天内可能被同设备使用者看到；用户应可理解并使用清空。
- 本机草稿键使用账户末尾提示而不是秘密账户 ID，是显示/隔离提示，不是安全凭证；真正服务端数据仍靠 HttpOnly 会话校验。
- 前端从近期用户电影消息生成会话摘要预览，目前没有模型级敏感分类；上线前需增加敏感样例和服务端内容策略评测。
- 周建议生成当前是确定性目录推荐，没有单独模型成本，但推荐质量受片库、想看列表和画像质量限制。
- 周刷新限流基于成功事件；生成本身异常时不计入 3 次，防止服务错误消耗用户额度。
- 后台任务由守护线程执行，进程崩溃后不会自动恢复；数据库可能留下 `queued/running`，到期后轮询才会转 `expired`。
- 立即重试没有指数退避，适用于当前短用户任务，不适合生产第三方限流场景。
- 语音成功数据只存在当前进程 10 分钟，服务重启或多实例会丢失；文字回复仍可用。
- 公共分享令牌持有者在有效期内可以访问，撤回不能让已经复制或截图的内容消失；预览文案必须继续提醒用户。
- `noindex` 依赖爬虫遵守，不代替令牌、过期和撤回。
- SQLite 和 `ThreadingHTTPServer` 不适合公网并发和高可用；多实例还会使进程内音频与任务执行不一致。
- 旧代码回滚会失去新数据管理页面；不得删除新表或静默使仍有效分享无法撤回。

## 21. 产品指标与成本观测

新增产品事件包括：

- `conversation_summary_saved/deleted`
- `weekly_recommendation_generated/refreshed/dismissed`
- `weekly_recommendation_preference_changed`
- `watchlist_follow_up`
- `monthly_recap_generated/confirmed`
- `share_card_created/revoked`
- `reflection_job_created/reflection_draft_created`
- `voice_job_succeeded/background_job_failed`

事件属性只保存电影 ID、版本、动作、布尔、数量、月份、任务 ID/类型和分享卡 ID/类型等结构信息；`record_product_event()` 继续过滤 message、content、prompt、reflection、note、audio、api_key、invite_code、token、transcript 和 query。

`metrics_summary().continuity` 按状态汇总五类新资源，不返回正文。语音每次实际尝试都会记录 `model_usage_events.operation='tts'` 的成功或失败、延迟和价格版本；观后感、搜索、海报仍沿用既有用量记录边界。

当前正式价格表未配置，估算金额为 0。下一步必须使用供应商账单价格，而不是根据营销页面猜测；测试账户仍没有排除标识，分析真实留存前必须定义。

## 22. 下一阶段进入条件

第十一阶段主题为 P2“共同观影与上线就绪”。开始前必须：

1. 复跑 49 项测试并重新验证真实模式、数据库数量、外键和任务状态。
2. 确认双人/小组人数上限、默认过期时间、邀请与退出权限。
3. 确认正式账户登录方式和账户找回，结合上线地区与运营主体决定。
4. 确认生产数据库和部署平台，定义备份、恢复、迁移、并发、区域和运维责任。
5. 为共享数据定义成员、资源和条目三级权限，默认不共享个人观后感、摘要或画像。
6. 把进程内后台任务迁移为生产可恢复 worker，或明确关闭异步功能后上线；不得以当前守护线程冒充生产队列。
7. 配置真实成本价格并区分测试账户，获得至少可解释的成本基线。
8. 不提前开发商业化；是否商业化仍需真实上线数据后单独立项。

## 23. AI 接手入口

下一次 AI 必须按顺序执行：

1. 完整读取仓库根 `AGENTS.md`。
2. 完整读取 `影伴产品开发总文档.md`。
3. 完整读取本文档，不只读快速接手卡。
4. 只读检查 `git branch --show-current`、`git status --short`，保护用户现有改动；不得假定主分支已合并。
5. 只读核对启动方式、`/api/health`、`model/demo`、模型 ID 掩码、schema、五张新表、账户周建议偏好、主数据库数量和 `PRAGMA foreign_key_check`。
6. 执行 Python 编译、`node --check web/app.js`、`node --check web/share.js`、`node --check web/share-card.js` 和 49 项完整测试。
7. 重点复核本机草稿 7 天/清空、摘要显式同意、周建议两级关闭、想看四动作、回顾确认来源、分享撤回、任务过期与重试、历史分页和推荐硬排除。
8. 如果开始第十一阶段，先向用户确认小组人数/过期、正式登录和生产部署；不得用隐式默认做出账户和权限决策。
9. 第十一阶段必须保留本阶段所有定义和意义；如迁移数据库或任务系统，先兼容现有摘要、回顾、分享撤回状态和任务审计，不得删除用户内容。
10. 完成后新建 `第十一阶段技术开发文档.md`，更新 README 和总文档，不覆盖本文档。
