# 第三阶段技术开发文档

> AI 读取定位：这是当前最新、当前权威的版本迭代交接文档。后续迭代先读根目录 `AGENTS.md` 和本文件；只有追溯邀请制、基础 Agent 架构或管理员模型配置的设计来源时，才读取第一、第二阶段文档。

## 0. AI 快速接手卡

```text
DOCUMENT_ROLE=current_authoritative_handoff
CURRENT_STAGE=3
STATUS=completed_code_and_local_verification_waiting_for_external_service_credentials
CODE_ROOT=products/yingban
PRODUCT_START=products/yingban/app.py
HTTP_ROUTING=products/yingban/server.py
AGENT_RUNTIME=products/yingban/agent.py
PERSISTENCE=products/yingban/storage.py
OFFLINE_MOVIE_RETRIEVAL=products/yingban/catalog.py
LIVE_MOVIE_AND_WEB=products/yingban/integrations.py
VOICE_RUNTIME=products/yingban/voice.py
USER_UI=products/yingban/web/index.html + app.js + styles.css
ADMIN_UI=products/yingban/web/admin.html + admin.js + admin.css
TESTS=products/yingban/tests/test_product.py
START_COMMAND=cd products/yingban && python3 app.py serve
TEST_COMMAND=cd products/yingban && python3 -m unittest discover -s tests -v
EXPECTED_TEST_BASELINE=16 tests passing
CURRENT_DATABASE_MODE=model
CURRENT_TMDB_MODE=disabled_missing_credential
CURRENT_WEB_SEARCH_MODE=disabled_missing_credential
CURRENT_VOICE_MODE=disabled_missing_credential
CURRENT_MOVIE_COUNT=24
NEXT_DOCUMENT=products/yingban/第四阶段技术开发文档.md
```

### 0.1 当前实现一句话

影伴现在是一个邀请码制 AI 电影伙伴：原有双 Agent、长期观影记录和推荐硬去重全部保留；本地片库未命中时可以通过 TMDB 自动补全新电影与真实海报；真实模型可以按全网、豆瓣或知乎范围搜索公开资料并受限读取公开的豆瓣/知乎页面；用户可以按住说话，把转写文本发送给 Agent，并在每条 AI 文字回复下主动播放语音回复。

### 0.2 当前完成状态

- 代码、数据库自动迁移、管理配置入口、用户交互、自动测试和真实浏览器界面检查已经完成。
- 当前真实数据库中的大模型配置处于 `model` 模式，表示管理员此前已经保存了模型密钥；阶段文档不记录密钥、模型完整调用内容或其他敏感值。
- 当前真实数据库尚未保存 TMDB 凭据、Brave Web Search 密钥或语音 API 密钥。因此实时新电影、真实海报、公开网页搜索和语音按钮目前处于安全关闭状态。
- 未配置外部服务时，邀请登录、离线电影搜索、讨论自动记忆、离线推荐、看过硬去重、历史片单、真实大模型聊天和管理员功能继续工作。
- 本阶段没有使用真实外部服务密钥，因此完成了协议、请求、持久化、密钥掩码、模拟响应、浏览器交互与降级验证；真实 TMDB、Brave Search、豆瓣/知乎公开页面阅读、STT 和 TTS 的最终质量验收仍需管理员提供对应凭据。

### 0.3 不可破坏的产品约束

- 一个邀请码只对应一个长期账户；同一码重新登录必须回到原账户。
- 邀请码完整正文不得写入数据库、日志或阶段文档。
- 用户在讨论中确认看过电影后自动写入 `watched`，不再次征求同意。
- 推荐结果必须在数据检索层排除 `watched`，不能只依赖模型提示词去重；TMDB 动态电影也必须经过同一硬排除。
- “聊电影”和“找电影”使用独立完整提示词，但共享身份、安全、隐私、事实和长期记忆边界。
- Agent 不得冒充真人或虚构真人经历；用户明确询问身份时必须透明说明。
- 现实烦恼、心理状态和敏感经历不得自动进入长期记忆。
- 高风险自伤或伤人文本必须在普通电影 Agent 调用前由产品代码短路。
- 管理配置读取接口不得返回任何完整 API 密钥。
- 录音、转写前的原始音频和合成语音不得写入长期数据库。
- 浏览器不得自动播放有声回复；用户必须主动点击播放。
- 豆瓣与知乎能力只允许处理公开页面；不得自动登录、提交 Cookie、绕过验证码、绕过付费墙、绕过访问控制或抓取私密内容。
- 外部电影必须先转换成统一电影记录并写入 `movies`，才能进入观影状态和推荐曝光链路；不能把未落库的搜索摘要直接当成推荐电影。
- 旧账户、邀请码绑定、观影历史、推荐曝光和管理员配置在数据库升级时不得丢失。

### 0.4 下一阶段建议的第一步检查

1. 运行 16 项测试，确认现有基线没有回归。
2. 访问 `GET /api/health`，确认服务和模型实时状态。
3. 登录 `/admin`，读取公开配置状态，不读取或输出完整密钥。
4. 向用户确认要接入的 TMDB、Brave Web Search 和语音供应商凭据；如果语音不是 OpenAI 兼容协议，需要先明确供应商契约，不能假设字段相同。
5. 分别执行“测试 TMDB”“测试 Web Search”“测试语音合成”，再做一次真实新电影搜索、一次豆瓣/知乎检索、一次用户录音和一次 AI 语音播放。

## 1. 文档信息与阶段编号说明

- 产品名称：影伴
- 阶段名称：第三阶段——实时电影知识、公开 Web 检索、真实海报与双向语音
- 开发状态：代码完成，等待外部服务凭据完成真实服务验收
- 阶段完成日期：2026-08-21
- 继承阶段：第二阶段——管理员大模型配置与双 Agent 提示词管理
- 下一阶段文档：`第四阶段技术开发文档.md`

用户把本轮称为“第二阶段开发”，但 `products/yingban/第二阶段技术开发文档.md` 已经存在且记录了一个独立完成的管理员模型配置阶段。根目录 `AGENTS.md` 明确要求新阶段号等于已有最大阶段号加一，并禁止覆盖旧阶段文档，因此本轮在项目权威历史中编号为第三阶段。该编号调整不改变用户提出的功能范围。

## 2. 需求背景

本阶段收到五项明确需求：

1. “聊电影”只能搜索有限种子片库，新电影需要人工更新，维护成本过高；希望增加 Web Search 类工具，并参考开源项目 Agent-Reach 的互联网能力。
2. 希望增加豆瓣和知乎公开资料获取能力。
3. 用户需要类似参考图功能的语音交互：用户可以说话，AI 回复同时保留文字和语音；只参考功能，现有前端视觉风格保持不变。
4. 语音如果需要 API，必须在后端管理设置中提供输入入口。
5. 推荐电影不能只有文字，应显示电影海报。

用户同时询问还有哪些可优化方向，本文件第 20 节给出优先级建议。

## 3. Agent-Reach 研究结论与采用方式

Agent-Reach 当前定位是 Python CLI、安装器、配置器和健康检查工具。它帮助 Agent 安装并诊断多个上游平台工具；实际读取和搜索由各个上游 CLI、MCP 或阅读器完成。它不是一个可以在影伴 HTTP 服务内直接调用的统一 `search()` SDK，也不原生提供豆瓣和知乎的结构化电影目录。

本阶段没有把 Agent-Reach 安装到产品运行环境，原因和意义如下：

- 影伴当前运行时只依赖 Python 标准库；直接引入 Agent-Reach 会同时引入多个平台工具、运行进程、Cookie 和诊断配置，超出电影产品需要。
- Agent-Reach 的 CLI 适合通用本地 Agent，不等同于面向终端用户的多租户 Web 服务。
- 产品需要稳定的电影统一 ID、海报、历史状态和推荐硬去重；通用网页搜索结果不能直接替代结构化电影数据源。
- 豆瓣和知乎存在登录、验证码、访问策略和内容授权边界，不能把“可抓取”理解为可以绕过站点限制。

影伴采用 Agent-Reach 的“可替换上游适配器和健康检查”思想，原生实现：

1. TMDB 结构化电影适配器，负责电影搜索、发现、统一 ID 和海报。
2. Brave Web Search 适配器，负责一般 Web、豆瓣站内范围和知乎站内范围检索。
3. Jina Reader 风格的公开页面阅读适配器，只允许豆瓣和知乎公开 URL。
4. OpenAI 兼容 STT/TTS 适配器，负责语音识别和语音合成。
5. 每个适配器都有独立管理员配置、密钥掩码、连接测试和未配置降级。

## 4. 阶段目标与完成定义

本阶段目标闭环是：

```text
管理员登录
→ 配置 TMDB、Web Search 和语音 API
→ 分别测试连接并保存
→ 用户搜索或讨论种子库外新电影
→ 后端从 TMDB 获取结构化电影并写入 movies
→ 推荐检索继续硬排除 watched
→ 电影卡片和历史片单显示 poster_url
→ 真实模型需要近期观点时调用 search_web
→ 豆瓣/知乎检索使用显式 source 站点限制
→ 用户切换语音输入并按住说话
→ 后端当次转写并进入原聊天链路
→ AI 返回文字，前端异步准备可主动播放的语音
```

完成定义包括：

- 新电影不再要求管理员编辑 `data/movies.json` 才能被搜索。
- 外部电影拥有稳定 `tmdb-{id}`，可以进入看过、想看、不感兴趣和推荐曝光表。
- TMDB 海报 URL 可以进入推荐卡和历史片单；图片失败时保留原有文字海报占位，不出现空白卡片。
- Web Search 工具明确区分 `web`、`douban` 和 `zhihu`。
- 页面阅读只允许豆瓣和知乎域名。
- 用户录音最大 10 MB、最长 60 秒；转写文字继续受聊天 6000 字符限制。
- AI 语音不自动播放，文字永远是可用的主回复。
- 外部服务失败不破坏离线核心闭环。
- 所有新密钥读取接口只返回末四位掩码。
- 原有 13 项测试保留，并增加 3 项集成测试，最终 16 项通过。

## 5. 当前总体架构

当前运行架构仍是 Python 标准库单进程 HTTP 服务和 SQLite：

1. 交互层：`web/index.html`、`web/app.js`、`web/styles.css` 提供文字、录音、海报和音频播放。
2. 管理层：`web/admin.*` 提供模型、联网电影、Web Search、语音、双 Agent 提示词和邀请码管理。
3. HTTP 产品层：`server.py` 负责会话、鉴权、输入限制、安全短路、外部补全编排和 API 响应。
4. Agent 编排层：`agent.py` 负责两套系统提示词、最多八轮工具调用和七个领域工具。
5. 离线检索层：`catalog.py` 负责种子电影搜索、文本识别、情绪/类型匹配和推荐硬去重。
6. 实时知识层：`integrations.py` 负责 TMDB、Brave Web Search 和受限公开页面阅读。
7. 语音层：`voice.py` 负责 OpenAI 兼容语音识别和语音合成。
8. 数据层：`storage.py` 使用 SQLite 保存账户、电影、观影状态、推荐曝光和管理员配置。

## 6. 实时电影目录与搜索

### 6.1 本地优先、外部补全

`GET /api/movies?query=...` 和 Agent 工具 `search_movie` 先执行 `MovieCatalog.search()`。只有本地无结果且 TMDB 已配置时，才调用 `InternetRuntime.search_movies()`。

这样设计的意义是：

- 已知电影查询没有外部费用和网络延迟。
- 外部服务不可用时，本地片库仍然工作。
- 相同外部电影首次查询后进入 SQLite，后续可以本地命中。
- 外部电影和种子电影共享统一观影状态和推荐去重逻辑。

### 6.2 讨论模式的新电影识别

`server.movie_query_from_message()` 按以下优先级提取搜索词：

1. `《片名》` 书名号内容。
2. “刚看完”“看完”“看了”之后到标点或情绪连接词之前的文本。
3. 无法提取时使用原消息前 120 个字符。

讨论模式先用本地 `detect_in_text()`。没有命中时调用 TMDB 搜索，把结果写入 `movies`，再执行本地识别与同名候选确认。只有确认单一电影后，产品层才自动写入 `watched`。

### 6.3 推荐模式的动态候选

推荐前调用 `InternetRuntime.discover_movies(request)`。它把自然语言中的电影类型映射到 TMDB genre ID，并把“轻松、治愈、新片、烧脑”等需求转换成影伴的情绪标签。

“不要爱情”“不想看恐怖”“避开战争”等否定表达不会被转换成 TMDB 正向发现条件；外部结果写入目录后仍会被 `MovieCatalog._excluded_tags()` 再次过滤。

动态候选进入 `MovieCatalog.recommend()` 后继续执行：

- 排除 `Store.watched_ids(account_id)`。
- 默认排除近期推荐曝光。
- 按类型、主题、情绪和内容提醒匹配。
- 写入 `recommendation_impressions`。

### 6.4 TMDB 电影记录转换

外部电影字段映射：

- `id`：`tmdb-{TMDB movie id}`。
- `title_zh`：TMDB `language=zh-CN` 返回的 `title`。
- `title_original`：`original_title`。
- `year`：`release_date` 前四位；缺失时为 `0`。
- `regions`：`origin_country`。
- `genres`：TMDB genre ID 转换为影伴中文类型。
- `summary`：`overview`，最多 4000 字符。
- `poster_url`：`tmdb_image_base_url + poster_path`。
- `source_url`：`https://www.themoviedb.org/movie/{id}`。
- `external_ids`：`{"tmdb": "..."}`。
- `source`：`tmdb`。

当前搜索/发现响应没有导演和完整别名，因此 `directors` 和 `aliases` 可能为空。下一阶段可按需调用 TMDB detail/credits 端点补充，但不能为了列表页一次性放大所有请求。

## 7. Web Search、豆瓣与知乎

### 7.1 新增 Agent 工具

Agent 工具从五个增加到七个：

1. `search_movie`
2. `get_movie_detail`
3. `list_watched_movies`
4. `mark_movie_watched`
5. `recommend_movies`
6. `search_web`
7. `read_public_page`

### 7.2 `search_web`

参数定义：

- `query`：搜索词，后端最多使用 300 字符。
- `source`：只允许 `web`、`douban` 或 `zhihu`。
- `limit`：1 到 10。

`source=douban` 时，后端把查询改为 `site:movie.douban.com {query}`；`source=zhihu` 时改为 `site:zhihu.com {query}`。返回字段只包含：

- `title`
- `url`
- `snippet`
- `source`

搜索摘要是公开资料线索，不自动变成确定电影事实。系统提示词要求使用豆瓣/知乎资料时给出来源链接。

### 7.3 `read_public_page`

只允许以下主机或子域名：

- `douban.com`
- `*.douban.com`
- `zhihu.com`
- `*.zhihu.com`

只允许 `http` 和 `https`，禁止 URL 用户名，默认最多返回 12000 字符，硬上限 30000 字符。阅读器不接收用户 Cookie，不实现登录，不处理验证码，不绕过站点访问控制。

### 7.4 供应商与降级

当前 Web Search 协议按 Brave Web Search 实现，请求端点为：

```text
{web_search_base_url}/web/search
```

使用 `X-Subscription-Token` 认证。没有密钥时 `search_web` 返回可理解的配置错误；普通电影聊天仍可使用本地或 TMDB 数据。

公开页面阅读器默认根地址为 `https://r.jina.ai`，实际目标构造为：

```text
{web_reader_base_url}/http://{allowed_host}{path_and_query}
```

阅读器可由管理员关闭。外部页面阅读失败不会写入长期数据库。

## 8. 海报实现

### 8.1 数据来源

真实海报来自 TMDB 响应中的 `poster_path`，默认图片根地址为：

```text
https://image.tmdb.org/t/p/w500
```

### 8.2 页面行为

`web/app.js` 的 `posterNode(movie)` 被推荐卡片和历史片单共同使用：

- 有 `poster_url` 时创建语义正确的 `<img>`，`alt` 为“《片名》电影海报”。
- 图片使用 `loading=lazy` 和 `referrerPolicy=no-referrer`。
- 图片加载失败时移除 `<img>`，恢复原有年份与片名文字海报。
- 海报上覆盖渐变层，保证年份和片名可读。

### 8.3 当前限制

现有 24 部离线种子电影没有手工写入海报 URL。配置 TMDB 后，首次外部搜索/发现的电影立即带真实海报。已有种子电影的海报自动回填尚未实现；这是下一阶段高优先级优化，建议使用 TMDB 标题+年份批量匹配并保留人工确认机制，避免同名错配。

## 9. 语音交互

### 9.1 用户流程

语音服务配置后，`GET /api/me` 返回 `voice_available=true`。前端同时检查浏览器是否支持 `MediaRecorder` 和 `getUserMedia`，满足条件才显示语音切换按钮。

用户流程：

1. 点击麦克风按钮在文字输入和语音输入之间切换。
2. 鼠标、触摸或手写笔按下“按住说话”开始录音，松开结束并发送。
3. 键盘用户按住空格或 Enter 录音，松开结束；`Esc` 取消。
4. 录音状态每 0.2 秒更新，最多 60 秒。
5. 小于 0.5 秒或没有音频块的录音不会发送。
6. 浏览器把音频以 Base64 JSON 发送到 `/api/voice/transcribe`。
7. 后端调用 STT，返回转写文字。
8. 转写文字作为普通用户消息进入 `/api/chat`，并在用户气泡显示“语音已转写”和时长。
9. AI 文字回复立即显示；前端异步调用 `/api/voice/synthesize`。
10. 合成成功后，同一气泡显示原生可访问音频播放器；不会自动播放。

### 9.2 STT 契约

端点：

```text
POST {voice_base_url}/v1/audio/transcriptions
```

请求为 `multipart/form-data`，字段：

- `file`
- `model`
- `language=zh`

支持的浏览器录音 MIME 映射包括 WebM、Ogg、MP4/M4A、MP3 和 WAV。影伴请求体最大 12 MB，解码后的单条音频最大 10 MB。STT 返回 JSON，必须包含非空 `text`。

### 9.3 TTS 契约

端点：

```text
POST {voice_base_url}/v1/audio/speech
```

JSON 字段：

- `model`
- `voice`
- `input`
- `response_format`

允许音频格式：`mp3`、`wav`、`opus`、`aac`。单次合成文字最多 3000 字符。后端把音频以 Base64 和 `content_type` 返回浏览器。

### 9.4 语音隐私定义

- 原始录音只存在于浏览器内存、当前 HTTP 请求体和语音供应商请求中。
- 后端不把原始录音、Base64、音频文件或合成音频写入 SQLite 或磁盘。
- 长期数据仍只有观影状态，不自动保存现实烦恼或原始聊天正文。
- 外部语音供应商能收到当次音频或 AI 回复文本。公开部署前必须在隐私政策中明确供应商、用途、保留政策和跨境边界。
- 浏览器不会自动播放声音，避免突然发声并符合无障碍媒体约束。

## 10. Agent 行为变化

### 10.1 系统提示词职责变化

“聊电影 Agent”新增：

- 本地资料不足时先调用 `search_movie`。
- 需要近期公开观点时可以调用 `search_web`。
- 豆瓣或知乎必须显式选择对应 `source` 并给出来源链接。
- `read_public_page` 只能读取公开豆瓣/知乎页面。
- 不得把网页观点冒充成确定事实。

“找电影 Agent”新增：

- 本地目录覆盖不足时可用 `search_movie`、`search_web` 查证。
- 最终推荐仍必须落在产品检索层候选中，不能仅凭搜索摘要补造影片。
- 使用豆瓣或知乎公开资料时必须给出来源链接。

### 10.2 运行时上下文注入

原有注入继续保留：当前模式、剧透状态、确认电影、是否已自动写入看过、去重候选片单和避免重复推荐工具调用。外部电影进入上下文前已转换为 `BoundMovieTools._public_movie()`，新增可公开字段：

- `poster_url`
- `source_url`
- `external_ids`
- `source`

### 10.3 工具调用限制

- Agent Loop 仍最多八轮。
- 产品层已经生成推荐候选时，系统上下文要求不要再次调用 `recommend_movies`。
- `search_web` 只返回公开摘要和 URL。
- `read_public_page` 在后端硬限制域名，不能由提示词解除。
- 工具异常转为结构化错误，不把密钥放入工具结果。

### 10.4 长期记忆与安全降级

- 外部电影可以保存为 `watched`、`watchlist` 或 `disliked`。
- Web 搜索结果和页面正文不写入长期记忆。
- 录音和语音不写入长期记忆。
- 高风险内容仍在调用外部电影搜索、普通 Agent 或语音合成之前由产品层短路；高风险响应可以在用户启用语音后合成，但产品不会继续推荐电影。
- 任何外部服务失败时保留文字错误或离线结果，不编造事实。

## 11. 数据结构变化

### 11.1 `movies` 新增字段

现有表新增：

```sql
poster_url TEXT NOT NULL DEFAULT ''
source_url TEXT NOT NULL DEFAULT ''
external_ids_json TEXT NOT NULL DEFAULT '{}'
```

字段意义：

- `poster_url`：可直接用于页面展示的电影海报 URL；空字符串表示没有真实海报。
- `source_url`：结构化电影来源详情页，用于溯源；当前 TMDB 电影指向 TMDB 详情页。
- `external_ids_json`：外部电影 ID 映射 JSON；当前格式为 `{"tmdb": "123"}`，后续可增加其他合规数据源 ID。

### 11.2 迁移方式

`Store.initialize()` 在创建基础表后执行 `PRAGMA table_info(movies)`。缺少任一字段时执行单列 `ALTER TABLE movies ADD COLUMN`。迁移不重建表、不删除数据、不修改主键和外键。

### 11.3 Upsert 兼容规则

`Store.upsert_movies()` 已扩展三个新字段。冲突更新时：

- 外部记录提供非空 `poster_url` 时更新；新值为空时保留数据库原值。
- 外部记录提供非空 `source_url` 时更新；新值为空时保留数据库原值。
- 外部记录提供非空 `external_ids` 时更新；新值为 `{}` 时保留数据库原值。

这个规则避免应用每次加载不含海报的种子 JSON 时把已经获取的外部海报覆盖为空。

### 11.4 新配置键

`app_settings` 新增的逻辑键：

联网电影与 Web：

- `tmdb_api_key`
- `tmdb_base_url`
- `tmdb_image_base_url`
- `web_search_api_key`
- `web_search_base_url`
- `web_reader_enabled`
- `web_reader_base_url`

语音：

- `voice_api_key`
- `voice_base_url`
- `voice_stt_model`
- `voice_tts_model`
- `voice_name`
- `voice_audio_format`
- `voice_timeout_seconds`

### 11.5 旧数据兼容性与回滚风险

- 原有 24 部种子电影保留。
- 原账户、邀请码、会话、观影状态、推荐曝光、安全事件和模型配置保留。
- 本阶段已对真实数据库完成字段迁移，迁移后电影数量仍为 24。
- 旧版代码使用 `SELECT * FROM movies` 时通常会忽略新增列，但旧版 `INSERT` 不写新列时由默认值兼容。
- 如果回滚到旧版后再次加载种子，新增海报字段不会被旧代码更新，但字段仍保留。
- SQLite `ALTER TABLE ADD COLUMN` 没有自动向下迁移；若必须完全回滚 schema，需要备份数据库后重建表，风险高，不应在没有用户确认时执行。

## 12. 接口变化

### 12.1 现有接口变化

- `GET /api/me`：新增 `voice_available`。
- `GET /api/movies`：本地无结果且 TMDB 已配置时执行外部补全。
- `POST /api/chat`：讨论模式可补全新电影；推荐模式可补充 TMDB 动态候选。
- `GET /api/admin/agent-config`：响应新增 `internet` 和 `voice` 公开配置，仍不返回完整密钥。

### 12.2 用户语音接口

`POST /api/voice/transcribe`

请求：

```json
{"audio_base64": "...", "mime_type": "audio/webm;codecs=opus"}
```

响应：

```json
{"text": "转写后的用户文字"}
```

`POST /api/voice/synthesize`

请求：

```json
{"text": "AI 文字回复"}
```

响应：

```json
{"audio_base64": "...", "content_type": "audio/mpeg"}
```

两个接口都要求有效用户会话。

### 12.3 管理员联网接口

- `POST /api/admin/internet-config`：保存 TMDB、Web Search 和阅读器配置。
- `POST /api/admin/internet-config/test`：`target` 允许 `tmdb` 或 `web_search`，使用页面当前值做不持久化连接测试。

密钥提交语义：输入框留空表示保留；`clear_tmdb_api_key=true` 或 `clear_web_search_api_key=true` 才清除。

### 12.4 管理员语音接口

- `POST /api/admin/voice-config`：保存 OpenAI 兼容语音配置。
- `POST /api/admin/voice-config/test`：合成“连接成功”短音频，不持久化页面临时配置。

密钥输入框留空表示保留；`clear_api_key=true` 才清除。

## 13. 管理页面变化

管理配置中心导航从两个入口增加为四个：

1. 模型与 Agent
2. 联网与影片
3. 语音
4. 邀请码

“联网检索与电影海报”区域包括：

- TMDB 接口根地址。
- TMDB 海报图片根地址。
- TMDB API Read Access Token 或 v3 API Key。
- 清除 TMDB 凭据。
- Brave Web Search 接口根地址。
- Brave Web Search API 密钥。
- 清除 Web Search 密钥。
- 公开页面阅读器地址。
- 是否允许读取公开豆瓣/知乎页面。
- 测试 TMDB。
- 测试 Web Search。
- 保存联网配置。

“语音交互”区域包括：

- 语音接口根地址。
- API 密钥。
- 清除语音密钥。
- STT 模型。
- TTS 模型。
- 音色。
- 音频格式。
- 请求超时。
- 测试语音合成。
- 保存语音配置。

视觉继续使用米色纸张、酒红强调、衬线标题、圆角配置卡和原有栅格，没有采用参考图的动漫背景、悬浮黑色气泡或底部导航设计。

## 14. 用户页面变化

- 聊天输入区新增带可访问名称的麦克风切换按钮。
- 文字和语音输入共用原有 composer，视觉仍是米色/酒红电影杂志风格。
- 语音按钮使用 `aria-pressed` 表示模式，录音状态使用 `role=status` 和 `aria-live=polite`。
- 按住说话支持指针和键盘，`Esc` 支持取消。
- 用户语音消息显示转写文字、语音来源和录音时长。
- AI 文字回复下方显示“语音回复”和原生 `<audio controls>`。
- 语音不会自动播放。
- 推荐卡片和历史片单共享真实海报组件。
- 海报加载失败时回退到原有文字海报。

## 15. 配置变化

`.env.example` 新增启动默认变量：

```env
TMDB_API_KEY=
TMDB_BASE_URL=https://api.themoviedb.org/3
TMDB_IMAGE_BASE_URL=https://image.tmdb.org/t/p/w500
BRAVE_SEARCH_API_KEY=
BRAVE_SEARCH_BASE_URL=https://api.search.brave.com/res/v1
WEB_READER_ENABLED=true
WEB_READER_BASE_URL=https://r.jina.ai
VOICE_API_KEY=
VOICE_BASE_URL=https://api.openai.com
VOICE_STT_MODEL=whisper-1
VOICE_TTS_MODEL=gpt-4o-mini-tts
VOICE_NAME=alloy
VOICE_AUDIO_FORMAT=mp3
VOICE_TIMEOUT_SECONDS=60
```

数据库配置继续优先于 `.env`。管理员保存后下一轮请求立即生效，无需重启。

## 16. 安全与隐私边界

### 16.1 密钥

- 完整 TMDB、Web Search、语音和模型密钥只保存在本机 SQLite `app_settings`。
- 数据库文件权限尝试设为 `0600`。
- 管理接口只返回 `has_api_key`、`api_key_hint` 和 `enabled`。
- 测试和保存错误日志只记录错误类型，不记录完整密钥。
- 当前仍是本机明文 SQLite 密钥存储；公开部署必须迁移到专用 Secrets Manager。

### 16.2 外部请求与 SSRF

- TMDB、Web Search、阅读器和语音根地址由管理员配置，保留连接代理或自建兼容服务的能力。
- 地址校验要求 http/https、有效网络位置且不能包含 URL 用户名。
- 这不能完全防止管理员误配到内网地址。公开部署应增加允许域名列表、DNS/IP 检查和出站网络策略。
- `read_public_page` 的目标 URL 另有豆瓣/知乎域名硬限制，不能读取任意内网 URL。

### 16.3 站点访问边界

- 豆瓣和知乎只使用公开搜索结果和公开页面阅读器。
- 不保存或提交用户浏览器 Cookie。
- 不实现验证码处理、账号自动登录、私密内容读取、付费内容绕过或频繁批量抓取。
- 上线前仍需由产品方确认数据源条款、版权展示、缓存策略和必要署名。

### 16.4 音频与聊天

- 原始音频和合成音频不落盘。
- 当前聊天正文仍不写入长期数据库。
- 音频与文本会发送到管理员配置的语音供应商；正式上线前必须向用户披露。
- 高风险内容安全短路、消息 6000 字符限制和用户认证继续存在。

## 17. 修改文件清单

### 新增

- `integrations.py`：TMDB、Brave Web Search、受限豆瓣/知乎页面阅读、配置校验、密钥掩码和连接测试。
- `voice.py`：OpenAI 兼容 STT/TTS、multipart 构造、音频大小限制、配置校验、密钥掩码和连接测试。
- `第三阶段技术开发文档.md`：当前权威交接文档。

### 修改

- `settings.py`：新增联网电影、Web Search、阅读器和语音 `.env` 默认配置。
- `storage.py`：迁移 `movies` 三个字段，扩展电影 upsert、历史查询和 JSON 解码。
- `catalog.py`：新增“新片”“最近上映”“最近的电影”意图标签。
- `agent.py`：注入 `InternetRuntime`，工具从五个增加到七个，公开电影新增来源与海报字段，两套提示词增加 Web 事实边界。
- `server.py`：新增外部电影补全、语音接口、管理员联网/语音接口、公开配置和请求体上限分流。
- `app.py`：构建 `InternetRuntime` 与 `VoiceRuntime`，启动日志显示联网电影和语音状态。
- `web/index.html`：新增文字/语音切换、按住说话和录音状态结构。
- `web/app.js`：新增录音状态机、转写发送、语音回复、真实海报与失败回退。
- `web/styles.css`：新增语音输入、音频回复和海报图片样式，保留原设计系统。
- `web/admin.html`：新增联网与影片、语音配置表单。
- `web/admin.js`：新增配置渲染、保存、测试和密钥掩码交互；同时修正新邀请码标题选择器从不存在的 `h2` 为真实 `h3`。
- `tests/test_product.py`：新增集成配置、海报持久化和管理员配置 HTTP 测试；HTTP 测试上下文注入联网与语音运行时。
- `.env.example`：新增外部服务变量。
- `README.md`：新增第三阶段能力、配置、隐私、工具和目录说明。

## 18. 测试与验证结果

### 18.1 语法检查

以下 Python 文件通过 `python3 -m py_compile`：

- `app.py`
- `agent.py`
- `catalog.py`
- `integrations.py`
- `server.py`
- `settings.py`
- `storage.py`
- `voice.py`

以下 JavaScript 文件通过 `node --check`：

- `web/app.js`
- `web/admin.js`

### 18.2 自动测试

完整命令：

```sh
python3 -m unittest discover -s tests -v
```

最终基线：

```text
Ran 16 tests
OK
```

16 项覆盖：

1. 两套提示词独立保存。
2. 模型配置持久化和密钥掩码。
3. 管理员生成邀请码。
4. 管理员管理模型和独立提示词。
5. 管理员通过 HTTP 管理联网与语音配置且响应不泄露密钥。
6. 讨论自动写入看过状态。
7. 身份询问透明回应。
8. 推荐不返回看过电影。
9. 换片时保留此前意图。
10. 联网和语音配置直接持久化、启用状态和密钥掩码。
11. 模拟 TMDB 新电影写入稳定 ID、海报 URL 和外部 ID。
12. 不同账户观影历史隔离。
13. 推荐逻辑硬性排除看过电影。
14. 停用邀请码后无法登录。
15. 换发邀请码保留账户并使旧码失效。
16. 同一邀请码长期进入同一账户。

### 18.3 真实数据库迁移

真实数据库迁移后验证：

- 模型模式：`model`。
- TMDB：未配置。
- Web Search：未配置。
- 语音：未配置。
- 电影数量：24。
- `movies` 已存在 `poster_url`、`source_url` 和 `external_ids_json`。
- 数据库权限：`0600`。

### 18.4 浏览器验证

使用隔离临时数据库和本地预览服务完成：

- 邀请码登录成功。
- 用户首页和找电影聊天页正常。
- 管理页四段导航、联网表单、语音表单和既有表单全部可见。
- 保存临时语音配置后，用户页无需重启即可出现语音切换按钮。
- 切换后显示“按住说话”和可访问录音状态。
- 用户端和管理端控制台没有 error 或 warn。
- 桌面视觉继续使用现有米色、酒红、衬线标题和电影杂志布局。

浏览器麦克风真实录音需要操作系统权限，同时真实 STT/TTS 需要供应商密钥；本阶段没有伪造这两项成功结果。

## 19. 启动与使用方法

### 19.1 启动

```sh
cd products/yingban
python3 app.py serve
```

地址：

- 用户端：`http://127.0.0.1:8765`
- 管理端：`http://127.0.0.1:8765/admin`

### 19.2 配置实时电影与海报

1. 登录管理端。
2. 进入“联网与影片”。
3. 填写 TMDB API Read Access Token 或 v3 API Key。
4. 保持默认 TMDB API 和图片根地址，除非使用受信代理。
5. 点击“测试 TMDB”。
6. 测试成功后保存。
7. 在用户端搜索一部种子库外电影，确认片名、年份和海报。

### 19.3 配置豆瓣与知乎公开检索

1. 填写 Brave Web Search API 密钥。
2. 点击“测试 Web Search”。
3. 确认“允许读取公开的豆瓣与知乎页面正文”是否符合部署政策。
4. 保存。
5. 使用真实模型询问一部电影的近期豆瓣或知乎讨论，确认回答提供来源链接。

### 19.4 配置语音

1. 填写支持 `/v1/audio/transcriptions` 和 `/v1/audio/speech` 的 OpenAI 兼容接口根地址与密钥。
2. 填写供应商实际支持的 STT 模型、TTS 模型、音色和格式。
3. 点击“测试语音合成”。
4. 保存。
5. 用户刷新或重新进入页面，点击麦克风图标。
6. 按住说话并允许浏览器麦克风权限。
7. 验证用户气泡有转写文本和时长，AI 气泡有文字和可播放音频。

## 20. 尚未完成事项、已知风险与优化建议

### 20.1 必须由用户提供或决定

- TMDB API Read Access Token 或 v3 API Key。
- Brave Web Search API 密钥；如果不使用 Brave，需要提供目标搜索服务协议，本阶段不能自动假设兼容。
- 语音供应商、接口根地址、STT 模型、TTS 模型、音色、格式和 API 密钥。
- 是否允许正式环境启用公开豆瓣/知乎页面阅读。
- 是否接受语音数据发送给所选外部供应商，并如何在用户隐私政策中披露。

### 20.2 高优先级优化建议

1. 为现有 24 部种子电影批量匹配 TMDB ID 和海报，增加同名年份确认与人工审核，避免种子推荐仍显示文字占位。
2. 给外部电影增加详情和 credits 延迟补全，补充导演、别名、片长和更准确地区；列表查询不要一次请求所有详情。
3. 增加电影数据来源署名、海报版权展示规则、缓存时效和删除/纠错流程。
4. 给 Web Search 回答增加代码层引用校验：凡使用外部观点，输出必须带至少一个允许域名来源链接。
5. 为外部服务增加超时重试、指数退避、熔断、按账户配额和缓存，避免每次推荐都访问供应商。
6. 语音合成可以增加“关闭本轮语音”“仅 Wi-Fi 生成”“重新生成”和播放速度设置，控制费用和移动流量。
7. 增加真实移动设备录音兼容测试，重点覆盖 iOS Safari 的 MIME、权限恢复和锁屏中断。
8. 增加转写确认模式：短语音自动发送，长语音先让用户编辑转写，降低识别错误造成的误会。

### 20.3 生产级安全风险

- 管理员可配置外部根地址，存在 SSRF 和误发密钥风险；需要域名允许列表、出站防火墙和私网 IP 阻断。
- 密钥仍以本机 SQLite 明文值保存；需要 Secrets Manager、轮换和管理员审计。
- 没有 CSRF Token；管理保存和邀请码操作应增加 CSRF 防护。
- 标准库 HTTP 服务不适合直接公开部署；需要反向代理、TLS、请求限流和生产应用服务器。
- 音频 Base64 增加约 33% 请求体体积；高并发应改为流式或 multipart 用户接口，增加并发和总时长限制。
- 当前 TTS 每条 AI 回复都会由前端请求一次，可能产生显著费用；需要服务端配额和成本指标。
- 豆瓣/知乎公开页面可用性会随站点规则变化，不能保证永久稳定；必须把失败当作正常降级，而不是不断绕过限制。

### 20.4 推荐质量风险

- TMDB discover 只能把有限类型词转换成 genre ID，复杂心情仍依赖影伴本地标签和模型解释。
- TMDB 搜索响应缺少导演和完整别名，可能出现同名版本确认不足。
- 外部电影的内容提醒为空；在内容安全敏感场景中不应把“空”解释为“没有风险”。
- 种子电影和动态电影的 `popularity_rank` 标尺不同，未来需要统一排序校准。
- 近期推荐曝光只按内部电影 ID 去重；如果未来接入第二个电影源，需要基于 `external_ids` 做跨源合并，避免同一电影重复。

## 21. 下一阶段建议

第四阶段建议以“真实外部服务验收与数据质量”为唯一主线：

1. 用户提供 TMDB、Web Search 和语音供应商选择。
2. 完成三个连接测试。
3. 为 24 部种子电影回填并人工核验 TMDB ID 与海报。
4. 验证至少三部 2026 新电影的搜索、讨论自动记忆和推荐硬去重。
5. 验证豆瓣、知乎各一次搜索和公开页面读取，检查引用链接。
6. 在真实手机完成录音、转写、文字回复、语音播放和取消操作。
7. 记录外部服务延迟、错误率、请求次数和语音成本。
8. 增加缓存、重试、配额、来源署名和隐私披露。

不要在没有真实凭据和条款确认的情况下把“模拟协议通过”写成“生产服务已接入”。

## 22. AI 接手入口

后续 AI 按以下顺序接手：

1. 读取根目录 `AGENTS.md`。
2. 确认 `第三阶段技术开发文档.md` 是否仍是最大阶段号；如果已经存在更晚文档，改读最大阶段号。
3. 运行 16 项测试，不得无理由删除已有测试。
4. 只读检查真实数据库 schema、电影数量、模型模式、TMDB/Web/语音启用状态；不得输出完整密钥。
5. 实时电影问题从 `integrations.py`、`storage.py`、`catalog.py` 和 `server.py` 开始。
6. Agent Web 工具问题从 `agent.py` 的 `BoundMovieTools` 和两套系统提示词开始。
7. 语音问题从 `voice.py`、`server.py` 的两个语音接口和 `web/app.js` 的录音状态机开始。
8. 海报问题从 `storage.py` 三个新字段和 `web/app.js::posterNode()` 开始。
9. 管理配置问题从 `web/admin.*` 和三个管理员配置接口族开始。
10. 变更数据库时记录字段、迁移、兼容和回滚；变更 Agent 时记录提示词、上下文、工具和安全降级。
11. 完成边界明确的下一阶段后生成 `第四阶段技术开发文档.md`，不得覆盖本文件。
