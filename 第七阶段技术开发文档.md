# 第七阶段技术开发文档

> AI 读取定位：这是影伴当前最新、当前权威的工程交接文档。后续迭代必须先读仓库根目录 `AGENTS.md` 和本文件；只有追溯邀请码、双 Agent、TMDB、Tavily、Brave、豆包语音或海报惰性补全的设计来源时，才读取第一至第六阶段文档。当前代码、数据库和真实运行状态高于本文档快照；发现不一致时必须先只读验证，再在新的阶段文档中完整记录差异。

## 0. AI 快速接手卡

```text
DOCUMENT_ROLE=current_authoritative_handoff
CURRENT_STAGE=7
STATUS=completed_code_automated_tests_and_real_browser_verification
CODE_ROOT=products/yingban
PRODUCT_START=products/yingban/app.py
HTTP_ROUTING=products/yingban/server.py
AGENT_RUNTIME=products/yingban/agent.py
PERSISTENCE=products/yingban/storage.py
OFFLINE_MOVIE_RETRIEVAL=products/yingban/catalog.py
LIVE_MOVIE_AND_WEB=products/yingban/integrations.py
VOICE_RUNTIME=products/yingban/voice.py
USER_UI=products/yingban/web/index.html + app.js + styles.css
ADMIN_UI=products/yingban/web/admin.html + admin.js + admin.css
TESTS=products/yingban/tests/test_product.py
START_COMMAND=cd products/yingban && python3 app.py serve
TEST_COMMAND=cd products/yingban && python3 -m unittest discover -s tests -v
EXPECTED_TEST_BASELINE=23 tests passing
CURRENT_URL=http://127.0.0.1:8765/
CURRENT_DATABASE_MODE=model
CURRENT_MODEL_PROTOCOL=anthropic
CURRENT_MODEL_ID=deepseek-v4-flash
CURRENT_TMDB_MODE=enabled
CURRENT_WEB_SEARCH_PROVIDER=tavily
CURRENT_WEB_SEARCH_MODE=enabled
CURRENT_WEB_READER_MODE=enabled_douban_and_zhihu_public_pages_only
CURRENT_VOICE_PROVIDER=doubao
CURRENT_VOICE_MODE=enabled
CURRENT_MOVIE_COUNT=47
CURRENT_VOICE_AUTOPLAY_DEFAULT=true
CURRENT_VOICE_AUTOPLAY_STORAGE=browser localStorage key yingban.voiceAutoPlay
CURRENT_REPLY_PRESENTATION=plain text with server-side poster Markdown and emphasis cleanup
FILE_ENTRY_GUARD=file:// web/index.html redirects to http://127.0.0.1:8765/
RUNTIME_EXTRA=ffmpeg required when browser recording is WebM/MP4
KNOWN_BLOCKER=audible autoplay can be rejected by browser policy after asynchronous synthesis; manual voice pill remains available
NEXT_DOCUMENT=products/yingban/第八阶段技术开发文档.md
SUGGESTED_FIRST_CHECK=run 23 tests, open only http://127.0.0.1:8765/, then request three films and assert the reply contains neither [海报] nor image.tmdb.org
```

### 0.1 当前实现一句话

影伴是邀请码制 AI 电影伙伴：双 Agent 共享真实电影资料、安全和长期观影记忆边界；TMDB 负责新电影、真实海报和缺图旧片惰性补图；Tavily `basic` Search 负责全网及豆瓣、知乎公开搜索；豆包负责语音识别和合成；AI 回复保留文字和“波形＋时长”语音胶囊；推荐正文由服务端清除模型偶发输出的海报 Markdown、TMDB 图片 URL 和多余强调符号，误用 `file://` 打开页面时自动回到本地 HTTP 服务。

### 0.2 当前完成状态

- 用户截图中的 `[海报]` 和下一行 `(https://image.tmdb.org/...)` 是模型返回的 Markdown 链接，不是电影卡片或图片加载失败。前端按纯文本安全显示，因此 Markdown 语法和 URL 被原样暴露。
- 新增 `server.py::sanitize_agent_reply()`，在 `/api/chat` 返回前清理演示或真实模型回复。
- 清理规则支持单行 `[海报](URL)`、跨行 `[海报]\n(URL)`、`![海报](URL)`、裸 TMDB 图片 URL、`**强调**` 和 `__强调__`。
- 清理后压缩由删除内容产生的三行以上空白，但保留普通段落的一个空行。
- 清理发生在 JSON 响应形成前，因此前端聊天记录、后续对话历史以及豆包语音合成收到的都是干净文本，不会朗读图片网址。
- 推荐 Agent 默认系统提示词新增禁止输出海报链接、图片 URL 和 Markdown 强调标记的规则。
- 即使管理员数据库中保存的是旧版自定义提示词，`AgentRuntime._context()` 仍会在存在候选电影时注入同一运行时约束；本阶段不依赖管理员手动恢复提示词。
- `web/index.html` 在 `<head>` 最前部检测 `window.location.protocol === "file:"`，误用本地文件打开时调用 `window.location.replace("http://127.0.0.1:8765/")`。
- 直接打开文件之所以出现无样式裸页面，是因为 `/styles.css`、`/app.js`、登录 Cookie 和 `/api/*` 都依赖 HTTP 服务；`file://` 既没有本地服务源，也不能运行产品后端。
- 静态资源查询版本更新为 `v=7`，避免浏览器继续使用上一阶段缓存。
- 23 项自动化测试通过；Python 编译与 JavaScript 语法检查通过。
- 真实浏览器重新推荐三部电影后，回复正文不含 `[海报]`，也不含 `image.tmdb.org`；三张结构化电影卡片仍正常显示真实海报。
- 浏览器自动化安全策略禁止程序化访问 `file://`，因此文件跳转由静态 HTML 测试验证；用户实际误开文件时，浏览器内联脚本会执行跳转。自动化没有尝试绕过浏览器的文件 URL 安全策略。
- 当前本地服务使用本阶段代码运行在 `http://127.0.0.1:8765/`；大模型、TMDB、Tavily 和豆包均为已配置状态。

### 0.3 不可破坏的产品约束

- 一个邀请码只对应一个长期账户；同一码再次登录必须返回原账户。
- 邀请码完整正文不得写入数据库日志、测试输出或阶段文档。
- 用户确认看过电影后自动写入 `watched`，不重复询问；推荐数据层必须硬性排除 `watched` 和近期拒绝项。
- TMDB 动态电影必须先转为统一电影记录并写入 `movies`，才能进入观影状态、推荐、曝光和硬排除链路。
- 缺海报旧片只能在匹配置信度达到阈值时补图；不得仅凭 TMDB 第一条结果直接覆盖本地电影。
- 海报只在结构化电影卡片或片单卡片中显示；模型回复正文不得输出海报 URL 或 Markdown 海报占位符。
- `sanitize_agent_reply()` 只能清理明确属于展示噪声的内容；不得无差别删除豆瓣、知乎或其他正文来源链接。
- 电影海报加载失败不得显示破图图标、坍塌卡片或影响电影操作按钮；必须保留文字封面降级。
- 用户端必须通过 `http://127.0.0.1:8765/` 或部署后的 HTTPS 地址访问，不能把 `web/index.html` 当成独立静态网站使用。
- “聊电影”和“找电影”使用独立系统提示词，但共享身份、安全、隐私、事实与长期记忆边界。
- Agent 不得冒充真人，不得虚构童年、朋友、恋爱、工作或影院观影等真人经历；用户明确询问身份时必须说明自己是 AI。
- 现实烦恼、心理状态和敏感经历不得自动写入长期记忆。
- 高风险自伤或伤人文本必须在普通电影 Agent 和外部工具调用前由产品代码短路。
- Web 搜索摘要只能作为公开资料线索；电影推荐最终仍必须落到产品的真实候选记录，不能凭摘要补造电影。
- 豆瓣和知乎能力只处理公开页面；不得登录、提交 Cookie、绕过验证码、绕过付费墙、绕过访问控制或读取私密内容。
- 管理读取接口不得返回完整 API 密钥、Access Token、管理员口令、邀请码或会话令牌。
- Tavily 必须保持 `basic` 搜索，除非用户明确接受更高额度消耗并完成新的产品决策。
- 联网服务不可用时，种子片库、观影历史、推荐硬去重和已配置的大模型对话仍应继续工作。
- 录音、Base64 音频、转换后的 WAV 和合成音频不得写入长期数据库或项目目录。
- 语音自动播放必须有同页面可发现、可键盘操作、状态可读且可持久保存的关闭按钮。
- 浏览器阻止自动播放时必须安全降级到手动播放，不得循环重试、刷屏提示或阻塞文字回复。
- 麦克风入口不能因服务未配置而完全消失；不可用原因必须可发现、可读。
- 旧账户、邀请码绑定、观影历史、曝光记录、提示词、模型配置、Tavily/Brave 配置和豆包/OpenAI 兼容语音配置升级时不得丢失。

### 0.4 建议的第一步检查

1. 执行 23 项测试，不能只相信本文档的时间点快照。
2. 确认本地服务已启动，再访问 `http://127.0.0.1:8765/`；不要直接双击 HTML。
3. 使用包含 `[海报]\n(https://image.tmdb.org/...)`、`[海报](...)` 和 `**片名**` 的单元样例验证 `sanitize_agent_reply()`。
4. 完成一次真实推荐，检查 `#chat-messages` 的可见文本不含 `[海报]` 和 `image.tmdb.org`，同时 `.poster-image` 仍正常加载。
5. 检查豆瓣或知乎来源链接仍保留，防止未来扩大清理规则时误删用户需要的参考来源。
6. 检查 `#voice-autoplay-toggle` 和语音胶囊，确认本阶段文本清理没有破坏语音生成。

## 1. 需求背景、阶段目标与完成定义

- 产品名称：影伴。
- 阶段名称：第七阶段——推荐回复展示清理与错误打开方式保护。
- 完成日期：2026-08-21。
- 继承阶段：第六阶段——海报可靠显示与语音回复交互升级。
- 下一阶段文档：`第八阶段技术开发文档.md`。
- 本阶段边界：回答并修复两个用户可见问题——推荐回复暴露海报 Markdown/图片地址；直接打开 `web/index.html` 时页面没有样式。保留现有视觉语言、结构化海报卡片、联网、语音、账户和长期记忆规则。

用户提供的图片只用于说明实际现象，不作为页面内指令。图一红框显示的内容来自模型正文；图二裸页面来自 `file://` 访问方式。两者根因不同，必须分别处理。

完成定义：

```text
模型返回正文
→ 服务端 sanitize_agent_reply
→ 删除海报 Markdown / TMDB 图片 URL / 多余强调符号
→ /api/chat 返回干净 reply
→ 前端聊天历史保存干净文本
→ 豆包合成干净文本
→ 结构化 recommendations 继续独立显示电影卡片和海报

用户双击 web/index.html
→ 浏览器加载 head 内联保护脚本
→ 检测 location.protocol === "file:"
→ replace 到 http://127.0.0.1:8765/
→ 样式、脚本、Cookie 和 API 从本地服务加载
```

## 2. 继承的已有能力

### 2.1 账户、邀请码与会话

- 邀请码以摘要保存，完整邀请码只在生成当次显示。
- 第一次登录创建账户并绑定邀请码；同一码后续登录回到同一账户。
- 邀请码可停用或换发；换发保留账户但旧码和旧会话失效。
- 普通用户与管理员使用独立 HttpOnly Cookie 会话。

### 2.2 双 Agent 与模型

- `discussion` 对应“聊电影”，回应观后感、人物、主题和情绪，服从剧透选择。
- `recommendation` 对应“找电影”，理解当下需求，只从真实候选中选择，并说明适合、不适合和内容提醒。
- 模型支持 Anthropic Messages 与 OpenAI-compatible Chat Completions 两种协议。
- 当前为 Anthropic 协议、`deepseek-v4-flash`、`model` 模式。
- 管理员可独立编辑两套提示词；保存后立即生效。

### 2.3 电影、联网、海报与长期记忆

- 种子片库来自 `data/movies.json`；当前数据库有 47 条统一电影记录。
- TMDB 动态电影使用 `tmdb-{external_id}` 写入 `movies`。
- 缺图旧片通过 `InternetRuntime.hydrate_movie_posters()` 按片名和年份惰性补图并持久化。
- 海报容器固定 2:3；图片加载失败时保留文字封面。
- Tavily `basic` Search 支持全网、豆瓣和知乎域名限定搜索；Brave 兼容仍保留。
- 公开页面读取只允许豆瓣和知乎，不登录、不带 Cookie、不绕过访问控制。
- `user_movie_states` 保存 `watched`、`watchlist`、`disliked`；现实烦恼不自动持久化。
- 推荐层硬性排除看过和近期拒绝电影，`recommendation_impressions` 记录曝光。

### 2.4 语音

- 输入区始终显示语音切换按钮。
- 浏览器使用 `MediaRecorder`；必要时后端通过 `ffmpeg` 内存转换为单声道 16 kHz WAV。
- 当前豆包 ASR：`volc.bigasr.auc_turbo`。
- 当前豆包 TTS：`seed-tts-2.0`。
- 当前音色：`zh_female_xiaohe_uranus_bigtts`。
- AI 回复使用“波形＋时长”小胶囊；自动播放默认开启，偏好保存在 `yingban.voiceAutoPlay`。
- 浏览器拒绝有声自动播放时安全降级到手动播放。
- 原始录音、转换音频和合成音频均不落盘。

## 3. 架构与关键决策

影伴继续使用 Python 标准库单进程 HTTP 服务、SQLite 持久化和原生 HTML/CSS/JavaScript，无前端构建步骤。

```text
Model / Demo Agent
  └─ reply text
       │
       ▼
server.py::sanitize_agent_reply
  ├─ remove poster Markdown
  ├─ remove TMDB image URLs
  ├─ remove ** / __ markers
  └─ normalize excess blank lines
       │
       ├─ JSON reply → web/app.js → safe text node
       ├─ chatHistory → next Agent turn
       └─ /api/voice/synthesize → Doubao TTS

recommendations[]
  └─ poster_url → posterNode() → structured movie cards

file://web/index.html
  └─ inline head guard → http://127.0.0.1:8765/
```

关键决策：

1. 不把模型正文中的 `[海报]` 渲染成图片。产品已经有结构化卡片；把模型任意 URL 渲染为图片会扩大安全、布局和内容真实性风险。
2. 在服务端 API 边界清理，而不只在 CSS 中隐藏。这样前端显示、对话历史和 TTS 都不会继续消费技术链接。
3. 清理规则窄化到“海报标签”和 `image.tmdb.org` 图片 URL，不清理普通来源链接，避免破坏豆瓣/知乎引用能力。
4. 同时加入提示词约束和确定性后处理。提示词降低生成概率；服务端清理保证模型不服从时仍不会显示。
5. 自定义管理员提示词可能长期保存，因此关键禁止规则也注入每轮 `_context()`，不能只修改默认常量。
6. 前端继续用 `document.createTextNode()` 显示回复，不启用不受控的 `innerHTML` 或通用 Markdown 渲染器，保持 XSS 边界。
7. `file://` 页面不是支持的离线模式。自动跳转是入口纠错，不代表应用能脱离服务器运行。
8. 跳转使用 `location.replace()` 而不是 `location.href`，避免用户点击返回后再次回到错误的裸页面。
9. 本地跳转目标使用产品默认端口 8765；如果未来支持动态端口或桌面打包，需要重新设计入口发现机制。

## 4. 已实现功能与代码入口

### 4.1 `server.py::sanitize_agent_reply(text)`

清理顺序：

1. `POSTER_MARKDOWN_RE` 删除带可选 `!` 的 `[海报](URL)`、`[电影海报](URL)`、`[poster](URL)`；标签和圆括号之间允许换行。
2. `TMDB_IMAGE_URL_RE` 删除剩余的 `https://image.tmdb.org/t/p/...`，同时删除紧邻的可选圆括号。
3. 行级规则删除没有 URL 的孤立 `[海报]`、`[电影海报]` 或 `[poster]`。
4. 把 `**内容**` 和 `__内容__` 转为普通内容，避免纯文本气泡显示星号或下划线。
5. 删除行尾空白并把三个以上连续空行压缩为一个段落空行。
6. 返回 `.strip()` 后文本。

调用位置：`YingbanHandler._chat()` 在 `self.app.agent.respond()` 成功后、构造 JSON 前执行 `reply = sanitize_agent_reply(reply)`。

意义：

- 用户看不到海报技术地址。
- `state.chatHistory` 保存的是服务端干净文本。
- `attachVoiceReply(assistantMessage, data.reply)` 发送给豆包的是干净文本。
- 结构化 `recommendations[].poster_url` 未经过该函数，不影响真实海报。

### 4.2 `agent.py` 输出约束

`RECOMMENDATION_AGENT_PROMPT` 的工作规则新增：电影海报由卡片显示，正文不得输出海报链接、图片 URL、`[海报](...)` 或 `**` 等 Markdown 标记。

`AgentRuntime._context()` 在候选存在时新增同类运行时说明。因为 `_context()` 每轮与管理员保存的系统提示词拼接，所以旧数据库提示词同样受到此规则约束。

### 4.3 `web/index.html` 文件入口保护

`<head>` 中的内联脚本位于外部 CSS 之前：

```html
<script>
  if (window.location.protocol === "file:") {
    window.location.replace("http://127.0.0.1:8765/");
  }
</script>
```

样式和脚本引用更新为 `/styles.css?v=7`、`/app.js?v=7`。

## 5. Agent 行为、上下文注入、工具和安全降级

本阶段没有改变工具数量、工具参数、Agent Loop 上限、长期记忆字段或高风险短路顺序。当前七个工具仍为：

1. `search_movie(query)`：先查本地真实电影；无结果且 TMDB 可用时查 TMDB 并入库。
2. `get_movie_detail(movie_id)`：读取已收录电影事实、主题、情绪和内容提醒。
3. `list_watched_movies()`：只读取当前账户的看过列表。
4. `mark_movie_watched(movie_id, source)`：确认后写入看过，`source` 受枚举限制。
5. `recommend_movies(request, limit)`：本地推荐器硬排除看过和近期拒绝电影。
6. `search_web(query, source, limit)`：支持 `web`、`douban`、`zhihu`，当前 Tavily 固定 `basic`。
7. `read_public_page(url)`：只允许公开豆瓣和知乎页面。

新增行为边界只涉及输出呈现：

- 模型仍可读取候选中的 `poster_url` 作为产品事实字段，但不得把它复制到正文。
- 服务端清理不是事实校验器，不负责修正片名、剧情、年份或主创；事实仍由候选和工具约束。
- 清理异常不得使整个聊天请求失败；当前正则只处理字符串，无网络或数据库依赖。
- 没有候选电影的讨论回复不会注入海报约束，但所有回复仍经过服务端清理。

安全降级继续为：

- TMDB 不可用：继续返回电影和文字封面。
- 图片 CDN 失败：移除失败图片，保留卡片文字和操作。
- 豆包合成失败：删除语音占位，文字回复保留。
- 浏览器拒绝自动播放：保留手动语音胶囊。
- Web Search 不可用：本地片库、TMDB、历史和模型继续工作。
- 模型不可用：切换演示 Agent。
- 高风险文本：在模型和外部工具前短路并返回安全支持文本。

## 6. 数据结构、迁移和兼容性

本阶段没有新增或修改数据库表、字段、索引和 `app_settings` 键。

当前表及意义：

- `accounts`：长期账户。
- `invite_codes`：邀请码摘要、状态、账户绑定和换发关系。
- `sessions`：普通用户会话摘要和过期时间。
- `admin_sessions`：管理员会话摘要和过期时间。
- `movies`：统一电影事实、海报、来源链接和外部 ID。
- `user_movie_states`：账户级 `watched`、`watchlist`、`disliked`。
- `recommendation_impressions`：推荐曝光和短期去重。
- `safety_events`：最小化安全事件类别。
- `app_settings`：模型、提示词、联网和语音运行时配置。

兼容性：

- 旧数据库无需迁移。
- 旧管理员自定义提示词继续保留；运行时 `_context()` 增加约束，不覆盖用户保存文本。
- 旧电影海报 URL 和补图数据不变。
- 旧账户、邀请码、会话、片单和曝光不变。
- 旧客户端如果直接调用 `/api/chat`，收到的 `reply` 会比以前少海报 Markdown和强调符号，这是有意的向后兼容清理。

## 7. HTTP 接口变化

没有新增或删除路径。

### 7.1 `POST /api/chat`

请求字段不变：`mode`、`message`、`history`、`selected_movie_id` 和 `spoilers_allowed`。

响应结构不变：`reply`、`selected_movie`、`recommendations`、`memory_event`。

语义变化：`reply` 在返回前经过 `sanitize_agent_reply()`。`recommendations[].poster_url` 不受影响。

### 7.2 语音接口

- `POST /api/voice/transcribe` 无变化。
- `POST /api/voice/synthesize` 无变化。
- 前端传给合成接口的 `data.reply` 已在聊天接口服务端清理，因此不会朗读 TMDB 图片地址。

### 7.3 管理接口

模型、提示词、联网和语音管理接口均无契约变化。管理员保存的旧提示词不会被静默修改。

## 8. 页面变化

- 正确 HTTP 入口的视觉设计、布局、电影卡片、语音胶囊和自动播放开关不变。
- 误用 `file://` 时不再停留在没有 CSS 的裸登录页面，而是跳转到默认本地服务地址。
- 推荐回复中的片名、理由和内容提醒保留；`[海报]`、TMDB 图片地址、`**` 和 `__` 不再显示。
- 海报继续位于回复下方的结构化 2:3 电影卡片中。
- 本阶段没有引入 Markdown 渲染器；普通回复仍使用安全文本节点，模型返回的其他 Markdown 语法可能仍按文本显示。

## 9. 配置变化

没有新增服务器配置。

当前关键配置状态：

```text
model_provider=anthropic
model_id=deepseek-v4-flash
tmdb_api_key=<configured secret>
web_search_provider=tavily
web_search_api_key=<configured secret>
voice_provider=doubao
voice_api_key=<configured secret>
voice_stt_model=volc.bigasr.auc_turbo
voice_tts_model=seed-tts-2.0
voice_name=zh_female_xiaohe_uranus_bigtts
```

本文档不保存任何真实密钥、管理员口令、完整邀请码或会话令牌。

## 10. 安全、隐私与可访问性边界

- 不使用 `innerHTML` 渲染模型正文；`renderMessage()` 继续通过 `document.createTextNode(text)` 输出，避免模型文本注入 HTML。
- 海报清理只针对明确标签和 TMDB 图片域名，不执行任意 URL 请求。
- `file://` 保护只跳到本机回环地址，不向第三方发送文件路径、账户信息或凭据。
- 豆包语音数据边界不变：AI 回复文字发送给已配置语音供应商；录音和合成音频不长期保存。
- Web Search、豆瓣和知乎读取边界不变，不登录、不绕过访问限制。
- 管理接口继续只返回密钥是否存在和末四位提示，不返回完整密钥。
- 自动播放开关和语音胶囊的键盘、焦点、`aria-pressed`、动态 `aria-label` 语义不变。

## 11. 修改文件清单

- `server.py`
  - 新增 `POSTER_MARKDOWN_RE`、`TMDB_IMAGE_URL_RE` 和 `sanitize_agent_reply()`。
  - `_chat()` 在返回前清理 Agent 回复。
- `agent.py`
  - 默认推荐提示词新增海报链接和 Markdown 禁止规则。
  - `_context()` 为所有候选推荐轮次注入同一约束。
- `web/index.html`
  - 新增 `file://` 自动跳转保护。
  - 静态资源版本更新为 `v=7`。
- `tests/test_product.py`
  - 新增跨行海报 Markdown、裸 TMDB URL 和强调符号清理测试。
  - 新增 `file://` 入口跳转静态测试。
- `README.md`
  - 补充正确打开方式、回复清理定义、23 项测试基线和阶段文档链接。
- `第七阶段技术开发文档.md`
  - 本阶段最新权威交接文档。

## 12. 测试与验证结果

### 12.1 自动化

执行：

```bash
cd products/yingban
python3 -m py_compile agent.py server.py tests/test_product.py
node --check web/app.js
python3 -m unittest discover -s tests -v
```

结果：

```text
Ran 23 tests in 4.062s
OK
```

新增断言：

- `**《流浪地球》**` 转为 `《流浪地球》`。
- 跨行 `[海报]\n(https://image.tmdb.org/...)` 被完整删除。
- 清理后不含 `image.tmdb.org` 和 `**`。
- `web/index.html` 包含 `file:` 协议检测和默认本地服务跳转。

### 12.2 真实浏览器

步骤：

1. 使用修复后的 `http://127.0.0.1:8765/?stage=7` 打开产品。
2. 进入“下一部电影”。
3. 输入“推荐三部适合周末看的科幻电影，要显示海报。”
4. 等待模型、TMDB 卡片和豆包语音流程完成。
5. 读取 `#chat-messages` 可见文本。

结果：

- 回复包含三部电影的正常片名和推荐理由。
- `hasPosterMarkup=false`，没有 `[海报]`。
- `hasTmdbText=false`，没有 `image.tmdb.org`。
- 《群体》《最后孤屋》《逃出绝命街》三张结构化海报正常显示。
- 语音准备流程继续出现，说明文本清理没有阻断豆包链路。

文件 URL 验证说明：浏览器自动化安全策略拒绝导航到 `file://`，因此没有绕过该限制；使用自动化测试读取 HTML 并断言内联跳转逻辑。用户真实浏览器直接打开文件时不受该自动化工具限制，会执行该脚本。

## 13. 启动与使用方法

```bash
cd products/yingban
python3 app.py serve
```

然后使用：

- 用户端：`http://127.0.0.1:8765/`
- 管理后台：`http://127.0.0.1:8765/admin`

不要双击：

```text
products/yingban/web/index.html
```

原因不是 CSS 文件损坏，而是该 HTML 不是可独立运行的静态站点。影伴登录、会话、电影、联网、语音和管理能力都依赖 `app.py serve`。

## 14. 尚未完成事项与已知风险

### 14.1 尚未完成

- 普通 Markdown 标题、列表和一般链接仍按纯文本显示；本阶段只处理用户实际遇到的海报链接和强调符号。
- 尚未实现受控 Markdown 子集或结构化模型响应 schema。
- `file://` 自动跳转固定使用默认端口 8765；自定义 `YINGBAN_PORT` 的用户仍需手动打开实际地址。
- 尚未增加服务未启动时的桌面启动器或友好诊断页。
- 自动播放偏好仍按浏览器保存，不跨设备同步。
- 自动有声播放仍可能被浏览器策略拦截。

### 14.2 已知风险

- 如果模型使用未知标签描述海报，例如 `[封面图]` 且不是 TMDB 域名，当前窄规则可能保留它；扩大规则前必须避免误删普通来源链接。
- 正则清理不是完整 Markdown 解析器；它只承担确定的展示噪声清理。
- 固定本地端口跳转在端口被修改时不准确。未来若支持多实例，应由启动器生成入口页面或注入端口。
- `file://` 页面执行内联脚本依赖浏览器允许本地 HTML JavaScript；主流浏览器允许，但组织安全策略可能禁用。
- TMDB 海报仍是外部资源，CDN 失败时依赖文字封面降级。
- 当前 HTTP 本地模式未启用 `Secure` Cookie；公开部署必须使用 HTTPS 并开启 `YINGBAN_COOKIE_SECURE`。

## 15. 下一阶段建议

1. 将推荐 Agent 输出改成结构化 JSON，例如 `intro`、`choices[]`、`closing_question`，从根源消除展示层解析自由文本 Markdown 的需要。
2. 如果需要富文本，设计严格允许列表：段落、粗体和普通 HTTPS 来源链接；禁止图片、HTML、脚本、iframe 和任意样式。
3. 为自定义端口增加启动器或运行时生成的本地入口，避免 `file://` 保护硬编码 8765。
4. 为模型正文与语音合成分别设计文本：屏幕保留完整理由，语音使用更短、无 URL 的摘要。
5. 增加端到端测试，验证 `/api/chat` 的真实模型模拟返回海报 Markdown 后，HTTP 响应和 TTS 请求均不含图片 URL。
6. 在管理后台增加“回复格式预览”，让管理员修改提示词时能看到纯文本显示效果。

## 16. AI 接手入口

下一次迭代 AI 必须：

1. 先完整阅读仓库根目录 `AGENTS.md` 和本文件。
2. 检查 `server.py::sanitize_agent_reply()`、`YingbanHandler._chat()`、`agent.py::RECOMMENDATION_AGENT_PROMPT`、`AgentRuntime._context()` 和 `web/index.html` 头部跳转脚本。
3. 执行 23 项测试并记录真实结果。
4. 只读检查数据库电影数量与配置启用状态，不输出任何完整凭据。
5. 真实发送一次推荐并确认正文不含 `[海报]`、`image.tmdb.org`，结构化卡片仍有海报。
6. 保持服务端文本清理的窄范围，新增规则前测试豆瓣和知乎来源链接不被误删。
7. 若完成新的边界明确开发，创建 `第八阶段技术开发文档.md`，不得覆盖本文件。

