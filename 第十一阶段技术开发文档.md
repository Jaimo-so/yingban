# 影伴第十一阶段技术开发文档

> 文档日期：2026-08-22
> 阶段主题：本机历史对话与“每次普通进入都是新对话”
> 开发分支：`feature/yingban-stage-9`（分支名保留历史命名；第十、十一阶段于 2026-08-22 一并提交并快进合并到本地 `main`）
> 文档性质：下一次版本迭代 AI 的工程交接上下文和当前状态快照，不是宣传稿。

## AI 快速接手卡

- **当前阶段**：第十一阶段；这是用户在原定 P2 前插入的一次边界明确的本机聊天体验迭代，不代表“共同观影与上线就绪”已经开始。
- **完成状态**：新会话语义、本机历史列表、按电影命名、显式恢复、单条删除确认、全部清空、旧草稿迁移、桌面真实浏览器验收和 50 项自动化已完成；第十、十一阶段改动已一并提交并快进合并到本地 `main`。
- **权威代码目录**：`/Users/Admin/Tool/agent-blueprint/products/yingban/`。
- **当前分支状态**：工作树仍停留在 `feature/yingban-stage-9`；本地 `main` 与该分支已指向同一个第十/十一阶段交付提交。仓库未配置远程地址，因此没有远程推送状态。
- **启动入口**：`cd /Users/Admin/Tool/agent-blueprint/products/yingban && python3 app.py serve`。
- **用户端**：`http://127.0.0.1:8765/`。
- **管理端**：`http://127.0.0.1:8765/admin`。
- **测试命令**：`cd /Users/Admin/Tool/agent-blueprint/products/yingban && python3 -m unittest discover -s tests -v`。
- **当前测试基线**：50 项，2026-08-22 全部通过；前 49 项测试意义继续保留。
- **当前运行模式**：`GET /api/health` 返回 `{"ok": true, "mode": "model"}`；数据库非敏感配置显示模型标识 `deepseek-v4-flash`。交接时本机 `127.0.0.1:8765` 有 Python 服务监听并可从浏览器使用。
- **当前主数据库时点**：账户 2、电影 80、电影状态 9、观后感版本 2、电影会话摘要 0、月度回顾 1、分享卡 5、后台任务 17；`PRAGMA foreign_key_check` 无结果。本阶段没有数据库写入、schema 变化或迁移。
- **核心代码入口**：`web/app.js` 的 `localConversationStore`、`openChat()`、`renderConversationHistory()`、`openConversationHistory()`；`web/index.html` 的聊天顶部入口和两个历史对话框；`web/styles.css` 的历史列表布局；`tests/test_product.py` 的第十一阶段静态契约测试。
- **不可破坏的产品约束**：普通进入“聊电影”必须是新对话；恢复旧对话必须由用户在历史列表中显式选择；原始聊天只存在当前浏览器最多 7 天，不上传服务端；历史标题来自已确认电影；服务端 `conversation_summaries` 仍必须由用户单独点击“下次继续聊”并确认，不得与本机历史混淆；未来云端同步必须重新确认同意、冲突、删除和安全契约。
- **已知阻塞**：正式成本价格表仍需依据供应商账单填写；P2 的小组人数与默认过期、正式登录方式、生产数据库和部署平台仍未确认；进程内后台 worker 仍没有跨进程恢复。
- **下一阶段文档名**：`第十二阶段技术开发文档.md`，不得覆盖本文档或更早阶段文档。
- **建议第一步检查**：完整读取根 `AGENTS.md`、`影伴产品开发总文档.md` 和本文档；确认分支与工作区；复跑 50 项测试；在真实浏览器分别验证普通进入、历史恢复和 7 天本机删除；再决定是否进入原定 P2。

## 1. 需求背景

第十阶段把进行中的电影讨论保存为当前浏览器 7 天草稿，并在重新进入时自动恢复。用户进一步明确要求：每次打开“聊电影”都应该是新的对话，同时需要一个“历史对话”按钮查找以前的聊天记录，聊天记录按所聊电影命名。

旧行为把“重新进入”默认解释为“继续上一段草稿”。新要求把两种意图拆开：

1. 普通进入代表开始一段新的电影聊天，不携带旧消息、旧电影和未发送输入。
2. 继续旧聊天必须进入“历史对话”，看到电影名和时间，再显式选择“打开对话”。

这不是把原始聊天改为服务端长期记忆。用户此前确认的“当前仅本机保存 7 天、保留未来改为云端的可能”继续有效。

## 2. 阶段目标

1. 让“聊电影”普通入口每次都生成独立会话 ID 和空白上下文。
2. 让已确认电影的本机聊天按《电影名》显示在历史列表中。
3. 让同一电影的多段聊天通过更新时间区分，而不是互相覆盖。
4. 让恢复、删除和清空都是用户可见、可控的动作。
5. 保持 7 天本机保留、账户提示隔离和未来可替换存储适配器的边界。
6. 兼容第十阶段已经存在的 `yingban.chatDraft.v1.*` 草稿，不要求用户手工迁移。

## 3. 进入条件验证

- 完整读取了根 `AGENTS.md` 和最新的 `第十阶段技术开发文档.md`。
- 当前分支只读确认为 `feature/yingban-stage-9`。
- 第十阶段记录的启动入口、模型模式、7 天保留和本机存储边界通过当前代码与运行状态重新核对。
- 开发后全量测试为 `50/50 OK`；没有用阶段文档中的 49 项时点数字替代重新执行。
- `GET /api/health` 在沙箱外只读检查返回 `mode=model`；模型标识从 `app_settings.model_id` 的非敏感值确认是 `deepseek-v4-flash`。
- 主数据库外键检查为空。本阶段功能只修改浏览器静态资源，不修改 SQLite 数据。

## 4. 继承的已有能力

以下既有能力继续有效，其定义和意义没有因本阶段被删除：

- 邀请码登录、账户隔离、5 部电影冷启动、证据型口味画像和推荐曝光反馈。
- 推荐在数据层硬排除已看和持续不感兴趣；`watched`、`watchlist`、`not_now` 和 `disliked` 语义继续分离。
- 讨论与推荐使用独立系统提示词；AI 身份透明；敏感风险时安全降级优先。
- 观后感保持版本化草稿、确认、编辑、锁定、解锁和独立删除，不由聊天主回复直接覆盖。
- 用户显式同意后的 `conversation_summaries` 仍是服务端电影讨论摘要，不含原始聊天，可与观后感独立删除。
- 本周建议、想看跟进、月度回顾、精美 HTML 分享、后台任务和稳定分页继续工作。
- 原始录音和合成音频不长期保存；主回复始终保留文字。
- 产品事件和模型用量事件继续过滤原始聊天正文、敏感内容、令牌和密钥。

## 5. 范围内功能

### 5.1 普通进入新对话

- 首页点击“聊聊我看完的电影”、从其他功能进入讨论或从已恢复对话点击“开始新对话”时，`openChat("discussion")` 创建新的 `conversation.id`。
- 新对话的 `state.chatHistory` 为空、`state.selectedMovie` 只使用调用方显式传入的电影、输入框为空、剧透开关回到默认开启。
- 页面只渲染一条当前配置的开场白，不自动渲染本机旧聊天，也不自动把服务端电影摘要塞入新聊天。
- 在当前对话切换到其他入口前，仍会先把当前有效电影会话写回本机历史。

### 5.2 历史对话列表

- 聊天页顶部只在 `discussion` 模式显示“历史对话”。
- 列表只展示已经确认 `movie.title_zh` 的记录，因此标题始终是 `《电影中文名》`。
- 每条记录显示更新时间、用户发言次数、7 天过期说明和第一条用户消息预览。
- 多次聊同一电影会得到不同会话 ID；电影标题可以相同，时间用于区分具体一段。
- 未确认电影的半成品输入可以在本机短时保存，但不作为“按电影命名”的历史记录展示。

### 5.3 显式恢复

- 只有历史列表中的“打开对话”调用 `openChat("discussion", conversation.movie, { conversation })`。
- 恢复后渲染该记录保存的消息、输入、电影和剧透设置。
- 页面显示“正在查看《片名》历史对话；新的内容仍只保存在此设备 7 天”，并提供“开始新对话”。
- 再次通过普通入口进入时仍是新对话，不因为刚恢复过历史而自动续接。

### 5.4 删除与清空

- 单条“删除”先打开产品内确认对话框，明确删除后无法恢复且不影响电影片单和观后感。
- 取消删除会回到历史列表，记录不变。
- 删除当前恢复的记录后会立即开始同一电影的新对话，避免页面继续编辑已经删除的记录。
- “连续性与主动建议设置”保留“清空此设备全部历史对话”。
- 退出登录继续提供“保留历史并退出”和“清空历史并退出”。

### 5.5 旧草稿迁移

- 登录成功后 `showAuthenticated()` 调用 `localConversationStore.migrateLegacyDrafts()`。
- 有效的 `yingban.chatDraft.v1.<account>.discussion.*` 转换为新的 v2 记录并删除旧键。
- 过期、空白或 JSON 损坏的旧键被清理或忽略，不阻止其他合法旧记录迁移。
- 迁移后的有效期不超过旧记录原有效期，也不超过当前 7 天保留上限。

## 6. 明确不做

- 不把原始聊天上传到 SQLite、对象存储或第三方云端。
- 不开发跨设备同步、多人共享聊天或公共聊天索引。
- 不把服务端 `conversation_summaries` 当作历史聊天全文，也不让它自动恢复旧会话。
- 不使用模型自动为未确认电影的记录猜标题。
- 不提供无限期保留、导出聊天全文或云端冲突合并。
- 不改变观后感、片单、口味画像、推荐、月度回顾和分享卡的数据契约。
- 不进入原定 P2 的双人/小组观影、正式账户和生产部署开发。

## 7. 架构与关键决策

### 7.1 会话身份与普通入口语义分离

`state.currentConversationId` 表示当前浏览器会话资源，`state.restoredConversation` 只表示当前页面是否由历史显式恢复。普通进入不查找“最近一个会话”，而是调用 `localConversationStore.newId()`。这保证“新对话”是稳定契约，不依赖页面刷新、最近电影或本机是否存在历史。

### 7.2 本机适配器升级为多记录模型

第十阶段的 `localDraftStore` 每个账户/模式/电影主要对应一个草稿。第十一阶段改为 `localConversationStore`，键前缀为：

```text
yingban.conversation.v2.<account_hint>.<conversation_id>
```

多记录模型的意义是让同一电影可以有多段独立聊天，同时继续把具体存储介质隔离在适配器中。未来若改云端，只能替换存储实现，不能绕过用户对同步、保留、删除和冲突的重新确认。

### 7.3 历史命名只使用确认电影

列表过滤条件要求 `value.movie.title_zh` 存在。该决策避免把第一句用户文字、AI 猜测或“尚未指定电影”当作会话名称。电影一经 `/api/chat` 的 `selected_movie` 确认，后续 `writeCurrent()` 会把标题更新为 `《title_zh》`。

### 7.4 DOM 安全与删除确认

历史标题、预览和元数据全部通过 `document.createElement()` 与 `textContent` 渲染，没有把聊天正文拼入 `innerHTML`。删除使用二次确认对话框，不把不可恢复动作绑定在单次误触上。

## 8. 已实现功能

- `localConversationStore.list/read/writeCurrent/remove/clearAll/migrateLegacyDrafts/newId`。
- `openChat(mode, movie, options)` 的新建与显式恢复双路径。
- `conversationHistoryItem()` 的安全列表项渲染。
- `renderConversationHistory()` 的计数、空状态、打开与删除动作。
- `openConversationHistory()` 在展示列表前保存当前有效对话。
- 聊天顶部历史入口、历史列表对话框、删除确认对话框。
- 桌面与窄屏响应式布局；桌面显示时钟图标与文字，手机避免顶部控件压缩标题。
- 设置和退出文案从“草稿”统一为“历史对话”。

## 9. 数据结构与迁移

### 9.1 v2 本机记录

每条 `localStorage` JSON 结构为：

```json
{
  "schema_version": 2,
  "storage": "local-browser",
  "id": "conv_<uuid-or-fallback>",
  "account_hint": "<账户末尾提示>",
  "mode": "discussion",
  "title": "《电影中文名》",
  "movie": { "id": "<movie_id>", "title_zh": "<电影名>" },
  "history": [{ "role": "user|assistant", "content": "<消息>" }],
  "input": "<未发送输入>",
  "spoilers_allowed": true,
  "created_at": 0,
  "updated_at": 0,
  "expires_at": 0
}
```

约束：

- `history` 写入最近 40 条。
- `input` 最多 6000 字符。
- `expires_at = updated_at + 7 天`。
- `schema_version/storage/account_hint/mode/expiry` 任一不合法时记录不可恢复。
- 列表按 `updated_at` 降序。
- `account_hint` 只用于当前浏览器键空间分区，不是认证凭证；服务端安全仍依赖 HttpOnly 会话。

### 9.2 SQLite

本阶段没有新增表、字段、索引或迁移版本；没有旧数据回填和数据库回滚风险。`conversation_summaries`、`movie_reflections` 与 `user_movie_states` 均未改变。

## 10. API 变化

本阶段没有新增或修改 HTTP API。继续使用：

- `GET /api/me`：读取开场白和当前账户状态。
- `POST /api/chat`：只接收当前会话所需的历史，不持久化原始聊天。
- 既有电影会话摘要 API：只处理用户显式同意的摘要资源，与本机历史列表无自动同步关系。

公共分享、管理、回顾、观后感、推荐和后台任务接口没有变化。

## 11. 页面与交互变化

- 聊天页顶部增加 `#chat-history-button`。
- 新增 `#conversation-history-dialog`，包含隐私说明、记录数量和列表。
- 新增 `#conversation-history-delete-dialog`，明确单条删除影响范围。
- 恢复记录时显示 `#chat-draft-notice`，按钮语义改为“开始新对话”。旧 DOM ID 为兼容现有事件代码继续保留，但用户可见文案不再称作“清空当前草稿”。
- 设置页说明“每次进入聊电影都会开始新对话”，退出页使用“历史对话”称谓。
- 列表卡片保持现有纸张、酒红、衬线标题和克制边框体系，没有引入新的渐变、装饰性动画或不同视觉语言。

## 12. Agent 行为变化

本阶段没有修改 Agent 系统提示词、工具权限、循环上限、模型调用或长期记忆规则。

重要边界：新对话不自动注入旧本机消息或服务端摘要。这是前端会话上下文选择变化，不是 Agent 获得或失去长期记忆能力。显式恢复时，旧消息仍按原有 `/api/chat` 请求约束传递；服务端最多接受最近 20 条，Agent 内部继续执行长度约束和安全降级。

## 13. 配置与第三方变化

- 没有新增 `.env` 键。
- 7 天仍是前端 `retentionMs: 7 * 24 * 60 * 60 * 1000` 产品常量。
- 没有新增模型、TMDB、Tavily、语音或其他第三方调用。
- `index.html` 的静态脚本版本更新为 `/app.js?v=14`；`styles.css` 版本为 v15，`share.html` 同步引用 v15 以避免缓存不一致。

## 14. 安全、隐私与合规边界

- 原始聊天正文仍不进入长期数据库。
- 本机历史按当前账户提示分区、7 天过期，可单条删除、全部清空或退出时清空。
- 共用设备中的同一浏览器系统用户仍可能访问本机历史，因此界面继续明确本机范围和清空入口。
- 历史列表只展示已确认电影记录，不把敏感现实话题自动变成会话标题。
- DOM 使用 `textContent`，避免聊天正文触发 HTML 注入。
- 未来云端保存只是架构可能，当前没有授权；云端方案必须另行定义加密、权限、同步冲突、导出、删除、留存和跨设备退出。
- 单条历史删除不删除电影状态、观后感、回顾或服务端摘要；这些是不同资源和不同删除契约。

## 15. 修改文件清单

- `web/app.js`：新增多记录本机历史适配器、新对话语义、列表/恢复/删除/迁移逻辑。
- `web/index.html`：历史入口、列表和删除对话框、设置与退出文案、静态资源版本。
- `web/styles.css`：历史列表、确认框、聊天顶部和响应式样式。
- `web/share.html`：同步 `styles.css?v=15`，避免公共页继续命中旧缓存。
- `tests/test_product.py`：新增第十一阶段静态契约测试。
- `README.md`：更新历史对话、隐私、测试基线和阶段文档入口。
- `影伴产品开发总文档.md`：更新当前阶段、会话契约、路线、测试与决策记录。
- `第十一阶段技术开发文档.md`：本文档。

`agent.py`、`server.py`、`storage.py` 等文件在当前工作区还包含第十阶段及随后修复的既有未提交变更，但不是本次历史对话功能新增的修改范围；不得误删或回退。

## 16. 测试与验收结果

### 16.1 自动化

执行：

```sh
node --check web/app.js
python3 -m py_compile agent.py server.py storage.py tests/test_product.py
python3 -m unittest discover -s tests -v
```

结果：JavaScript 与 Python 语法通过；`Ran 50 tests ... OK`。第十一阶段新增测试 `test_stage_eleven_discussion_entry_is_new_and_history_restore_is_explicit`，验证：

- 历史按钮、列表和删除确认对话框存在。
- v2 前缀与 v1 迁移入口存在。
- 普通入口生成新 ID，恢复路径必须传入 `options.conversation`。
- 历史只展示确认电影记录。
- 本机消息上限 40 条。
- 历史卡片和操作区样式存在。

### 16.2 真实浏览器

在当前账户和正在运行的 `http://127.0.0.1:8765/` 验证：

1. 首页点击“聊聊我看完的电影”后标题为“刚看完哪一部？”，历史恢复提示不可见，消息区只有一条开场白。
2. “历史对话”可见；旧版草稿迁移后的列表只显示 `《流浪地球》`，不展示“尚未指定电影”的半成品。
3. 点击“打开对话”后标题为“聊聊《流浪地球》”，恢复提示可见，原 20 条消息正常渲染。
4. 回首页再次普通进入后重新只有一条开场白，没有静默续接。
5. 点击删除会先出现确认框；点击取消后历史列表仍有 1 条，未删除用户记录。
6. 对话框桌面截图检查通过：标题、隐私说明、计数、片名、时间、预览、打开与删除形成清晰层级，没有横向溢出。

未在验收中确认删除用户真实历史，避免为测试破坏用户数据。

## 17. 启动与使用方法

```sh
cd /Users/Admin/Tool/agent-blueprint/products/yingban
python3 app.py serve
```

打开 `http://127.0.0.1:8765/`：

1. 从首页普通进入“聊电影”，得到新对话。
2. 在聊天顶部点击“历史对话”，按《电影名》和时间查找旧记录。
3. 点击“打开对话”显式恢复；点击“开始新对话”离开恢复上下文。
4. 单条删除需要二次确认；全部清空在“连续性与主动建议设置”中。
5. 退出账户时选择保留或清空本机历史。

不要双击 `web/index.html`；必须通过本地 HTTP 服务使用登录和 API。

## 18. 实际实现与总文档计划的差异

原总文档把第十一阶段规划为 P2“共同观影与上线就绪”。本次用户在进入 P2 前提出了独立且明确的本机会话体验需求，因此实际第十一阶段用于“本机历史对话与新会话语义”。

P2 的定义、进入条件、数据权限、安全、部署和验收意义没有删除或压缩，只顺延为第十二阶段候选。原来的 P0→P1→P2 产品优先级仍有效；本次插入阶段不是共享功能，也没有提前满足 P2。

## 19. 尚未完成事项

- 本机历史不跨浏览器、不跨设备同步；未来云端保存尚未授权。
- 没有历史全文导出、搜索、置顶、手工改名和永久保留。
- 未确认电影的半成品记录不展示在历史列表，7 天到期或全部清空时才移除。
- P2 小组人数/默认过期、正式登录、数据库和部署平台仍待用户确认。
- 正式成本价格表仍需管理员根据供应商账单填写。
- 后台任务仍为当前进程守护线程，服务重启后的可靠恢复尚未生产化。

## 20. 已知风险与回滚风险

- 浏览器 `localStorage` 容量有限；当前每条最多 40 条消息、输入最多 6000 字符，但没有总容量配额。写入失败会安静降级，不影响当前聊天。
- `account_hint` 不是秘密账户 ID；它用于本地分区，不可被误作服务端认证。
- 同一电影多段聊天标题相同，当前靠时间区分；大量历史时可能需要搜索或更细的副标题。
- 普通进入不再自动恢复旧草稿，这是用户明确的新语义；回滚到旧行为会重新引入意图混淆，不能只回退 UI 按钮。
- 回滚 v2 时不能直接删除 `yingban.conversation.v2.*`，否则会丢失用户仍在 7 天期内的本机历史。安全回滚应保留读取兼容或提供 v2→v1 迁移。
- 服务端摘要不再自动注入新聊天；若未来恢复这种行为，必须明确区分“新对话”和“继续上次摘要”，并重新获得用户产品决策。

## 21. 产品指标与成本观测

本阶段没有新增服务端产品事件或模型调用，因此不会增加模型成本。当前无法在服务端统计“打开历史列表”“恢复旧对话”“删除本机历史”，因为这些动作按隐私边界完全发生在浏览器本机。

如果未来需要观测，只能新增不含电影名、消息正文和本机会话 ID的结构化事件，例如 `history_opened` 或 `history_restored` 的计数；是否采集必须再次评估必要性和用户预期，不能把本机隐私功能变成隐式内容分析。

## 22. 下一阶段进入条件

若下一阶段进入原定 P2，必须先由用户确认：

1. 双人或小组的最大人数。
2. 邀请和共享空间的默认过期时间。
3. 正式账户登录方式。
4. 生产数据库、部署平台、地区、域名和运营主体。
5. 数据导出、删除、备份、恢复和审计契约。

还必须复跑 50 项测试，重新核对主数据库、`/api/health`、后台任务和第三方配置，并把真实本机历史在桌面与移动端都再验收一次。

## 23. AI 接手入口

下一次 AI 必须按以下顺序工作：

1. 读取根 `AGENTS.md`。
2. 完整读取 `影伴产品开发总文档.md`。
3. 完整读取本文档，不能只读快速接手卡。
4. 用当前代码重新验证 `localConversationStore` 的 v2 schema、7 天过期、v1 迁移、普通新建和显式恢复。
5. 执行 50 项全量测试和 JavaScript/Python 语法检查。
6. 只读核对运行模式、模型标识、SQLite schema、外键和主数据数量。
7. 未经用户重新确认，不得把原始历史对话上传云端，不得自动恢复旧会话，不得进入 P2 的人数、登录和部署默认实现。
8. 完成下一次边界明确开发后，新建 `第十二阶段技术开发文档.md`，不得覆盖本文档。
