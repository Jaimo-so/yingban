# 第八阶段技术开发文档

## AI 快速接手卡

- 当前阶段：第八阶段——聊天界面、可配置开场白与账户级电影观后感。
- 完成状态：已完成；Python 编译、JavaScript 语法检查、27 项自动化测试和真实浏览器验收均通过。
- 权威代码目录：`products/yingban/`。
- 启动入口：在该目录执行 `python3 app.py serve`，用户端为 `http://127.0.0.1:8765/`，管理员后台为 `http://127.0.0.1:8765/admin`。
- 测试命令：`python3 -m unittest discover -s tests -v`。
- 语法检查：`python3 -m py_compile agent.py integrations.py storage.py server.py tests/test_product.py`，以及 `node --check web/app.js && node --check web/admin.js`。
- 当前运行模式：真实模型模式，当前模型标识为 `deepseek-v4-flash`；TMDB、Tavily Web Search 和豆包语音已接入。密钥只记录配置位置，不在本文保存真实值。
- 当前数据快照：2 个账户、55 部 SQLite 电影目录记录、2 条账户电影状态、1 条观后感笔记；这是 2026-08-21 验收时的时间点数据，接手时必须重新查询。
- 核心代码入口：`server.py` 的 `_handle_api_get()`、`_handle_api_post()` 和 `_chat()`；`agent.py` 的 `AgentService.openings()`、`save_openings()`、`summarize_reflection()`；`storage.py` 的 `Store.save_movie_reflection()`、`movie_states()`；`web/app.js` 的 `openChat()`、`renderHistory()`、`openReflection()`、`attachVoiceReply()`。
- 不可破坏的产品约束：邀请码必须稳定绑定账户；推荐必须在数据层硬排除已看电影；聊天正文和原始音频不得写入长期数据库；观后感只能保存电影本身相关内容，不得把现实烦恼或敏感经历混入笔记；海报失败必须降级为文字封面；模型和第三方密钥不得返回给浏览器；AI 身份必须透明；两套 Agent 提示词继续独立。
- 已知阻塞：没有功能性阻塞。标准库 HTTP 服务、SQLite 明文应用配置和本地单机部署仍不是生产级架构；真实模型生成观后感会增加一次模型调用和响应等待时间。
- 下一阶段文档名：`第九阶段技术开发文档.md`。
- 建议的第一步检查：先读根目录 `AGENTS.md` 和本文，再运行 27 项测试，随后访问 `/api/health`、`/api/me`、`/api/history?state=watched`，确认实际运行模式、开场白、海报和观后感数据与本文时间点快照是否一致。

## 1. 需求背景

第七阶段解决了推荐正文中错误展示海报 Markdown、裸 TMDB 图片地址和本地 `file://` 打开方式的问题。本阶段在此基础上处理四个明确的产品需求：

1. 聊天输入框原先由外框、文本框、语音按钮和发送按钮叠加组成，层级和留白不协调，需要统一成符合影伴现有纸张与酒红主题的输入组件。
2. 聊天开场白此前写死在前端，管理员不能调整；需要在后台提供配置入口。
3. AI 语音胶囊原先使用偏黑背景并通过定位压在消息气泡左上方，颜色与主题不一致，而且遮挡“阿映”名称。
4. 聊电影时只有离散聊天内容，没有形成可回看的电影感受记录；需要自动整理观后感，并在“我的电影 → 看过”中从电影海报进入查看。

附图只作为功能位置与问题表现参考，不继承其视觉风格。影伴继续使用既有暖白纸张、深棕文字、酒红强调色、衬线标题和克制阴影体系。

## 2. 阶段目标

本阶段的交付目标是形成一个完整闭环：

1. 用户进入聊电影或推荐电影时，收到后台可配置的对应开场白。
2. 用户可以用统一的聊天输入组件发送文字或切换语音输入。
3. AI 文字回复下方出现不遮挡角色名的主题化语音胶囊，并继续支持自动播放偏好、播放、暂停和重播。
4. 当聊电影已经确认具体电影时，服务端把本轮以及已有讨论中与电影直接相关的内容整理成账户级观后感笔记。
5. 该电影自动进入“看过”；用户点击“看过”列表中的电影海报，打开带海报、元数据和笔记正文的弹窗，并可继续聊该片。
6. 海报补全不能丢失账户级 `state`、`note`、`rating`、`source` 等数据。

## 3. 继承的已有能力

### 3.1 邀请码和账户

- 邀请码同时承担首次注册与后续登录凭证。
- 同一邀请码始终进入同一账户。
- 邀请码停用后，该码和已有会话失效。
- 换发邀请码保留原账户和观影历史，旧码失效。
- 数据库只保存邀请码摘要和末尾提示字符，不保存完整邀请码。

### 3.2 电影目录、历史和推荐

- `data/movies.json` 提供离线种子片库。
- TMDB 搜索得到的新电影以 `tmdb-{id}` 进入 `movies` 表。
- 推荐层通过 `Store.watched_ids(account_id)` 硬性排除已看电影，不能只依赖提示词。
- 账户电影状态包括 `watched`、`watchlist` 和 `disliked`。
- 推荐卡片和片单优先展示结构化 `poster_url`，图片失败时使用文字封面。
- 缺失海报可以按片名和年份通过 TMDB 惰性补全并持久化。

### 3.3 Agent、联网和语音

- `discussion` 和 `recommendation` 使用两套独立系统提示词。
- 无模型密钥时使用演示 Agent；有密钥时运行真实模型和最多 8 轮工具调用。
- 领域工具保持 7 个：`search_movie`、`get_movie_detail`、`list_watched_movies`、`mark_movie_watched`、`recommend_movies`、`search_web`、`read_public_page`。
- Web Search 默认接入 Tavily，也保留 Brave 配置；豆瓣和知乎通过域名限定搜索与公开页面阅读器访问，不登录、不带 Cookie、不绕过验证码。
- 豆包语音和 OpenAI 兼容语音均通过后端调用；原始录音和合成音频不写入长期数据库。
- AI 回复保留文字，语音播放器是附加能力；用户的自动播放偏好保存在当前浏览器。

### 3.4 已有隐私边界

- 原始聊天正文只存在当前页面，并在每轮请求中回传；后端不把聊天全文写入 SQLite。
- 现实烦恼不进入长期观影记忆。
- 安全事件只保存账户、类别和时间，不保存原话。
- 管理接口对模型、联网和语音密钥只返回是否已配置及末四位掩码。

## 4. 架构与关键决策

### 4.1 开场白分为三种场景

开场白没有继续复用系统提示词，因为二者职责不同：系统提示词约束 Agent 行为，开场白只决定聊天页第一次显示给用户的界面文案。本阶段定义三个稳定键：

- `discussion`：进入聊电影但尚未选择具体电影。
- `discussion_movie`：已经确认具体电影；支持 `{movie}` 占位符。
- `recommendation`：进入找电影。

默认文案定义在 `agent.py::DEFAULT_OPENINGS`。管理员保存后写入 `app_settings`；没有数据库值时自动回退默认文案。前端每次执行 `openChat()` 都刷新 `/api/me`，因此管理员保存后，用户下一次进入聊天即可获得新文案，无需重启服务。

### 4.2 观后感复用 `user_movie_states.note`

没有增加新表。现有 `user_movie_states.note` 原本就是账户与电影状态关联上的可选文本字段，正好满足“每个账户、每部电影一份笔记”的语义。这样可以继续使用 `(account_id, movie_id)` 主键保证账户隔离，并避免另建表造成重复状态源。

本阶段把该字段的产品定义明确为：

> 账户针对一部已经看过的电影，由讨论中与电影直接相关的感受、理解、困惑、主题关注和人物关注整理而成的观后感摘要。它不是聊天全文、现实生活日记、心理档案或医疗记录。

`Store.save_movie_reflection()` 会保证保存笔记时状态为 `watched`。若该电影尚无状态记录，就以 `source='discussion_reflection'` 创建已看状态；若已有已看状态，只更新 `note` 和 `updated_at`。

### 4.3 摘要是第二次、受限的模型调用

主回复生成完毕后，`server.py::_chat()` 只在以下条件同时成立时触发 `AgentService.summarize_reflection()`：

- 当前模式是 `discussion`；
- 已经解析出 `selected_movie`；
- 当前 Agent 处于真实模型模式。

摘要调用输入包括电影元数据、已有观后感、受长度限制的近期对话、本轮用户消息和本轮助手回复。模型被明确要求：

- 只提取与该电影直接相关的信息；
- 不记录现实烦恼、身份、家庭、工作、学校、联系方式、心理、医疗或其他敏感经历；
- 不推断用户没有说过的事实；
- 不写标题、列表、Markdown、链接和技术字段；
- 没有新电影感受时返回 `NO_UPDATE`；
- 输出目标为 80–500 个中文字符，服务端最终上限为 2000 个字符。

演示模式不伪造观后感，保持已有笔记不变。摘要失败也不影响主聊天回复；服务端记录无敏感正文的警告并回退已有笔记。

### 4.4 海报补全必须保留账户元数据

`InternetService.hydrate_movie_posters()` 取得刷新后的电影目录记录时，不再用纯电影记录覆盖历史条目，而是按 `{**current, **refreshed_movie}` 合并。这样电影目录字段得到更新，原历史条目中的 `state`、`note`、`rating`、`created_at`、`updated_at` 等账户数据继续保留。

`Store.movie_states()` 的查询结果补齐了 `id` 以及目录需要的 aliases、directors、regions、themes、content notes、summary、source 等字段，使同一个补图函数可安全作用于历史列表。

### 4.5 前端使用原生 `dialog`

观后感查看器使用 `<dialog>`，具备原生模态、Esc 关闭和焦点管理。电影海报使用真正的 `<button>`，而不是给 `div` 增加点击事件。关闭弹窗时恢复到原触发海报按钮；“继续聊这部电影”关闭弹窗并进入该片讨论。

### 4.6 语音胶囊进入正常文档流

语音胶囊不再绝对定位。`attachVoiceReply()` 把 `.voice-reply` 插入在 `.message-name` 后面，DOM 顺序固定为“阿映 → 语音胶囊 → 回复正文”。背景使用 `var(--wine)`，文字使用暖白色，hover/focus 使用 `var(--wine-dark)` 与主题焦点环。

## 5. 已实现功能

### 5.1 聊天输入框

- 输入区改为统一圆角外壳，不再表现为多个嵌套边框。
- 文本框本身透明且无独立边框，由容器提供焦点状态。
- 语音切换按钮与发送按钮使用同一高度和稳定的 44px 点击面积。
- 桌面端和移动端均维持原有暖白、酒红和克制阴影风格。
- 语音入口未配置时仍保持可见，并给出明确配置提示。

### 5.2 后台开场白设置

- 管理员导航新增“开场白”。
- 可编辑未选电影、已选电影和推荐模式三套文案。
- 已选电影文案支持 `{movie}`。
- 单项长度限制为 1–800 字符。
- 支持一键恢复默认文案。
- 保存后写入 SQLite，不需重启服务。

### 5.3 主题化语音回复

- 语音胶囊背景由黑色改为酒红色。
- 波形、时长、播放、暂停、重播和自动播放偏好继续保留。
- 胶囊进入文档流，不再遮挡“阿映”。
- 浏览器阻止有声自动播放时保留手动播放入口。

### 5.4 观后感笔记

- 确认具体电影后的讨论自动尝试更新该电影的账户级笔记。
- 笔记成功更新时 `/api/chat` 返回 `reflection_note` 与 `reflection_updated`。
- 该电影自动进入“看过”。
- “我的电影 → 看过”中的海报可点击。
- 有笔记的电影在海报上显示“有笔记”。
- 弹窗展示海报、年份、地区、类型和观后感正文。
- 没有笔记时显示明确空状态，不伪造内容。
- 弹窗提供“继续聊这部电影”。

### 5.5 海报稳定性

- 获取“看过”历史时，服务端最多对 5 个缺失海报条目执行惰性补图。
- 补图结果持久化到 `movies.poster_url`。
- 补图过程保留账户历史和笔记字段。
- 图片加载失败继续降级为文字封面。

## 6. 数据结构

### 6.1 现有表及意义

- `accounts`：账户主体，状态为 `active`、`suspended` 或 `deleted`。
- `invite_codes`：邀请码摘要、提示字符、状态、账户绑定和换发关系。
- `sessions`：用户会话摘要、账户和过期时间。
- `admin_sessions`：管理员会话摘要和过期时间。
- `movies`：统一电影目录，包含标题、别名、年份、主创、地区、类型、主题、心情、内容提醒、摘要、排序、来源、海报、来源地址和外部 ID。
- `user_movie_states`：账户与电影的唯一状态关系，包含 `state`、`source`、`rating`、`note` 和时间。
- `recommendation_impressions`：推荐曝光，支持后续分析和去重策略。
- `safety_events`：不含原话的安全类别事件。
- `app_settings`：模型、联网、语音、提示词与开场白等应用配置。

### 6.2 本阶段新增配置键

管理员实际保存后，`app_settings` 增加或更新：

- `discussion_opening`
- `discussion_movie_opening`
- `recommendation_opening`

未保存时这些键可以不存在，运行时由 `DEFAULT_OPENINGS` 回退。当前验收数据库仍使用默认开场白，所以这三个键未强制写入。

### 6.3 本阶段对 `note` 的语义收敛

- 字段：`user_movie_states.note TEXT NULL`。
- 所属范围：`(account_id, movie_id)`。
- 最大保存长度：2000 字符。
- 允许状态：保存观后感时最终必须是 `watched`。
- 迁移方式：无需 DDL 迁移，旧数据中的空值和已有文本继续兼容。
- 回滚风险：回滚代码不会删除笔记，但旧前端不展示它；若未来把 `note` 改作手工短评，必须先定义自动摘要和手工内容的合并或拆表规则。

## 7. 接口变化

### 7.1 `GET /api/me`

认证成功时新增：

```json
{
  "openings": {
    "discussion": "...",
    "discussion_movie": "嗯，《{movie}》。...",
    "recommendation": "..."
  }
}
```

接口仍返回账户提示、看过数量、演示模式和语音可用性。

### 7.2 `GET /api/admin/agent-config`

新增 `openings` 和 `opening_defaults`，同时保留模型、提示词、联网和语音公开配置。所有密钥继续只返回掩码信息。

### 7.3 `POST /api/admin/agent-config/openings`

需要管理员会话。请求体：

```json
{
  "discussion": "未选电影开场白",
  "discussion_movie": "正在聊《{movie}》时的开场白",
  "recommendation": "推荐电影开场白"
}
```

成功返回 `{"ok": true, "openings": {...}}`。任一字段为空或超过 800 字符返回 400。

### 7.4 `POST /api/chat`

原有聊天字段不变。讨论已确认电影时新增：

- `reflection_note`：当前保存后的观后感；没有更新时可能为空或为已有笔记。
- `reflection_updated`：本轮是否写入了新笔记。

摘要调用异常不会把主接口变成失败。

### 7.5 `GET /api/history?state=watched`

响应中的历史条目现在完整包含电影目录字段和账户状态字段。若缺少海报，联网服务最多补全 5 项，再返回带 `note` 的合并结果。

## 8. 页面变化

### 8.1 用户端 `web/index.html`、`web/app.js`、`web/styles.css`

- 资源版本更新为 `v=8`，避免旧缓存继续使用第七阶段样式和脚本。
- 聊天输入区改为统一组件。
- 语音胶囊移动到角色名后方并改为主题色。
- 新增 `#reflection-dialog`。
- 看过电影海报改为 `.history-poster-button`。
- `openReflection()` 负责填充海报、元数据、正文和空状态。
- `openChat()` 每次进入聊天都刷新开场白配置。

### 8.2 管理端 `web/admin.html`、`web/admin.js`、`web/admin.css`

- 资源版本更新为 `v=8`。
- 新增“开场白”导航与 `#opening-config-form`。
- 三个文本域分别绑定三种场景。
- 新增保存、状态提示和恢复默认交互。
- 旧语音安全说明中“默认不自动播放”的过时描述已修正。

## 9. 配置变化

本阶段没有新增 `.env` 必填项。开场白通过数据库 `app_settings` 配置。

既有主要配置继续有效：

- 模型：`MODEL_ID`、`ANTHROPIC_API_KEY`、`ANTHROPIC_BASE_URL`，或后台对应 OpenAI 兼容设置。
- TMDB：`TMDB_API_KEY`、API 根地址和图片根地址。
- Web Search：Tavily 或 Brave 的 provider、API key 和 base URL。
- 豆包语音：App Key；旧版可附加 App ID 和 Access Token；STT、TTS、音色、格式和超时均由后台保存。
- 安全：`YINGBAN_ADMIN_TOKEN`、邀请码 pepper、session secret；HTTPS 部署时启用 `YINGBAN_COOKIE_SECURE=true`。

数据库可能保存敏感第三方密钥，因此 `Store.initialize()` 继续尝试把数据库权限设为 `0600`。本文不记录真实凭据。

## 10. Agent 行为变化

### 10.1 系统提示词职责边界

- `discussion` 继续负责电影讨论、人物、主题、感受和剧透选择。
- `recommendation` 继续负责候选选择、数据层去重和结构化电影卡片。
- 三套 opening 只是初始 UI 文案，不替代系统提示词，也不参与工具调用权限。
- `summarize_reflection()` 使用独立、窄职责的摘要指令，不修改两套主 Agent 提示词。

### 10.2 运行时上下文

主对话仍注入账户已看历史、当前模式、选择电影、剧透偏好和必要工具结果。观后感摘要额外注入电影元数据、已有笔记和近期讨论，但只允许产出该电影笔记。

### 10.3 长期记忆规则

- 可以长期保存：电影状态、该电影的观后感摘要、推荐曝光和不含原话的安全类别。
- 不可以长期保存：聊天全文、原始录音、合成音频、现实烦恼、身份、家庭、工作、学校、联系方式、心理或医疗信息。
- 用户移除电影状态时，对应 `user_movie_states` 行和其笔记一起删除。
- 账户隔离由数据库复合主键和所有读写接口的 `account_id` 约束共同保证。

### 10.4 安全降级

- 演示模式不生成虚构观后感。
- 摘要失败保留已有笔记且不阻断主回复。
- TMDB 失败使用原目录和文字封面。
- TTS 失败保留文字回复。
- 浏览器阻止自动播放时保留手动播放按钮。

## 11. 安全与隐私边界

1. 新开场白接口需要管理员认证。
2. 开场白被作为纯文本渲染，不使用 `innerHTML`。
3. 观后感弹窗正文使用文本节点，不执行模型输出中的 HTML。
4. 观后感摘要明确排除现实与敏感内容；现阶段该边界依赖提示词与服务端长度限制，生产化仍需要分类器或规则评测。
5. 原始聊天、原始录音和合成音频继续不落库。
6. 海报只接受既有电影目录和 TMDB 补全结果；失败时使用本地文字封面。
7. 管理密钥不随公共配置返回。
8. 当前标准库服务器只适合本地和受控内网，公开部署必须配置 HTTPS、反向代理、CSRF、持久化限流、密钥服务和审计。

## 12. 修改文件清单

- `agent.py`
  - 新增 `DEFAULT_OPENINGS`、`OPENING_SETTING_KEYS`。
  - 新增 `openings()`、`save_openings()`、`summarize_reflection()`。
  - 公共配置返回 opening 当前值和默认值。
- `storage.py`
  - 新增 `save_movie_reflection()`。
  - 扩充 `movie_states()` 的电影目录字段。
- `server.py`
  - `/api/me` 返回开场白。
  - 新增后台开场白保存接口。
  - 聊天主回复后生成并保存观后感。
  - 看过历史执行缺失海报补全。
- `integrations.py`
  - 补图结果通过合并保留账户状态和笔记元数据。
- `web/index.html`
  - 新增观后感原生对话框，资源版本更新为 8。
- `web/app.js`
  - 动态开场白、观后感入口和弹窗、继续聊、语音 DOM 顺序调整。
- `web/styles.css`
  - 统一聊天输入框、主题化语音胶囊、可点击海报、笔记标记和观后感弹窗。
- `web/admin.html`
  - 新增开场白配置区，修正语音说明，资源版本更新为 8。
- `web/admin.js`
  - 开场白读取、保存和恢复默认。
- `web/admin.css`
  - 开场白表单与移动端布局。
- `tests/test_product.py`
  - 新增账户级观后感、开场白、主题化播放器、观后感弹窗和聊天更新笔记测试。
- `README.md`
  - 增加第八阶段入口、开场白、观后感、语音位置和 27 项测试说明。
- `第八阶段技术开发文档.md`
  - 当前阶段权威交接文档。

## 13. 测试与验证结果

### 13.1 自动化

执行：

```sh
python3 -m py_compile agent.py integrations.py storage.py server.py tests/test_product.py
node --check web/app.js
node --check web/admin.js
python3 -m unittest discover -s tests -v
```

结果：27 项测试全部通过，无失败、无错误。测试覆盖：

- 邀请码同码同账户、停用和换发。
- 账户电影历史隔离。
- 推荐硬排除已看电影和替换推荐意图继承。
- 讨论自动标记已看。
- 观后感只属于对应账户和电影。
- 讨论生成观后感并能从历史接口读取。
- 模型配置持久化和密钥掩码。
- 两套提示词独立。
- 三套开场白持久化和 `{movie}` 占位符。
- 管理员开场白接口。
- TMDB 新电影、真实海报、缺失海报补全和持久化。
- Tavily basic 搜索和域名限定。
- 豆包资源头、音频流解码和语音配置掩码。
- 麦克风入口常显。
- 主题化语音播放器和自动播放偏好。
- 观后感弹窗结构。
- 模型海报 Markdown 清理。
- `file://` 入口保护。
- AI 身份透明和管理员邀请码管理。

### 13.2 真实浏览器验收

在 `http://127.0.0.1:8765/?stage=8` 使用真实账户完成：

1. 新聊天输入框显示为单一圆角容器，语音和发送按钮对齐。
2. 讨论《星际穿越》，模型主回复成功。
3. 服务端生成并保存电影观后感，电影进入“看过”。
4. “看过”列表成功从 TMDB 加载 500×750 海报。
5. 点击海报打开《星际穿越》观后感弹窗，显示元数据与笔记正文。
6. 点击“继续聊这部电影”返回该片讨论。
7. 再发送一轮真实消息，豆包生成 51 秒语音回复。
8. DOM 顺序为 `.message-name` 后接 `.voice-reply`；语音胶囊计算样式为酒红 `rgb(143, 46, 43)`、暖白文字 `rgb(255, 248, 239)`，没有遮挡“阿映”。

验收在用户当前自动播放偏好为“手动播放”时进行，因此没有强制发声；播放器生成、时长、颜色、位置和可点击状态均已验证。

管理员开场白的认证写入通过 HTTP 自动化测试验证。未在浏览器中输入或展示管理员真实口令，避免把敏感凭据带入验收记录。

## 14. 启动与使用方法

```sh
cd products/yingban
cp .env.example .env  # 仅首次；已有 .env 不覆盖
python3 app.py generate-invites 1  # 需要新邀请码时执行
python3 app.py serve
```

用户操作：

1. 打开 `http://127.0.0.1:8765/`，输入邀请码。
2. 进入“刚刚散场”，告诉阿映电影名并讨论感受。
3. 打开“我的电影 → 看过”。
4. 点击海报查看观后感；点击“继续聊这部电影”恢复该片讨论入口。
5. 聊天页可切换自动播放偏好，也可点语音胶囊播放。

管理员操作：

1. 打开 `http://127.0.0.1:8765/admin` 并使用 `YINGBAN_ADMIN_TOKEN` 登录。
2. 进入“开场白”。
3. 编辑三套开场白；电影场景需要显示片名时使用 `{movie}`。
4. 保存后，无需重启；用户下一次进入聊天生效。

不要双击 `web/index.html`。影伴需要 HTTP 服务提供 API、认证、样式和脚本。

## 15. 尚未完成事项

1. 观后感目前完全自动生成，没有“本轮不写入”“手工编辑”“重新生成”“锁定笔记”按钮。
2. 自动摘要会在每次已确认电影的讨论回复后尝试执行；尚未做去抖、异步队列和成本预算。
3. `note` 同时承担未来可能出现的手工短评需求；若增加手工编辑，建议拆分 `reflection_note`、`manual_note` 或增加来源与版本表。
4. 没有观后感版本历史，更新会覆盖上一版摘要。
5. 没有显式的“删除笔记但保留已看状态”接口；当前删除电影状态会连同笔记删除。
6. 尚未为观后感安全边界建立独立分类器、红队样本和人工评测集。
7. 管理员不能在后台预览三套开场白在真实聊天气泡中的最终排版。
8. 看过历史一次最多补 5 张缺失海报；大规模片单需要后台队列或分页补图。

## 16. 已知风险

1. 每次有效讨论可能增加一次模型调用，带来延迟和成本。
2. 模型摘要即使有明确指令，仍可能误收与电影无关的敏感内容；公开生产前必须增加确定性过滤和评测。
3. `user_movie_states.note` 上限由服务端控制，但数据库没有长度约束；其他未来写入口必须复用相同校验。
4. `/api/history?state=watched` 的惰性补图可能在首次打开片单时增加最多 5 次 TMDB 搜索延迟。
5. SQLite 和标准库同步 HTTP 服务器不适合高并发；摘要调用会占用请求线程。
6. 浏览器自动播放受浏览器策略影响，产品只能尝试而不能保证首次有声播放。
7. TMDB 海报 URL 属于外部资源，网络或地区访问失败时只能降级文字封面。
8. 真实管理员口令和第三方密钥仍保存在本机环境或 SQLite 中，部署到共享主机前需要专用密钥管理。

## 17. 下一阶段建议

优先级建议：

1. 给观后感增加“自动整理开关”“手工编辑”“重新生成”“删除笔记但保留已看”四个明确控制，先由用户决定自动写入是否应该默认开启。
2. 将摘要生成放入异步任务，聊天主回复先返回；前端以轻量状态提示笔记稍后更新。
3. 为观后感增加来源、版本、更新时间和用户确认状态；避免模型覆盖用户手工内容。
4. 建立观后感隐私评测集，覆盖现实烦恼、身份、家庭、工作、学校、联系方式、心理、医疗等禁止内容。
5. 给历史接口分页，并将缺失海报补全迁移到后台队列。
6. 补充 CSRF、持久化限流、结构化审计、密钥服务、数据库迁移框架和 HTTPS 部署。
7. 增加完整浏览器端到端测试，覆盖管理员修改开场白后用户下一次进入聊天立即生效。

## 18. AI 接手入口

下一次迭代必须按以下顺序进行：

1. 读取仓库根目录 `AGENTS.md`。
2. 读取本文件，不要只读早期阶段文档。
3. 运行 27 项测试并记录实际数量；测试数量变化时以当前代码为准。
4. 查询 `app_settings` 的键名但不要输出真实密钥值。
5. 检查 `user_movie_states.note` 的真实数据和账户隔离。
6. 用真实浏览器验证“看过海报 → 观后感弹窗 → 继续聊”闭环。
7. 若修改摘要逻辑，必须同时验证隐私排除、主聊天失败隔离、演示模式降级和 2000 字符上限。
8. 若修改海报补全，必须验证 `note`、`state`、`rating`、`source` 和时间字段不会在目录刷新后丢失。
9. 完成新的边界明确开发后，新建 `第九阶段技术开发文档.md`，不得覆盖本文。
