# 第一阶段技术开发文档

> AI 读取定位：这是历史基线文档，记录第一阶段为什么这样构建以及基础业务定义。当前实现状态请优先读取 `第二阶段技术开发文档.md`；只有追溯邀请制、长期记忆、推荐去重和第一版 Agent 架构时才需要完整读取本文件。

## 0. AI 快速接手卡

```text
DOCUMENT_ROLE=historical_baseline
STAGE=1
STATUS=completed_and_superseded_by_stage_2
CODE_ROOT=products/yingban
START_ENTRY=products/yingban/app.py
SERVER_ENTRY=products/yingban/server.py
AGENT_ENTRY=products/yingban/agent.py
STORE_ENTRY=products/yingban/storage.py
TEST_ENTRY=products/yingban/tests/test_product.py
LATEST_DOCUMENT=products/yingban/第二阶段技术开发文档.md
```

第一阶段建立的不可破坏基线是：一个邀请码长期绑定一个账户；讨论确认电影后自动记为看过；推荐必须在数据层硬排除看过电影；现实烦恼不自动进入长期记忆；角色不能冒充真人；高风险内容由产品代码短路。第二阶段及后续阶段必须继续保持这些约束。

## 1. 文档信息

- 产品名称：影伴
- 阶段名称：第一阶段——邀请制 AI 电影伙伴可运行骨架
- 开发状态：已完成
- 阶段完成日期：2026-08-20
- 对应蓝图阶段：`BLUEPRINT.md` Step 5 的第一层“骨架”，并包含部分硬化能力
- 下一阶段：第二阶段——管理员模型配置与双 Agent 提示词管理

本文件是对第一阶段已完成工作的追溯记录。第一阶段开发完成时尚未建立阶段文档制度，因此本文件在第二阶段结束时补写。补写目的不是替代第二阶段文档，而是保留完整开发历史和产品能力来源。

## 2. 需求背景

产品面向学生和电影爱好者，覆盖两个核心场景：

1. 用户刚看完一部电影，希望和一个有观点、能理解感受的对象继续讨论电影。
2. 用户不知道看什么电影，希望得到推荐；推荐既可以基于电影类型和口味，也可以基于用户现实生活中的烦恼、压力或情绪，让电影提供放松、陪伴、共鸣或新的观察角度。

产品范围同时包含以下确定要求：

- 产品形态为 Web 端。
- 目标电影范围是中国和国外广受欢迎的电影。
- 产品采用邀请码制，一个邀请码对应一个账户。
- 邀请码由管理员后台批量生成，再通过线下或私下方式发放。
- 用户使用邀请码登录，同一个邀请码长期进入同一个账户。
- 用户确认看过的电影自动加入长期观影记录，不需要再次征求用户同意。
- 推荐时不得重复推荐用户已经看过的电影。
- 系统提示词以角色扮演式交流为主，让用户感觉在和一个自然的电影搭子交流，同时不能虚构真人经历，也不能隐瞒 AI 身份。

## 3. 阶段目标

第一阶段的目标是完成一个可在本机真实运行的 P0 产品闭环：

```text
管理员生成邀请码
→ 用户用邀请码登录并绑定账户
→ 用户选择“聊电影”或“找电影”
→ Agent 返回回应或推荐结果
→ 已确认电影自动进入长期观影记录
→ 后续推荐在数据层排除已看电影
```

“可运行”的定义包括：用户页面、管理员页面、HTTP API、SQLite 数据库、邀请登录、电影种子目录、Agent 运行时、长期观影状态和自动化测试能够共同工作，而不是只有静态页面或提示词示例。

## 4. 第一阶段架构

第一阶段依据 `BLUEPRINT.md` 的七层 Agent 产品架构实现：

1. 交互层：浏览器中的用户 Web 页面和管理员 Web 页面，同步请求与响应。
2. 产品层：邀请码登录、会话 Cookie、双入口模式、电影选择、历史列表、管理员邀请码管理。
3. Agent 编排层：单 Agent Loop、五个电影领域工具、最多八轮工具调用、对话历史裁剪、产品层预检索和安全短路。
4. 模型层：第一阶段使用 Anthropic Messages API；没有 API 密钥时自动进入演示 Agent 模式。
5. 能力集成层：电影搜索、影片详情、观影历史读取、看过状态写入、去重推荐五项内部工具。
6. 数据层：SQLite 保存账户、邀请码、会话、电影目录、用户电影状态、推荐曝光和安全事件。
7. 基础设施层：Python 标准库单进程 HTTP 服务，本机 SQLite，静态 HTML、CSS 和 JavaScript，无额外运行依赖。

安全边界和可观测性作为横切关注点：管理员鉴权、用户鉴权、邀请码摘要、会话摘要、登录限流、高风险文本短路、日志和测试贯穿产品层到基础设施层。

## 5. 已实现功能

### 5.1 邀请码与账户

- 管理员可以批量生成 1 到 100 个邀请码。
- 完整邀请码只在生成或换发当次展示。
- 数据库不保存完整邀请码，只保存使用服务端 pepper 生成的 HMAC-SHA256 摘要和末尾五位识别字符。
- 邀请码第一次使用时自动创建账户，并把邀请码与账户绑定。
- 同一邀请码再次登录时进入原账户，不创建新账户。
- 管理员可以停用邀请码；停用会使邀请码和该账户现有登录会话失效。
- 管理员可以为已经激活的账户换发邀请码；换发保留原账户和观影历史，旧邀请码及旧会话失效。
- 用户登录成功后使用 HttpOnly、SameSite=Lax 的 Cookie 保存会话。
- 管理员登录成功后使用独立的 HttpOnly 管理 Cookie，会话默认有效八小时。

### 5.2 用户双入口

- 首页明确提供“聊电影”和“找电影”两个入口。
- “聊电影”允许用户输入电影名和观后感，并可控制是否允许剧透。
- “找电影”允许用户描述口味、场景、情绪或现实烦恼。
- 两个入口共享阿映的角色身份和观影长期记忆，但业务处理流程不同。

### 5.3 电影识别与讨论

- 产品可以从用户文本中识别种子片库内的中文名、原名和别名。
- 如果文本只匹配一部电影，产品自动确认当前讨论电影。
- 如果文本匹配多部电影，产品返回候选，让用户选择具体版本。
- 用户在讨论模式中确认电影后，产品层自动把电影写入“看过”状态。
- 自动写入会返回 `memory_event`，前端向用户展示已经加入看过列表的反馈。
- 剧透状态通过运行时上下文传给 Agent。

### 5.4 推荐与硬性去重

- 推荐目录来自 `data/movies.json` 中的中外热门种子电影。
- 推荐根据用户请求中的类型、主题、情绪和表达进行匹配。
- 推荐逻辑在数据检索层硬性排除用户已经标记为 `watched` 的电影，而不是只依赖模型遵守提示词。
- 用户说“看过了”“换一部”或“不喜欢”时，产品会结合此前请求保留推荐意图，再生成新的去重候选。
- 推荐结果返回影片卡片，包含片名、年份、地区、类型、匹配理由和内容提醒。
- 推荐曝光记录写入数据库，为后续降低重复曝光和分析推荐效果保留数据。

### 5.5 长期观影记忆

- 一个账户可以保存 `watched`、`watchlist` 和 `disliked` 三种电影状态。
- 状态记录包含来源、可选评分、可选备注、创建时间和更新时间。
- 用户可以查看自己的看过、想看和不感兴趣列表。
- 用户可以手动添加或删除电影状态。
- 不同账户的电影状态完全隔离。
- 当前聊天正文不写入长期数据库；页面刷新后当前对话消失，但观影记录继续保留。
- 现实烦恼、心理状态和敏感经历不会被自动保存为长期记忆。

### 5.6 Agent 角色与工具

第一阶段的默认角色名为“阿映”。角色的核心边界包括：

- 交流自然、口语化，有稳定审美观点，但不卖弄知识。
- 先回应用户具体表达，再继续讨论或推荐。
- 不虚构童年、朋友、恋爱、工作、观影经历等真人经历。
- 用户明确询问身份时，如实说明自己是 AI 电影伙伴，不是真人。
- 电影事实应来自电影工具结果；资料不足时承认不确定。
- 不诊断心理问题，不宣称电影有治疗作用，也不承诺电影能够解决现实问题。

第一阶段提供五个领域工具：

1. `search_movie`：按中文名、原名或别名搜索真实影片。
2. `get_movie_detail`：读取影片事实资料、主题、情绪和内容提醒。
3. `list_watched_movies`：读取当前账户的看过列表。
4. `mark_movie_watched`：把已经确认的电影写入当前账户看过列表。
5. `recommend_movies`：根据用户请求从热门片库中推荐，并硬性排除看过和近期拒绝的电影。

### 5.7 演示模式与真实模型模式

- 没有配置 `ANTHROPIC_API_KEY` 时，系统进入演示 Agent 模式。
- 演示模式不是静态空页面：邀请登录、电影识别、自动记忆、推荐检索、去重、历史列表和管理员功能均真实运行。
- 配置模型密钥后，系统使用 Anthropic Messages API 进入真实工具调用循环。
- 模型循环最多运行八个工具轮次。
- 对话历史最多保留最近十六条有效用户或助手消息，每条内容最多四千字符。

### 5.8 安全短路

- 用户询问“你是真人吗”等身份问题时，产品直接返回透明身份说明，避免角色提示词造成误导。
- 检测到可能涉及立即自伤或伤害他人的高风险短语时，产品不继续推荐电影，而是返回立即寻求现实帮助的安全回应。
- 高风险事件只记录账户、事件分类和时间，不保存原始聊天正文。
- 登录接口有基于来源地址的进程内失败次数限制。
- JSON 请求体限制为最大 1 MB，聊天消息限制为最多 6000 个字符。

## 6. 第一阶段数据结构

SQLite 默认数据库路径为 `data/yingban.db`。第一阶段包含以下表：

### 6.1 `accounts`

保存账户 ID、账户状态和创建时间。账户状态限定为 `active`、`suspended` 或 `deleted`。

### 6.2 `invite_codes`

保存邀请码 ID、邀请码摘要、末尾识别字符、状态、绑定账户、创建时间、激活时间、停用时间和换发关系。状态限定为 `issued`、`active` 或 `revoked`。

### 6.3 `sessions`

保存用户会话 ID、令牌摘要、账户 ID、创建时间和过期时间。数据库不保存完整用户会话令牌。

### 6.4 `admin_sessions`

保存管理员会话 ID、令牌摘要、创建时间和过期时间。数据库不保存完整管理员会话令牌。

### 6.5 `movies`

保存电影统一 ID、中文名、原名、别名、年份、导演、地区、类型、主题、情绪、内容提醒、摘要、热度排序、数据来源和更新时间。数组类型字段以 JSON 文本保存。

### 6.6 `user_movie_states`

以账户 ID 和电影 ID 组成联合主键，保存 `watched`、`watchlist` 或 `disliked` 状态、数据来源、可选评分、可选备注和时间信息。

### 6.7 `recommendation_impressions`

保存推荐曝光 ID、账户 ID、电影 ID、请求摘要和创建时间。

### 6.8 `safety_events`

保存安全事件 ID、账户 ID、事件分类和创建时间，不保存原始聊天正文。

## 7. 第一阶段主要接口

### 7.1 公共和用户接口

- `GET /api/health`：服务健康状态和当前演示/模型模式。
- `GET /api/me`：当前用户登录状态、账户末尾识别字符和看过数量。
- `POST /api/auth/login`：使用邀请码登录或首次激活账户。
- `POST /api/auth/logout`：退出用户会话。
- `GET /api/movies`：搜索电影。
- `GET /api/history`：按状态读取当前账户电影列表。
- `POST /api/history`：写入电影状态。
- `DELETE /api/history/{movie_id}`：删除电影状态。
- `POST /api/chat`：处理聊电影或找电影请求。

### 7.2 管理员接口

- `POST /api/admin/login`：管理员口令登录。
- `POST /api/admin/logout`：退出管理员会话。
- `GET /api/admin/invites`：读取邀请码状态列表，不返回完整邀请码。
- `POST /api/admin/invites/generate`：批量生成邀请码。
- `POST /api/admin/invites/{invite_id}/revoke`：停用邀请码。
- `POST /api/admin/invites/{invite_id}/rotate`：为原账户换发邀请码。

## 8. 页面交付

### 8.1 用户端

- 邀请码登录页。
- 双入口首页。
- 聊电影对话页和剧透开关。
- 找电影对话页和推荐卡片。
- 电影搜索与手动添加状态弹窗。
- 看过、想看和不感兴趣历史页面。
- 演示模式标识、记忆写入提示和错误提示。

### 8.2 管理端

- 管理员口令登录页。
- 邀请码批量生成表单。
- 新邀请码一次性展示和复制。
- 邀请码状态列表。
- 激活邀请码的换发和停用操作。

整体视觉采用米色纸张、酒红色强调色、衬线标题和电影杂志式排版，并包含桌面端和移动端响应式规则。

## 9. 第一阶段配置

配置文件为本地 `.env`，示例来自 `.env.example`。核心变量包括：

- `YINGBAN_HOST`：监听地址。
- `YINGBAN_PORT`：监听端口。
- `YINGBAN_COOKIE_SECURE`：是否只通过 HTTPS 发送 Cookie。
- `YINGBAN_SESSION_DAYS`：用户会话有效天数。
- `YINGBAN_INVITE_PEPPER`：邀请码摘要使用的服务端 pepper。
- `YINGBAN_SESSION_SECRET`：用户和管理员会话令牌摘要使用的服务端密钥。
- `YINGBAN_ADMIN_TOKEN`：管理员网页登录口令。
- `ANTHROPIC_API_KEY`：Anthropic 模型 API 密钥。
- `MODEL_ID`：模型名称。
- `ANTHROPIC_BASE_URL`：Anthropic 接口根地址。
- `MODEL_TIMEOUT_SECONDS`：模型请求超时时间。

`.env`、SQLite 数据库和运行时数据库 WAL 文件均不应进入版本控制。

## 10. 第一阶段文件清单

- `app.py`：命令行入口、上下文构建和服务启动。
- `server.py`：HTTP 路由、鉴权、聊天预处理、安全短路和静态资源服务。
- `agent.py`：阿映角色提示词、领域工具、Anthropic 客户端、Agent Loop 和演示回应。
- `catalog.py`：电影种子加载、标题识别、搜索、匹配和推荐去重。
- `storage.py`：SQLite 初始化、邀请码、账户、会话、电影状态和安全事件。
- `settings.py`：`.env` 加载和运行配置。
- `data/movies.json`：中外热门电影种子目录。
- `web/index.html`：用户页面结构。
- `web/app.js`：用户页面状态、登录、聊天、推荐卡片和历史交互。
- `web/styles.css`：用户端和共享视觉样式。
- `web/admin.html`：第一阶段管理员页面结构。
- `web/admin.js`：邀请码管理交互。
- `web/admin.css`：管理员页面样式。
- `tests/test_product.py`：第一阶段核心闭环测试。
- `.env.example`：环境配置模板。
- `.gitignore`：本地密钥、数据库和缓存忽略规则。
- `README.md`：启动、功能、数据和生产化说明。

## 11. 测试与验证

第一阶段结束时共有 10 项自动测试，覆盖：

- 同一邀请码长期进入同一账户。
- 停用邀请码后无法继续登录。
- 换发邀请码保留账户并使旧会话失效。
- 推荐结果硬性排除看过电影。
- 不同账户的观影历史隔离。
- 讨论已识别电影时自动写入看过列表。
- 推荐接口不返回已看电影。
- 用户要求换片时保留此前推荐意图。
- 用户询问身份时明确说明 AI 身份和非真人身份。
- 管理员可以批量生成邀请码。

第一阶段运行检查确认用户端和管理员后台能够在 `127.0.0.1:8765` 正常打开。

## 12. 启动与使用

```sh
cd products/yingban
cp .env.example .env
python3 app.py generate-invites 1
python3 app.py serve
```

默认地址：

- 用户端：`http://127.0.0.1:8765`
- 管理端：`http://127.0.0.1:8765/admin`

完整管理员口令、邀请码和模型密钥不得写入阶段技术文档。

## 13. 第一阶段尚未完成事项与风险

- 模型配置只能通过 `.env` 修改，修改后需要重启。
- 只有 Anthropic Messages 协议，不能直接连接 OpenAI 兼容服务。
- “聊电影”和“找电影”共享一套完整系统提示词，不能独立调整角色行为。
- 管理页面只有邀请码管理，没有模型连接测试和提示词管理。
- 电影目录是有限的种子数据，不是生产级实时电影数据库。
- 标准库 HTTP 服务适合本地骨架验证，不适合直接公开部署。
- 登录限流只在单个进程内保存，服务重启后清空。
- 尚未实现 CSRF Token、持久化审计日志、专业密钥服务、模型重试和成本指标。
- 高风险文本检测是固定短语规则，不能替代专业安全分类器和安全评测。
- 当前聊天历史只在页面内，刷新后消失；这是第一阶段明确的数据最小化选择，不是长期会话功能。

## 14. 第二阶段接手入口

第二阶段应从以下位置继续：

1. 在 `storage.py` 增加管理员可修改的应用配置持久化结构。
2. 在 `agent.py` 把固定模型设置改为每轮动态加载。
3. 增加 Anthropic 和 OpenAI 兼容协议的统一模型客户端。
4. 把共享角色提示词拆为“聊电影 Agent”和“找电影 Agent”两套完整提示词。
5. 在 `server.py` 增加管理员配置读取、保存和连接测试接口。
6. 扩展 `web/admin.*`，增加大模型配置和双提示词编辑界面。
7. 保证旧数据库自动升级，已有账户、邀请码和观影历史不丢失。
8. 增加模型配置持久化、密钥掩码和提示词独立性的自动测试。
