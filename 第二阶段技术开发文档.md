# 第二阶段技术开发文档

> AI 读取定位：这是当前最新的版本迭代交接文档。新一轮开发先读本节和第 17 节，再根据任务读取相关完整章节和代码。只有需要追溯第一阶段设计来源时，才读取 `第一阶段技术开发文档.md`。

## 0. AI 快速接手卡

```text
DOCUMENT_ROLE=current_authoritative_handoff
CURRENT_STAGE=2
STATUS=completed
CODE_ROOT=products/yingban
PRODUCT_START=products/yingban/app.py
HTTP_ROUTING=products/yingban/server.py
AGENT_RUNTIME=products/yingban/agent.py
PERSISTENCE=products/yingban/storage.py
MOVIE_RETRIEVAL=products/yingban/catalog.py
USER_UI=products/yingban/web/index.html + app.js + styles.css
ADMIN_UI=products/yingban/web/admin.html + admin.js + admin.css
TESTS=products/yingban/tests/test_product.py
START_COMMAND=cd products/yingban && python3 app.py serve
TEST_COMMAND=cd products/yingban && python3 -m unittest discover -s tests -v
EXPECTED_TEST_BASELINE=13 tests passing
DOCUMENTED_MODE_AT_STAGE_END=demo
NEXT_DOCUMENT=products/yingban/第三阶段技术开发文档.md
```

### 0.1 当前实现一句话

影伴是一个 Python 标准库单进程 Web 产品：用户使用一对一邀请码进入长期账户，在“聊电影”和“找电影”之间选择；SQLite 保存观影状态并在推荐检索层硬排除看过电影；管理员可以在 Web 配置 Anthropic 或 OpenAI 兼容模型，并独立管理两套 Agent 系统提示词。

### 0.2 开始新迭代前必须验证

1. 读取根目录 `AGENTS.md`。
2. 检查 `products/yingban/` 下是否已经存在晚于第二阶段的文档；如果存在，应改读阶段号最大的文档。
3. 运行 13 项测试基线。测试数量可以在后续增加，但已有测试不得无理由删除。
4. 访问 `/api/health` 验证真实运行模式。本文记录的 `demo` 只是第二阶段结束时的快照，用户可能之后在管理后台保存模型密钥。
5. 检查工作区已有修改，不能覆盖用户或上一次迭代尚未交付的改动。

### 0.3 不可破坏的产品约束

- 一个邀请码只对应一个长期账户；同一码重新登录必须回到原账户。
- 邀请码完整正文不得写入数据库、日志或阶段文档。
- 用户在讨论中确认看过电影后自动写入 `watched`，不再次征求同意。
- 推荐结果必须由数据检索层排除 `watched`，不能只依赖模型提示词去重。
- “聊电影”和“找电影”使用独立完整提示词，但共享同一套硬性身份、安全、记忆和推荐边界。
- Agent 不得冒充真人或虚构真人经历；用户明确询问身份时必须透明说明。
- 现实烦恼、心理状态和敏感经历不得自动进入长期记忆。
- 高风险自伤或伤人文本必须在调用普通电影 Agent 前由产品代码短路。
- 管理配置读取接口不得返回完整模型 API 密钥。
- 旧账户、邀请码绑定和观影历史在数据库升级时不得丢失。

### 0.4 当前已知阻塞与最可能的下一步

当前没有由开发流程提供的真实模型密钥，因此真实供应商连接、实际工具调用和输出质量尚未完成验收。下一次迭代如果目标是接入模型，第一步应先确认供应商、接口协议、模型 ID、接口根地址和密钥来源，然后通过管理后台“测试连接”，再分别验收聊电影与找电影。

### 0.5 信息源优先级

遇到不一致时按以下优先级判断，并把差异写入下一阶段文档：

1. 当前真实代码、数据库 schema 和运行状态。
2. 阶段号最大的技术开发文档。
3. 更早阶段技术开发文档。
4. `README.md` 中的使用说明。
5. 不能定位到实现的历史描述或口头假设。

## 1. 文档信息

- 产品名称：影伴
- 阶段名称：第二阶段——管理员大模型配置与双 Agent 提示词管理
- 开发状态：已完成并已在本机重新启动
- 阶段完成日期：2026-08-20
- 继承阶段：第一阶段——邀请制 AI 电影伙伴可运行骨架
- 当前运行地址：`http://127.0.0.1:8765`
- 当前管理地址：`http://127.0.0.1:8765/admin`
- 当前运行模式：演示 Agent 模式，尚未在管理页面保存真实模型 API 密钥

## 2. 本阶段需求

本阶段收到的明确需求是：

1. 在管理界面增加大模型配置功能。
2. 在管理界面增加系统提示词管理功能。
3. “聊电影”和“找电影”配置独立的 Agent 系统提示词。
4. 配置完成后应能够实际驱动用户端 Agent，而不是只把配置显示在页面上。

本阶段同时延续第一阶段的所有业务要求：邀请码登录、同码同账户、长期观影历史、讨论自动记忆、推荐硬去重、角色身份透明、现实烦恼不自动进入长期记忆以及高风险内容安全短路均不能被破坏。

## 3. 阶段目标与完成定义

第二阶段完成后的目标闭环是：

```text
管理员登录
→ 在页面选择模型接口协议
→ 填写模型、地址、密钥和生成参数
→ 保存前测试连接
→ 保存配置并立即生效
→ 分别编辑“聊电影 Agent”和“找电影 Agent”系统提示词
→ 用户进入对应模式
→ Agent 每轮动态读取对应模型配置和对应提示词
```

完成定义包括：

- 配置持久化到本机数据库。
- 不需要重启服务即可生效。
- 管理接口不向浏览器返回完整 API 密钥。
- 两套系统提示词数据上独立、运行时选择独立、页面编辑独立。
- 没有 API 密钥时保留原演示模式。
- 旧数据库可以自动增加新结构，旧账户、邀请码和观影历史保持可用。
- 原有测试继续通过，并增加本阶段测试。

## 4. 关键架构决策

### 4.1 配置保存在 SQLite，而不是修改 `.env`

`.env` 继续作为首次启动和命令行环境的默认配置来源。管理员第一次通过页面保存模型配置后，数据库中的运行时配置优先于 `.env`。

这样设计的意义是：

- 管理员可以在 Web 页面修改配置。
- 保存后下一轮对话立即读取新配置，无需重启 Python 服务。
- 保留 `.env` 作为部署初始值和故障恢复入口。
- 不需要让 Web 进程重写 `.env` 文件，降低文件格式损坏和并发写入风险。

### 4.2 使用通用键值表 `app_settings`

第二阶段新增 `app_settings` 表：

```sql
CREATE TABLE IF NOT EXISTS app_settings (
    key TEXT PRIMARY KEY,
    value TEXT NOT NULL,
    updated_at TEXT NOT NULL
);
```

使用通用键值表的意义是：模型参数和提示词都可以原子更新，后续新增 Agent 设置时不必频繁修改单例配置表字段。当前表没有直接对用户开放，只通过管理员鉴权接口和运行时代码访问。

### 4.3 每轮动态加载配置

`AgentRuntime` 不再在服务启动时永久固定模型客户端。每次用户发起真实模型对话时，运行时执行以下步骤：

1. 从 `app_settings` 读取已经保存的模型配置。
2. 对不存在的键回退到 `Settings` 中的 `.env` 默认值。
3. 验证接口协议、URL、模型名称、超时、输出长度和温度。
4. 判断是否存在 API 密钥；不存在则继续使用演示 Agent。
5. 根据当前模式选择独立系统提示词。
6. 创建当前轮次使用的模型客户端并运行工具循环。

这种方式保证管理员保存后无需重启即可生效，也避免旧客户端继续持有已经过期的配置。

### 4.4 统一 Anthropic 与 OpenAI 兼容接口

新增 `ModelMessageClient`，对 Agent Loop 暴露统一的内容块格式，内部支持：

- `anthropic`：Anthropic Messages API，地址规则为接口根地址加 `/v1/messages`；如果根地址已经以 `/v1` 结尾，则直接加 `/messages`。
- `openai_compatible`：OpenAI Chat Completions 兼容 API，地址规则为接口根地址加 `/v1/chat/completions`；如果根地址已经以 `/v1` 结尾，则直接加 `/chat/completions`。

OpenAI 兼容模式会完成以下转换：

- 把系统提示词转换为 `role=system` 消息。
- 把领域工具的 `input_schema` 转换为 OpenAI function tool 的 `parameters`。
- 把内部 `tool_use` 内容块转换为 OpenAI `tool_calls`。
- 把内部 `tool_result` 内容块转换为 OpenAI `role=tool` 消息。
- 把 OpenAI 返回的文本和工具调用重新归一化为原 Agent Loop 使用的内容块格式。

因此原有五个领域工具和最多八轮工具循环不需要为两种接口分别维护两份业务逻辑。

### 4.5 两套完整系统提示词

原来的共享 `PERSONA_PROMPT` 被拆为：

- `DISCUSSION_AGENT_PROMPT`：完整的“聊电影 Agent”默认系统提示词。
- `RECOMMENDATION_AGENT_PROMPT`：完整的“找电影 Agent”默认系统提示词。

两套提示词不是一段公共提示词加少量模式后缀，而是可以在管理页面分别编辑和保存的完整角色指令。这样做的意义是管理员可以分别调整讨论深度、追问方式、推荐策略、表达风格和工具约束，不会因为修改推荐规则而意外改变讨论行为。

产品运行时仍然自动追加不可由管理员页面直接拼装的当前请求上下文，包括：

- 当前模式名称。
- 当前剧透状态。
- 已经确认的电影事实数据。
- 产品层是否已经自动写入看过状态。
- 推荐检索层已经去重的真实候选片单。
- 当前轮次是否已经完成推荐工具调用，避免重复检索或补造候选。

独立提示词管理不会取消产品层的硬性安全和数据边界。身份询问短路、高风险文本短路、看过状态自动写入和推荐硬去重仍由代码执行。

## 5. 模型配置定义

本阶段新增不可变数据类 `ModelConfig`。字段及意义如下：

### 5.1 `provider`

接口协议。允许值：

- `anthropic`
- `openai_compatible`

### 5.2 `api_key`

模型接口密钥。空字符串表示演示模式。密钥最大允许 10000 个字符，以阻止异常超长输入。

### 5.3 `model_id`

模型名称或模型 ID，不能为空，最大 200 个字符。

### 5.4 `base_url`

模型接口根地址。必须是有效的 `http` 或 `https` 地址，必须包含网络位置，不能在 URL 中嵌入用户名。

允许 `http` 的原因是保留连接本机或内网模型服务的能力；公开部署时应优先使用 `https`。

### 5.5 `timeout_seconds`

单次模型请求超时，允许范围为 5 到 300 秒。默认从 `.env` 的 `MODEL_TIMEOUT_SECONDS` 读取，当前默认 60 秒。

### 5.6 `max_tokens`

模型最大输出长度，允许范围为 128 到 8192，当前默认 1200。

### 5.7 `temperature`

模型表达温度，允许范围为 0 到 2，当前默认 0.7。

### 5.8 `demo_mode`

这是一个派生属性，不单独存储。当 `api_key` 为空时为 `True`，表示系统使用内置演示 Agent；有密钥时为 `False`，表示调用真实模型。

## 6. 数据库变化

### 6.1 新增表

新增 `app_settings` 表，定义见第 4.2 节。

### 6.2 当前模型配置键

- `model_provider`
- `model_api_key`
- `model_id`
- `model_base_url`
- `model_timeout_seconds`
- `model_max_tokens`
- `model_temperature`

### 6.3 当前提示词配置键

- `discussion_prompt`
- `recommendation_prompt`

### 6.4 迁移方式

应用启动时 `Store.initialize()` 使用 `CREATE TABLE IF NOT EXISTS` 自动创建 `app_settings`。没有删除或修改第一阶段任何旧表和字段。

### 6.5 旧数据兼容性

- 已有 `accounts` 保留。
- 已有 `invite_codes` 及其账户绑定保留。
- 已有用户和管理员会话保留。
- 已有 `movies` 数据保留。
- 已有 `user_movie_states` 观影历史保留。
- 已有推荐曝光和安全事件保留。
- 如果 `app_settings` 尚无某项配置，运行时自动回退到代码默认值或 `.env` 默认值。

### 6.6 回滚风险

旧版本代码会忽略新表，因此代码回滚不会破坏旧业务数据。但旧版本无法使用管理员页面保存的模型配置和双提示词，会重新使用 `.env` 与第一阶段共享提示词。回滚前应保留数据库备份，避免后续再次升级时丢失管理员已经调整的提示词内容。

## 7. 新增与变化的管理员接口

所有本节接口都要求有效管理员会话。

### 7.1 `GET /api/admin/agent-config`

返回当前模型公开配置、两套当前提示词和两套默认提示词。

模型公开配置只包含：协议、模型名称、接口地址、超时、最大输出长度、温度、是否存在密钥、密钥末四位掩码和当前运行模式。响应不包含完整 API 密钥。

### 7.2 `POST /api/admin/agent-config/model`

保存模型配置。请求支持：

- `provider`
- `api_key`
- `clear_api_key`
- `model_id`
- `base_url`
- `timeout_seconds`
- `max_tokens`
- `temperature`

如果 `api_key` 留空且 `clear_api_key` 为 `false`，保留原密钥。如果 `clear_api_key` 为 `true`，清除原密钥并切回演示模式。

保存成功后立即影响下一轮模型请求。

### 7.3 `POST /api/admin/agent-config/prompts`

同时保存 `discussion` 和 `recommendation` 两套完整提示词。每套提示词去除首尾空白后必须在 20 到 30000 个字符之间。

两套提示词一起保存的意义是防止页面只提交一套时意外把另一套覆盖为空，同时让管理员明确确认当前完整 Agent 行为配置。

### 7.4 `POST /api/admin/agent-config/test`

使用页面当前填写的配置执行一次不持久化的连通性测试。测试系统提示词要求模型只完成连通性回应，请求不携带电影数据、用户历史或真实用户消息。

测试成功返回模型文本片段和耗时毫秒数。测试失败返回可读错误；服务器日志只记录错误类型，不记录 API 密钥。

### 7.5 既有接口变化

- `GET /api/health` 现在根据运行时数据库配置判断 `demo` 或 `model`，不再只检查启动时 `.env`。
- `GET /api/me` 中的 `demo_mode` 现在根据运行时数据库配置判断。
- 服务启动日志会读取当前运行时模型配置并显示模式或模型名称。

## 8. 管理页面变化

管理员后台从“单一邀请码页面”升级为“配置中心”，包含三个主要区域：

### 8.1 配置中心导航

- 页面顶部保留“影伴管理台”品牌和退出管理按钮。
- 新增“模型与 Agent”“邀请码”两个页内导航入口。
- 页面保存后立即生效的说明在主标题下明确展示。

### 8.2 大模型配置区

- 接口协议下拉选择。
- 模型名称输入框。
- 接口根地址输入框。
- API 密钥密码输入框。
- 当前密钥末四位掩码提示。
- 清除密钥并切回演示模式复选框。
- 请求超时输入框。
- 最大输出长度输入框。
- 表达温度输入框。
- “测试连接”按钮。
- “保存模型配置”按钮。
- 当前“演示模式”或“已接入模型”状态徽标。
- 测试和保存结果的可访问状态区域。
- API 密钥只保存在本机且不由管理接口完整返回的安全说明。

### 8.3 双 Agent 提示词区

- “聊电影 Agent”完整提示词编辑器。
- “找电影 Agent”完整提示词编辑器。
- 两个编辑器分别显示字符数。
- 两个编辑器分别提供“恢复默认”操作。
- 恢复默认只修改当前页面内容，必须再次点击保存才会生效。
- 两套提示词通过同一个明确的保存按钮提交。

### 8.4 邀请码区

第一阶段邀请码生成、一次性显示、复制、状态列表、换发和停用功能全部保留，移动到配置中心下方，没有改变数据和操作语义。

### 8.5 视觉与响应式

- 延续米色纸张、酒红强调、衬线大标题和电影杂志式视觉语言。
- 管理顶栏改为半透明粘性顶栏。
- 配置表单在桌面端使用六列网格组织宽字段、普通字段和三个紧凑参数。
- 两套提示词在桌面端并排，在窄屏下改为单列。
- 移动端表单按钮改为全宽，配置状态和导航适应小屏。
- 所有表单字段都有可关联标签，状态结果使用 `role=status` 和 `aria-live`。

## 9. 系统提示词职责边界

### 9.1 聊电影 Agent

职责包括：

- 接住用户的具体观后情绪、判断或困惑。
- 提供值得继续讨论的角度。
- 不把单一解读当作唯一答案。
- 遵守剧透设置。
- 电影事实来自工具结果。
- 用户确认刚看完电影时配合写入长期观影记录。
- 一次通常只追问一个最值得继续的问题。

### 9.2 找电影 Agent

职责包括：

- 理解用户的电影口味、观影场景、当前情绪或现实烦恼。
- 只从产品检索层提供的真实候选中推荐。
- 不补造片名、剧情、主创、奖项或其他事实。
- 说明为什么适合当下、可能不适合之处和必要内容提醒。
- 条件明确时给出首选，帮助用户缩小范围。
- 用户反馈看过或不喜欢时，配合更新状态并换候选。

### 9.3 两套提示词共同继承的边界

- 必须如实说明 AI 身份，不得冒充真人。
- 不得虚构真人经历。
- 不自动长期保存现实烦恼、心理状态或敏感经历。
- 不进行心理诊断。
- 不宣称电影具有治疗效果。
- 用户不愿继续讲烦恼时停止追问。
- 当前用户表达优先于从历史行为推断的偏好。

## 10. 安全与隐私

### 10.1 密钥返回边界

完整 API 密钥不会通过配置读取接口返回。页面只获得：

- `has_api_key`
- `api_key_hint`，格式为四个圆点加密钥末四位

管理员留空密钥输入框时保留原密钥，避免页面必须先读取完整密钥再提交。

### 10.2 密钥本地存储边界

当前阶段 API 密钥以本机 SQLite 文本值保存。`Store.initialize()` 会尝试把数据库文件权限设置为 `0600`，即只允许当前系统用户读写。

这不是生产级密钥管理。公开部署时应迁移到操作系统密钥链、云密钥管理服务或专用 Secrets Manager，并增加密钥轮换和访问审计。

### 10.3 管理鉴权

所有配置读取、保存和测试接口都要求有效管理员会话。用户邀请码会话不能访问这些接口。

### 10.4 模型连接测试数据边界

连通性测试只发送固定测试提示，不发送用户聊天、观影历史、现实烦恼、邀请码或账户标识。

### 10.5 现有安全规则保持

身份透明短路、高风险内容短路、聊天长度限制、JSON 请求体限制、Cookie 属性、邀请码摘要和会话摘要均保持不变。

## 11. 修改文件清单

### 11.1 `agent.py`

- 把共享提示词拆为两套完整默认提示词。
- 新增模型配置键和提示词配置键定义。
- 新增 `ModelConfig`。
- 把 `AnthropicMessageClient` 替换为支持两种协议的 `ModelMessageClient`。
- 新增 OpenAI 消息、工具调用和工具结果转换。
- 新增模型配置读取、校验、公开返回、保存和连接测试。
- 新增提示词读取和保存。
- 修改 `respond()`，每轮动态读取配置并按模式选择提示词。

### 11.2 `storage.py`

- 新增 `app_settings` 表。
- 新增 `get_app_settings()`。
- 新增 `set_app_settings()`，使用事务和 upsert 原子保存。
- 数据库初始化后尝试把文件权限设置为 `0600`。

### 11.3 `server.py`

- 健康状态和用户状态改为读取运行时模型模式。
- 新增管理员配置读取接口。
- 新增模型配置保存接口。
- 新增提示词保存接口。
- 新增模型连接测试接口和耗时返回。

### 11.4 `app.py`

- 启动日志改为显示运行时数据库中的当前模型模式和模型名称。

### 11.5 `web/admin.html`

- 管理后台升级为配置中心。
- 新增模型配置表单。
- 新增两套提示词编辑器。
- 保留并重新组织邀请码管理区。

### 11.6 `web/admin.js`

- 新增配置中心统一加载。
- 新增模型配置渲染、收集、保存和连接测试。
- 新增密钥掩码和清除密钥交互。
- 新增协议说明切换。
- 新增提示词字符统计、恢复默认和保存。
- 保留邀请码管理交互。

### 11.7 `web/admin.css`

- 新增配置中心、状态徽标、模型表单、提示词卡片、页内导航和响应式样式。
- 保留邀请码表格和状态样式。

### 11.8 `tests/test_product.py`

- 新增模型配置持久化和密钥掩码测试。
- 新增两套提示词独立持久化测试。
- 新增管理员通过 HTTP 管理模型和提示词的端到端测试。
- 验证保存模型密钥后健康接口立即从 `demo` 切换为 `model`。

### 11.9 `README.md`

- 增加管理页面模型配置和双提示词说明。
- 说明 `.env` 是首次启动默认值，数据库保存后优先。
- 说明密钥本地存储和生产级密钥服务边界。
- 更新测试覆盖和文件职责说明。

### 11.10 `.env.example`

- 明确模型环境变量是启动默认值，管理页面可以在运行时保存配置。

## 12. 测试与验证结果

### 12.1 Python 语法检查

以下文件通过 `python3 -m py_compile`：

- `app.py`
- `agent.py`
- `server.py`
- `settings.py`
- `storage.py`
- `catalog.py`

### 12.2 JavaScript 语法检查

`web/admin.js` 通过 `node --check`。

### 12.3 自动测试

自动测试从第一阶段的 10 项增加到 13 项，最终结果为：

```text
Ran 13 tests
OK
```

13 项测试完整覆盖：

1. 两套提示词独立保存。
2. 模型配置持久化和密钥掩码。
3. 管理员生成邀请码。
4. 管理员管理模型和独立提示词。
5. 讨论自动写入看过状态。
6. 身份询问透明回应。
7. 推荐不返回看过电影。
8. 换片时保留此前意图。
9. 不同账户观影历史隔离。
10. 推荐逻辑硬性排除看过电影。
11. 停用邀请码后无法登录。
12. 换发邀请码保留账户并使旧码失效。
13. 同一邀请码长期进入同一账户。

### 12.4 真实数据库迁移

使用第一阶段已有本机数据库重新启动第二阶段代码，`app_settings` 自动创建，原邀请码仍处于激活状态，原账户和观影数据能够继续读取。

### 12.5 服务健康检查

重新启动后访问：

```text
GET /api/health
```

返回：

```json
{"ok": true, "mode": "demo"}
```

这表明服务正常，且当前尚未配置真实模型密钥。

### 12.6 浏览器可见界面验证

管理页面在真实浏览器中完成以下检查：

- 配置中心主标题正常显示。
- 模型表单全部字段和当前演示状态正常显示。
- 两套默认提示词分别加载，内容不同。
- 两个字符计数正确显示。
- 原邀请码状态表正常显示已有激活账户。
- 页面没有 JavaScript `error` 或 `warn` 日志。
- 桌面端首屏排版保持现有电影杂志式视觉语言。

## 13. 当前启动与使用方法

### 13.1 启动

```sh
cd products/yingban
python3 app.py serve
```

### 13.2 进入管理后台

打开：

```text
http://127.0.0.1:8765/admin
```

使用本机 `.env` 中的 `YINGBAN_ADMIN_TOKEN` 登录。阶段文档不记录真实管理员口令。

### 13.3 配置 Anthropic

在模型配置区填写：

- 接口协议：Anthropic Messages
- 模型名称：由所使用的 Anthropic 服务实际支持的模型 ID
- 接口根地址：官方服务通常为 `https://api.anthropic.com`
- API 密钥：实际 Anthropic 密钥
- 请求超时、最大输出长度和表达温度：按业务需要设置

先点击“测试连接”，成功后点击“保存模型配置”。

### 13.4 配置 OpenAI 兼容服务

在模型配置区填写：

- 接口协议：OpenAI 兼容
- 模型名称：目标服务实际支持的模型 ID
- 接口根地址：目标服务提供的 API 根地址，可带或不带 `/v1`
- API 密钥：目标服务密钥
- 请求超时、最大输出长度和表达温度：按目标模型约束设置

先点击“测试连接”，成功后点击“保存模型配置”。

### 13.5 修改两套提示词

分别编辑“聊电影 Agent”和“找电影 Agent”，确认职责、身份、安全和工具限制完整后，点击“保存两套提示词”。保存后下一轮用户对话立即使用新提示词。

## 14. 当前尚未完成事项

- 当前没有真实模型 API 密钥，因此已经完成的是配置校验、配置持久化、密钥掩码、管理员管理流程和原有 Agent 业务回归测试；尚未对真实外部模型执行最终连通测试，也尚未增加面向具体供应商的独立协议契约测试。
- 尚未为每个模型服务提供预设模板；管理员需要知道目标服务的模型 ID 和接口根地址。
- 尚未实现模型调用重试、指数退避、备用模型和故障自动降级到演示模式。
- 尚未记录模型 token、费用、首字延迟、总延迟和成功率指标。
- 尚未实现提示词版本历史、差异对比、发布审批和一键回滚。
- 尚未实现不同模型分别绑定不同 Agent；当前两套 Agent 共享同一个模型连接配置，只使用不同系统提示词。
- 尚未实现多环境配置，例如开发、测试和生产配置隔离。
- 尚未迁移到专业密钥管理服务。
- 尚未增加 CSRF Token 和持久化管理员审计日志。
- 模型接口地址由管理员配置，公开部署前应增加允许域名策略或出站网络策略，降低错误配置和服务端请求伪造风险。
- 电影目录仍然是有限的种子数据，推荐质量上限受数据覆盖影响。

## 15. 已知风险

### 15.1 API 密钥存储风险

密钥当前以明文值保存在权限为 `0600` 的本机 SQLite 文件中。设备当前系统用户或拥有更高系统权限的进程仍然可以读取。公开部署必须改用专业密钥服务。

### 15.2 接口兼容差异

“OpenAI 兼容”服务之间可能在工具调用、参数名称、温度支持、最大 token 字段和错误格式上存在差异。当前实现以 Chat Completions 和 function tools 的主流兼容格式为准，接入具体供应商时必须使用“测试连接”，并用真实聊电影和找电影请求验证工具调用。

### 15.3 提示词误配置风险

管理员可以编辑完整系统提示词。如果删除身份边界、事实约束、工具限制或安全规则，模型行为可能退化。代码层的高风险短路和推荐硬去重仍然存在，但不能覆盖全部提示词风险。未来应增加版本、差异、审批和安全规则锁定。

### 15.4 连接测试局限

连接测试只验证固定文本响应，不验证工具调用、长上下文、电影候选格式和实际用户流。保存真实模型后还必须分别完成一次聊电影和找电影验收。

## 16. 下一阶段建议

第三阶段建议优先选择以下一个明确方向，不要同时无边界扩张：

### 方向 A：真实模型接入验收与可观测性

- 配置用户选定的真实模型。
- 完成连接测试。
- 分别验证聊电影和找电影工具循环。
- 增加调用轨迹脱敏、延迟、token、成本和错误指标。
- 增加超时重试与备用模型。

### 方向 B：提示词版本管理

- 保存每次提示词修改版本。
- 显示修改时间和修改者。
- 支持默认版、草稿版和已发布版。
- 支持差异对比和一键回滚。
- 对不能删除的身份、安全和数据规则做代码层锁定。

### 方向 C：生产级电影目录

- 选择合规电影数据源。
- 建立电影统一 ID、译名、原名和版本体系。
- 增加目录同步、数据质量检查和更新时间。
- 在管理员后台增加影片搜索、编辑、上下架和内容提醒管理。

## 17. AI 接手入口

下一次开发开始时按以下顺序执行：

1. 读取根目录 `AGENTS.md`，确认阶段文档和安全规则。
2. 查找阶段号最大的 `第X阶段技术开发文档.md`。如果第二阶段仍是最新文档，先读本文件第 0、14、15、16、17 节；根据具体任务再读取相关完整章节。
3. 不需要为了开始当前迭代而先通读第一阶段文档。本文件是当前自包含快照；只有追溯基础架构来源或发现矛盾时再读第一阶段。
4. 读取与本次需求直接相关的代码入口。模型与提示词从 `agent.py`、`server.py`、`storage.py`、`web/admin.*` 开始；邀请与记忆从 `storage.py`、`catalog.py`、`server.py` 开始；用户体验从 `web/index.html`、`web/app.js`、`web/styles.css` 开始。
5. 运行 `python3 -m unittest discover -s tests -v`，确认当前基线为 13 项通过。
6. 调用 `/api/health` 或检查管理后台，确认实时模型模式；不要直接把本文的阶段结束快照当作当前事实。
7. 读取 `BLUEPRINT.md` 中与本次架构决策相关的章节。只有当需求改变整体产品架构时，才需要重新完整推导七层架构。
8. 明确本次需求边界，从本文件第 14、15、16 节选择接手点，实施并验证。
9. 开发完成后生成 `第三阶段技术开发文档.md`。新文档必须自包含新的当前状态，并保留本文件，不得覆盖历史。
