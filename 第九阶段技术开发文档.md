# 影伴第九阶段技术开发文档

> 文档日期：2026-08-21
> 阶段主题：P0 冷启动、反馈与信任
> 开发分支：`feature/yingban-stage-9`
> 文档性质：下一次版本迭代 AI 的工程交接上下文和当前状态快照，不是宣传稿。

## AI 快速接手卡

- **当前阶段**：第九阶段，P0“冷启动、反馈与信任”。
- **完成状态**：功能、数据迁移、API、Agent、桌面真实浏览器和自动化已完成；真实窄屏浏览器未完成，不能把现有响应式 CSS 等同于移动端验收。
- **权威代码目录**：`products/yingban/`。
- **启动入口**：`cd products/yingban && python3 app.py serve`。
- **用户端**：`http://127.0.0.1:8765/`。
- **管理端**：`http://127.0.0.1:8765/admin`。
- **测试命令**：`cd products/yingban && python3 -m unittest discover -s tests -v`。
- **当前测试基线**：36 项，2026-08-21 全部通过；第八阶段 27 项意义未删除。
- **当前运行模式**：真实模型模式，时点模型标识 `deepseek-v4-flash`；TMDB 与语音已连接。服务器交接时处于停止状态，必须重新以 `/api/health` 和管理配置只读验证。
- **当前数据时点**：账户 2、电影 55、电影状态 2、非空旧 `note` 1、迁移后观后感版本 1、推荐曝光 21、新画像/反馈/普通产品事件/用量事件 0、安全事件 0；`PRAGMA foreign_key_check` 为空。
- **核心代码入口**：`app.py` 启动；`server.py` HTTP 契约；`storage.py` schema、迁移和账户隔离；`catalog.py` 推荐硬排除；`agent.py` 模型上下文和观后感草稿；`web/app.js` 用户流程；`web/admin.js` 管理汇总与价格表。
- **不可破坏的产品约束**：看过事实与喜欢评价分离；推荐数据层排除已看和持续不感兴趣；口味画像仅描述电影偏好；当次请求优先于画像；AI 观后感只能先成为草稿；锁定版本不得被静默改写；删除观后感保留已看；不持久化原始聊天和录音；不开发商业化。
- **当前实现基线而非永久确认**：冷启动反馈“喜欢/一般/不喜欢”；不能跳过 5 部；自动整理默认开启但只生成草稿。以后可以调整文案或默认值，不能破坏三档证据意义和观后感自主权。
- **已知阻塞**：第十阶段开始前应补真实窄屏浏览器验收；正式成本汇总前需管理员按供应商账单配置价格表，否则后续事件仍以 `unpriced-v1 / ¥0` 记录。
- **下一阶段文档名**：`第十阶段技术开发文档.md`，不得覆盖本文档。
- **建议第一步检查**：完整读取根 `AGENTS.md`、总文档和本文档；复跑 36 项测试；只读核对 schema、当前数据库数量、`/api/health`、真实配置模式和移动端验收缺口。

## 1. 需求背景

第八阶段结束时，影伴已具备邀请码账户、电影讨论、推荐硬去重、电影状态、单一观后感 `note`、模型/联网/语音管理和 27 项测试，但空白账户第一次进入仍缺少口味证据，推荐反馈没有关联具体曝光，观后感由讨论链路直接覆盖，产品也缺少不含原始内容的漏斗、错误、延迟和成本汇总。

第九阶段解决的不是“多做一个问卷页”，而是建立一条可验证的 P0 闭环：

`5 部真实看过电影 → 三档反馈 → 可见且可修正的口味画像 → 首次推荐或讨论 → 曝光级反馈 → 用户自主确认的观后感 → 非敏感健康汇总`。

冷启动数量 5 已在总文档 `DEC-002` 确认。商业化不在本阶段范围内，见 `DEC-003`。P0、P1、P2 分别对应第九、十、十一阶段，见 `DEC-004`。

## 2. 阶段目标

1. 让新账户在不填写抽象性格问卷的情况下，用恰好 5 部看过的真实电影建立第一版口味画像。
2. 让画像对用户可见、带电影证据、可修正、有版本，同时禁止心理、人格、医疗和现实身份推断。
3. 让推荐动作与具体 `recommendation_impressions.id` 关联，区分当下不想看与持续排除。
4. 把单一 `user_movie_states.note` 升级为有来源、版本和状态的观后感，并把最终控制权交给用户。
5. 建立不保存原始聊天、录音、观后感和密钥的产品事件与调用成本汇总。

## 3. 继承的已有能力

以下能力来自第一至第八阶段并继续有效：

- 邀请码首次使用创建账户，同一有效邀请码回到同一账户；停用和换发行为不变。
- 用户电影状态为 `watched`、`watchlist`、`disliked`，且按账户隔离。
- 推荐候选在目录层排除已看，而不是只依赖提示词。
- 讨论与推荐为两套独立系统提示词，模型最多运行 8 轮领域工具。
- 电影目录支持本地种子片、TMDB 搜索、真实海报持久化和文字封面降级。
- 联网搜索只面向公开页面，不携带 Cookie、不自动登录、不绕过访问控制。
- 文字主回复始终可用；语音转写和合成不长期保存原始音频。
- 管理配置中的模型、联网和语音密钥只向前端返回掩码。
- 阿映被询问身份时必须透明说明是 AI，不伪造真人经历。

第八阶段 27 项测试意义完整保留，具体清单继续以总文档第 18.1 节为准。

## 4. 架构与关键决策

### 4.1 冷启动是服务端状态机

`account_profiles.onboarding_status` 只能为 `not_started`、`in_progress`、`completed`。前端不能自行宣称完成；`complete_onboarding()` 必须看到当前账户恰好 5 条 `context='onboarding'` 的电影反馈。`add_onboarding_movie()` 在事务内拒绝第 6 部。

选择冷启动电影会同时写入 `user_movie_states.state='watched'`。三档反馈写入独立 `user_movie_feedback.sentiment`，负向 `negative` 不能把观看事实改成 `disliked`。

### 4.2 画像采用可解释的确定性生成

当前画像由已选电影的 `genres`、`themes`、`moods` 和正/中/负反馈生成可见维度，每个维度保留支持电影证据。这样在模型不可用时仍能完成冷启动，也避免把窄职责画像生成变成不可测试的自由文本推断。

用户修正不会删除历史事实，而是把对应维度标记为 `hidden` 并递增 `taste_version`。推荐和 Agent 上下文只读取未隐藏维度。当次明确请求始终优先。

### 4.3 推荐反馈以曝光为权限边界

每张实际返回卡片先写入 `recommendation_impressions`，再把 `impression_id` 返回前端。反馈接口从当前账户解析曝光，不能仅凭任意电影 ID 写入别人的曝光。

动作意义：

- `watchlist`：明确想看，写入想看状态。
- `watched`：已经看过，写入已看并持续硬排除。
- `discuss`：进入这部电影讨论，写入已看。
- `not_now`：方向可能正确但当下不看，不建立长期电影状态。
- `wrong_tone`：当次氛围不符合。
- `wrong_genre`：当次类型不符合。
- `too_heavy`：当次不接受沉重内容。
- `not_interested`：持续不感兴趣，写入 `disliked` 并作为后续排除。
- `other`：其他可选原因，不强制保存用户正文。

### 4.4 观后感使用追加版本

`movie_reflections` 以 `(account_id, movie_id, version)` 唯一。用户编辑创建新版本，不覆盖旧版本。状态完整定义：

- `draft`：AI 已生成、用户尚未确认；不能伪装成用户最终文字。
- `confirmed`：用户明确确认，或用户编辑后保存的新版本。
- `locked`：用户禁止 AI 静默改写的当前版本；重新整理只能产生独立草稿建议。
- `deleted`：正文已清空并从用户可见历史移除；保留最小审计壳，不保留被删除正文。

来源完整定义：

- `ai`：AI 原始草稿。
- `user`：用户直接创建的文字。
- `ai_then_user`：用户在 AI 草稿或确认版本基础上编辑。

聊天主请求不再同步生成观后感。显式“整理这次观后感”、弹窗“重新整理”，或用户开启自动整理并离开有效电影讨论时，才调用独立生成接口。任何生成都只产生草稿。

### 4.5 可观测数据与正文分离

`product_events` 只收事件名、账户可空引用、短会话提示和白名单式结构属性。记录前过滤包含 `message`、`content`、`prompt`、`reflection`、`note`、`audio`、`api_key`、`invite_code`、`token`、`transcript`、`query` 的键。

`model_usage_events` 保存操作、服务、成败、延迟、可用的输入/输出单位、估算金额、价格版本和错误类别，不保存 prompt 或输出正文。

管理员价格表按操作保存三类数字：`input_per_million`、`output_per_million`、`call_cost`。模型操作 `chat`、`reflection`、`taste_profile` 使用每百万输入/输出单位价格；`stt`、`tts`、`tmdb`、`web_search`、`web_reader` 可使用单次价格。金额单位固定为 CNY。新事件固化当时金额和版本，历史事件不重算。

## 5. 已实现功能

### 5.1 用户闭环

- 新账户登录后必须进入 5 部电影冷启动；已完成账户直接进入首页。
- 候选列表具有类型、年代、地区、主题和热度多样性，支持本地/TMDB 搜索、海报失败降级、撤销和替换。
- 进度从 `0/5` 到 `5/5`，未满 5 部时显示剩余数量，服务端拒绝超限。
- 每部已选电影可标记喜欢、一般、不喜欢，并始终保留已看事实。
- 完成后展示带证据的口味摘要和维度，提供“帮我找下一部”和“聊聊这 5 部中的一部”。
- 首页“阿映目前怎么理解我”可查看画像、证据、版本并修正不准确维度。
- 推荐卡带曝光 ID，原因反馈后明确提示“只影响电影推荐，不会推断现实生活”。
- 观后感弹窗显示状态、版本、来源、时间，支持生成、保存、确认、锁定、解锁、重新整理、删除和继续讨论。
- 自动整理偏好可开关；默认开启只代表允许创建草稿，不允许覆盖确认/锁定版本。

### 5.2 管理闭环

- 产品健康概览显示账户、冷启动、推荐曝光/反馈、观后感状态，以及调用数、失败数、平均延迟和估算成本。
- 页面不展示原始聊天、原始观后感、录音、模型完整输入输出或完整密钥。
- 管理员可配置价格版本、模型每百万输入/输出单位价格，以及语音、TMDB、网页搜索、网页读取的单次价格。
- 价格表默认 `unpriced-v1` 和零金额；必须由管理员根据真实供应商账单填写，系统不编造价格。

## 6. 数据结构

### 6.1 新表 `account_profiles`

- `account_id`：账户主键和外键，删除账户时级联。
- `onboarding_status`：`not_started/in_progress/completed`。
- `onboarding_completed_at`：最后完成时间。
- `taste_summary`：用户可见口味摘要。
- `taste_dimensions_json`：可见维度、证据和 `hidden` 修正状态。
- `taste_version`：每次重算或修正递增。
- `taste_generated_at`：画像生成时间。
- `auto_generate_reflection_drafts`：0/1，默认 1。
- `created_at/updated_at`：审计时间。

### 6.2 新表 `user_movie_feedback`

- 唯一约束：`(account_id, movie_id, context)`。
- `context`：`onboarding/recommendation/post_watch/manual`。
- `sentiment`：`positive/neutral/negative`。
- `reason_codes_json`：非敏感结构化原因码。
- 反馈与 `user_movie_states.state` 分离。

### 6.3 新表 `recommendation_feedback`

- 关联 `recommendation_impressions.id`、账户和电影。
- `action` 限定为九种反馈动作。
- `reason_code` 只保存结构化原因，不强制保存自由正文。
- 曝光和账户删除时级联。

### 6.4 新表 `movie_reflections`

- 按账户、电影和版本唯一。
- 保存 `content/source/status/based_on_version`。
- 保存创建、更新、确认、删除时间。
- 删除正文时状态改为 `deleted`、正文清空，不删除观看事实。

### 6.5 新表 `product_events`

- `account_id` 可空，账户删除后置空。
- `event_name`、过滤后的 `properties_json`、`schema_version=1`、`created_at`。
- 不承担聊天历史或用户内容存储职责。

### 6.6 新表 `model_usage_events`

- `operation/provider/service_id/success/latency_ms`。
- `input_units/output_units` 在 provider 返回时记录。
- `estimated_cost/pricing_version` 固化事件发生时估算。
- `error_category` 只保存错误类别，不保存错误中的用户正文。

### 6.7 新索引

- `idx_onboarding_feedback_account`
- `idx_recommendation_feedback_account`
- `idx_reflections_current`
- `idx_product_events_name_time`
- `idx_model_usage_operation_time`

### 6.8 新配置键

- `usage_pricing_version`：最多 80 字符的价格规则版本。
- `usage_pricing_json`：各操作的输入、输出和单次调用价格；不含密钥。

## 7. 接口变化

### 7.1 新增用户接口

| 方法 | 路径 | 责任与边界 |
|---|---|---|
| GET | `/api/onboarding` | 返回当前账户状态、已选电影、三档反馈和进度 |
| GET | `/api/onboarding/candidates?query=` | 返回多样候选或片名搜索结果 |
| POST | `/api/onboarding/movies` | 添加或更新一部反馈，事务内上限 5 |
| DELETE | `/api/onboarding/movies/{movie_id}` | 移除冷启动电影并重置需重算状态 |
| POST | `/api/onboarding/complete` | 恰好 5 部时生成新画像版本 |
| GET | `/api/taste-profile` | 返回可见画像、证据、版本和自动整理偏好 |
| POST | `/api/taste-profile/corrections` | 隐藏不准确维度并递增版本 |
| POST | `/api/reflection-preferences` | 开关自动草稿偏好 |
| POST | `/api/recommendations/{impression_id}/feedback` | 校验账户曝光后记录反馈并执行动作语义 |
| GET | `/api/reflections/{movie_id}` | 返回当前可见版本和历史元数据 |
| POST | `/api/reflections/{movie_id}` | 用户保存文字为新确认版本 |
| DELETE | `/api/reflections/{movie_id}` | 清空观后感正文但保留已看 |
| POST | `/api/reflections/{movie_id}/generate` | 生成独立 AI 草稿，不覆盖确认版本 |
| POST | `/api/reflections/{movie_id}/confirm` | 确认指定草稿 |
| POST | `/api/reflections/{movie_id}/lock` | 锁定当前确认版本 |
| POST | `/api/reflections/{movie_id}/unlock` | 解锁为确认状态 |

### 7.2 修改接口

- `GET /api/me` 增加冷启动状态、画像版本和自动整理偏好。
- `POST /api/chat` 返回 `reflection_draft_available`；`reflection_updated` 保留为兼容字段但固定为 `false`，聊天不再写观后感。
- 推荐结果每部电影增加 `impression_id`。
- `DELETE /api/history/{movie_id}` 删除当前账户电影状态和反馈，并把关联观后感清空为 `deleted` 审计壳。

### 7.3 新增管理接口

- `GET /api/admin/metrics/summary`：非敏感账户、漏斗、反馈、观后感和调用汇总。
- `GET /api/admin/usage-pricing`：读取 CNY 价格版本和操作价格表。
- `POST /api/admin/usage-pricing`：校验并保存价格表，只影响后续事件。

## 8. 页面变化

### 8.1 用户页 `web/index.html`、`web/app.js`、`web/styles.css`

- 新增必经冷启动视图：候选搜索、网格、已选列表、三档反馈、`x/5` 进度和完成动作。
- 首页新增口味画像入口；画像弹窗显示证据和修正按钮。
- 推荐卡新增曝光反馈选择器和隐私说明。
- 聊天页新增“整理这次观后感”。
- 观后感弹窗增加状态、版本、来源、时间、编辑、确认、锁定、解锁、重整、删除和自动草稿开关。
- 响应式样式已覆盖冷启动、画像、反馈和观后感，但仍需真实窄屏验收。

### 8.2 管理页 `web/admin.html`、`web/admin.js`、`web/admin.css`

- 导航增加“产品健康”。
- 新增汇总卡和调用表。
- 新增成本价格表表单；字段使用人民币，明确“保存后只影响后续事件”。
- 资源版本更新为 `v=9`，避免浏览器使用旧脚本和样式。

## 9. Agent 行为变化

- `AgentRuntime._context()` 注入当前账户可见、未隐藏的口味画像，最多 8 个维度。
- 上下文明确：画像只用于电影理解，当次请求优先，不得推断人格、心理、医疗或现实身份。
- 推荐仍只从目录层已经硬排除的候选中选择，Agent 不能补造候选。
- OpenAI 兼容响应保留 usage；聊天和观后感分别记录模型调用单位、延迟和成败。
- `summarize_reflection()` 是窄职责草稿生成器；它不持久化原始聊天，不直接写确认版本。
- 演示模式不会伪造 AI 观后感草稿；没有真实模型时显式返回可理解错误。
- 原有最多 8 轮工具限制、剧透边界、AI 身份透明和公开网页限制继续有效。

## 10. 配置变化

- `.env` 没有新增必须项。
- 模型、TMDB、搜索和语音配置继续以数据库 `app_settings` 为管理页保存后的权威来源。
- 新增价格配置也保存在 `app_settings`，不包含凭据。
- 未配置价格时 `usage_pricing()` 返回 `version='unpriced-v1'`、`currency='CNY'`、空费率；估算成本为 0。
- 管理员应使用真实供应商账单创建有意义的版本名。价格变化时使用新版本，不覆盖旧事件中的 `pricing_version`。

## 11. 数据迁移、兼容性与回滚风险

### 11.1 迁移方式

`Store.initialize()` 使用 `CREATE TABLE IF NOT EXISTS` 和 `CREATE INDEX IF NOT EXISTS` 幂等创建新结构。随后执行单条幂等 SQL：只把 `user_movie_states.note` 中 `trim()` 后非空、且当前账户与电影还没有任何 `movie_reflections` 的记录迁移为：

- `version=1`
- `source='ai'`
- `status='confirmed'`
- 创建、更新和确认时间继承旧状态时间

空 `note` 不创建虚假版本。重复启动不会重复迁移。

### 11.2 旧数据兼容

`user_movie_states.note` 保留为兼容快照。确认或锁定版本可同步兼容快照；新版本权威来源是 `movie_reflections`。现有账户没有 `account_profiles` 行时，读取接口返回合成的 `not_started` 默认状态，直到首次冷启动写入。

### 11.3 实际迁移核对

迁移前：账户 2、电影 55、状态 2、非空旧 `note` 1、曝光 21、安全事件 0。

迁移后：账户 2、电影 55、状态 2、非空旧 `note` 1、观后感版本 1、曝光 21、画像 0、普通事件 0、用量事件 0；外键检查为空。旧数据数量未丢失。

### 11.4 回滚风险

- 旧代码不知道新表，但仍能读取兼容 `note`；回滚不会自动删除新表。
- 若回滚后旧代码修改 `note`，再切回新代码时不会覆盖已存在的版本，可能形成兼容快照和权威版本差异，必须人工核对后追加新版本。
- 不建议删除新表回滚，因为会不可恢复地丢失反馈、画像证据、观后感版本和观测数据。
- `deleted` 观后感正文已清空，不能依赖数据库回滚恢复用户主动删除的内容。
- 价格表只作用于未来事件；修改或删除当前价格配置不能重新计算历史成本。

## 12. 安全与隐私边界

- 所有画像、反馈、观后感和推荐反馈查询都带当前 `account_id`。
- 推荐反馈先验证曝光属于当前账户。
- 口味画像只允许电影维度，不记录现实身份、工作、学校、家庭、政治、医疗或心理标签。
- 原始聊天只在当前请求和前端会话中使用，不写 `product_events`。
- 原始录音和合成音频不写数据库。
- 观后感不等于聊天全文、现实日记、心理档案或医疗记录。
- 删除观后感清空正文但保留已看事实；删除电影状态只处理当前账户。
- 产品事件过滤正文、原始搜索和凭据类键；管理汇总只返回聚合。
- `.env`、数据库、完整邀请码、会话令牌和管理员口令不得进入 Git、阶段文档、截图或日志。
- 当前 SQLite 与标准库 HTTP 只适用于本地或受控环境，不构成公网生产安全架构。

## 13. 修改文件清单

- `.gitignore`：忽略 `.DS_Store`，延续本地敏感/运行文件忽略规则。
- `products/yingban/storage.py`：新 schema、迁移、冷启动、画像、反馈、观后感版本、事件、价格和汇总。
- `products/yingban/catalog.py`：画像辅助排序、已看/持续排除/近期曝光硬排除、曝光 ID。
- `products/yingban/agent.py`：画像上下文、usage 保留、模型用量事件、草稿职责。
- `products/yingban/server.py`：全部新用户/管理 API、事件写入、聊天与观后感解耦。
- `products/yingban/web/index.html`：冷启动、画像、推荐反馈和观后感结构。
- `products/yingban/web/app.js`：第九阶段用户状态机和 API 交互。
- `products/yingban/web/styles.css`：新用户界面与响应式样式。
- `products/yingban/web/admin.html`：产品健康和价格表结构。
- `products/yingban/web/admin.js`：汇总渲染、价格读取和保存。
- `products/yingban/web/admin.css`：汇总卡、调用表和价格表布局。
- `products/yingban/tests/test_product.py`：第九阶段单元、HTTP、迁移、隔离、隐私和价格测试。
- `products/yingban/README.md`：当前能力、启动、模式、隐私、测试数量和文档入口。
- `products/yingban/影伴产品开发总文档.md`：当前权威状态、第九阶段实绩、API、数据和下一阶段入口。
- `products/yingban/第九阶段技术开发文档.md`：本文档。

## 14. 自动化测试与验证结果

2026-08-21 执行：

```sh
python3 -m py_compile agent.py catalog.py storage.py server.py tests/test_product.py
node --check web/app.js
node --check web/admin.js
python3 -m unittest discover -s tests -v
```

结果：Python 与 JavaScript 静态检查通过，完整自动化 `36/36 OK`，耗时约 6.08 秒。

第九阶段新增 9 项测试意义：

1. 冷启动恰好 5 部并拒绝第 6 部。
2. 负向反馈仍保留已看事实。
3. 画像只使用当前账户证据，替换后版本递增。
4. 曝光反馈账户隔离，`not_now` 与持续排除不同。
5. 观后感草稿、确认、编辑、锁定、解锁、删除均符合版本语义并保留已看。
6. 旧 `note` 幂等迁移且不伪造空记录。
7. 产品事件过滤正文和凭据；用量按配置版本估算成本。
8. 新账户通过 HTTP 完成冷启动并获得证据画像。
9. HTTP 推荐反馈必须通过曝光，并按动作更新状态。

## 15. 真实浏览器验收结果

验收使用 `/private/tmp` 独立数据库、独立测试邀请码和本地端口，没有修改主数据库用户数据。

已通过桌面真实浏览器：

- 新账户登录进入冷启动。
- `0/5 → 5/5`、多样候选、搜索、三档反馈和替换可用。
- 画像显示正/中/负证据，修正维度后版本变化。
- 推荐返回三张卡，原因反馈显示隐私说明，持续排除语义正确。
- “聊聊这部”进入讨论并写入已看。
- 观后感弹窗显示 AI 草稿、版本、来源、时间；确认、编辑为版本 2、锁定工作。
- 管理端显示账户、冷启动、曝光、反馈、观后感和调用汇总，无原始正文。
- 用户端和管理端控制台无错误。

未完成：浏览器插件的视口覆盖没有实际改变 `innerWidth`，因此没有完成真实窄屏设备交互验收。代码已有响应式断点和语义结构，但这不能替代实际验收。

## 16. 启动方法

要求 Python 3.11+，核心服务只使用 Python 标准库。

```sh
cd /Users/Admin/Tool/agent-blueprint/products/yingban
cp .env.example .env
python3 app.py generate-invites 1
python3 app.py serve
```

若已有 `.env` 和数据库，不要覆盖。完整邀请码只在生成时显示一次。打开用户端和管理端，不能用 `file://` 代替 HTTP 服务。

## 17. 使用方法

### 17.1 新账户

1. 使用邀请码登录。
2. 选择 5 部确实看过的电影，并分别给出喜欢、一般或不喜欢。
3. 查看带证据的初始画像；不准确维度可修正。
4. 选择第一次推荐或讨论。
5. 推荐反馈作用于具体卡片；观后感先生成草稿，再由用户确认、编辑或锁定。

### 17.2 管理员

1. 使用部署环境中的 `YINGBAN_ADMIN_TOKEN` 登录 `/admin`。
2. 在产品健康页查看不含正文的汇总。
3. 根据供应商账单填写价格版本和各服务价格；不要猜测或填写营销标价之外未经确认的数字。
4. 保存后只对新调用生效；历史事件保留原版本和金额。

## 18. 当前运行与数据模式

- 代码支持 `demo` 与 `model` 两种模式。
- 当前真实本地配置在验收时为 `model`，模型标识 `deepseek-v4-flash`；TMDB 和语音连接成功。
- 服务器目前没有保持后台运行；下次接手必须重新启动和验证。
- 主数据库没有被浏览器验收写入冷启动测试数据。
- 当前价格表尚未正式配置，默认成本为 0；这表示“未定价”，不表示真实调用免费。
- 当前部署为 SQLite + `ThreadingHTTPServer`，只适合本地或受控环境。

## 19. 尚未完成事项

- 真实窄屏/移动设备完成冷启动、画像、推荐反馈、观后感和管理价格表的交互验收。
- 管理员依据真实供应商账单录入正式成本价格版本。
- P1 本机聊天草稿保留期尚未由用户决定。
- P1 本周建议的出现时间和关闭位置尚未由用户决定。
- 服务端电影会话摘要、本周建议、想看跟进、月度回顾属于第十阶段，尚未开发。
- 小组共享、正式账户、数据导出/删除、生产部署和小范围上线属于第十一阶段，尚未开发。

## 20. 已知风险

- 画像当前是确定性启发式，解释稳定但可能过于粗；不能用扩大敏感推断范围来追求“聪明”。
- `taste_dimensions_json` 为 JSON 快照，复杂分析或大规模查询前可能需要规范化，但当前不应为未来猜测提前重构。
- 自动离开讨论生成草稿依赖前端判断“有实质表达”，刷新或异常关闭时不保证一定触发。
- 价格是管理员配置的估算，不是供应商账单；用量单位缺失时只能计算单次价格部分。
- 现有事件没有测试账户标识，大规模真实指标分析前需定义测试账户排除规则。
- SQLite 并发、备份、迁移和公网安全不足。
- 真实移动端尚未验收，可能存在软键盘、滚动、弹窗高度和触控目标问题。

## 21. 回滚与故障降级

- 模型不可用：文字接口返回明确错误；账户、冷启动、确定性画像、电影状态和演示推荐继续可用；不伪造观后感草稿。
- TMDB 不可用：本地片库继续工作，海报降级为文字封面。
- 语音不可用：文字主回复不受影响，用户看到可理解错误。
- 价格未配置：继续记录调用量、延迟和错误，金额为 0、版本为 `unpriced-v1`，不猜测成本。
- 数据回滚：优先恢复数据库备份，不删除新表；恢复后必须执行数量对照和 `PRAGMA foreign_key_check`。
- UI 回滚：资源版本必须同步，避免新 HTML 加载旧 JavaScript。

## 22. 下一阶段建议

第十阶段主题是 P1“留存、连续性与主动服务”。开发前先完成两项用户决策：

1. 本机聊天草稿保留多久，以及共用设备上的清空提示。
2. 本周电影建议何时出现、用户在哪里关闭。

建议实现顺序：

1. 补真实窄屏验收并处理发现的问题。
2. 建立不保存原始聊天的本机会话草稿与用户明确授权的电影会话摘要。
3. 做本周建议与可关闭入口。
4. 做想看跟进和月度电影回顾。
5. 建立第十阶段事件、隐私、降级和自动化，再创建 `第十阶段技术开发文档.md`。

不要提前开发第十一阶段的多人共享、正式登录、生产部署或商业化。

## 23. AI 接手入口

下一次 AI 必须按顺序执行：

1. 完整读取仓库根 `AGENTS.md`。
2. 完整读取 `影伴产品开发总文档.md`。
3. 完整读取本文档，不只读快速接手卡。
4. 用只读检查核对当前分支、工作区差异、启动入口、36 项测试、schema、API、配置状态和真实数据库数量。
5. 启动服务后检查 `/api/health`，确认当前是 `demo` 还是 `model`，不得把本文档时点状态当成永久事实。
6. 补真实窄屏浏览器验收；若发现问题，先修复并在第十阶段文档完整记录。
7. 向用户确认 P1 两项产品决策，不用隐式默认把它们变成永久事实。
8. 第十阶段开发必须保留第九阶段的数据定义、账户隔离、推荐硬去重、观后感自主权、非敏感事件和价格版本契约。
9. 完成后新建 `第十阶段技术开发文档.md`，更新 README 和总文档，不覆盖本文档。
