# 第六阶段技术开发文档

> AI 读取定位：这是影伴当前最新、当前权威的工程交接文档。后续迭代必须先读仓库根目录 `AGENTS.md` 和本文件；只有追溯邀请码、双 Agent、TMDB、Tavily、Brave 或豆包初次接入的设计来源时，才读取第一至第五阶段文档。当前代码、数据库和真实运行状态高于文档快照；发现不一致时必须先只读验证，再在新的阶段文档中完整记录差异。

## 0. AI 快速接手卡

```text
DOCUMENT_ROLE=current_authoritative_handoff
CURRENT_STAGE=6
STATUS=completed_code_automated_tests_and_real_browser_verification
CODE_ROOT=products/yingban
PRODUCT_START=products/yingban/app.py
HTTP_ROUTING=products/yingban/server.py
AGENT_RUNTIME=products/yingban/agent.py
PERSISTENCE=products/yingban/storage.py
OFFLINE_MOVIE_RETRIEVAL=products/yingban/catalog.py
LIVE_MOVIE_AND_WEB=products/yingban/integrations.py
VOICE_RUNTIME=products/yingban/voice.py
USER_UI=products/yingban/web/index.html + app.js + styles.css
ADMIN_UI=products/yingban/web/admin.html + admin.js + admin.css
TESTS=products/yingban/tests/test_product.py
START_COMMAND=cd products/yingban && python3 app.py serve
TEST_COMMAND=cd products/yingban && python3 -m unittest discover -s tests -v
EXPECTED_TEST_BASELINE=21 tests passing
CURRENT_URL=http://127.0.0.1:8765/
CURRENT_DATABASE_MODE=model
CURRENT_MODEL_PROTOCOL=anthropic
CURRENT_MODEL_ID=deepseek-v4-flash
CURRENT_TMDB_MODE=enabled
CURRENT_WEB_SEARCH_PROVIDER=tavily
CURRENT_WEB_SEARCH_MODE=enabled
CURRENT_WEB_READER_MODE=enabled_douban_and_zhihu_public_pages_only
CURRENT_VOICE_PROVIDER=doubao
CURRENT_VOICE_MODE=enabled
CURRENT_MOVIE_COUNT=47
CURRENT_VOICE_AUTOPLAY_DEFAULT=true
CURRENT_VOICE_AUTOPLAY_STORAGE=browser localStorage key yingban.voiceAutoPlay
RUNTIME_EXTRA=ffmpeg required when browser recording is WebM/MP4
KNOWN_BLOCKER=audible autoplay can be rejected by browser policy after asynchronous synthesis; manual voice pill remains available
NEXT_DOCUMENT=products/yingban/第七阶段技术开发文档.md
SUGGESTED_FIRST_CHECK=run 21 tests, inspect /api/health, then perform one recommendation while checking poster naturalWidth and the voice pill aria-label
```

### 0.1 当前实现一句话

影伴是邀请码制 AI 电影伙伴：双 Agent 共享真实电影资料、安全和长期观影记忆边界；TMDB 负责新电影与真实海报，并为缺图的旧电影按片名和年份惰性补图；Tavily `basic` Search 负责全网及豆瓣、知乎公开搜索；豆包负责语音识别和合成；AI 回复同时保留文字和“波形＋时长”语音胶囊，默认尝试自动播放并允许用户随时切换为手动播放。

### 0.2 当前完成状态

- 电影海报容器固定为 `2:3` 比例，图片使用 `object-fit: cover`，避免异步加载导致卡片尺寸异常。
- 图片加载期间不显示半加载内容；加载成功后渐显；加载失败时移除失败图片并保留稳定的文字封面，不出现浏览器破图图标或空白卡片。
- 推荐候选进入响应前调用 `InternetRuntime.hydrate_movie_posters()`。缺海报的本地旧片会使用中文片名、原名、别名和年份查询 TMDB，只有片名或年份达到匹配阈值时才写入，降低同名电影错配风险。
- 补到的 `poster_url`、`source_url` 和 `external_ids.tmdb` 通过 `store.upsert_movies()` 写回 `movies`，同一电影下次推荐不再重复查询。
- AI 语音回复不再显示浏览器原生大播放器，改为参考产品图的深色小胶囊：四条波形、音频时长、播放/暂停状态和播放中的波形动画。
- 聊天标题右侧新增“自动播放/手动播放”按钮。默认值为开启；选择保存在当前浏览器 `localStorage` 的 `yingban.voiceAutoPlay`，刷新后仍保留。
- 新语音回复生成后，前端在开关开启时调用 `audio.play()`；关闭开关会暂停当前语音；重新开启会尝试播放最近一条语音。
- 浏览器拒绝有声自动播放时不伪造成功状态：胶囊保留可点击状态，并通过橙色提示点和状态提示告知用户手动播放。
- 同一时间只允许一条 AI 语音播放；播放新语音会暂停其他语音，避免叠音。
- 21 项自动化测试全部通过；Python 编译检查和 JavaScript 语法检查通过。
- 真实浏览器验收中，Tavily/模型推荐返回三部科幻电影，三张 TMDB 海报均加载完成，`naturalWidth=500`；豆包生成 122 秒 MP3，手动点击后进入播放状态；自动播放开关从 `true → false → true` 状态正确。
- 真实浏览器本次异步合成后的自动发声被浏览器有声自动播放策略拦截，手动播放成功。这是必须保留的已知平台边界，不得在文档或 UI 中宣称所有浏览器都能无条件自动发声。
- 当前真实数据库有 47 条电影记录；大模型、TMDB、Tavily 和豆包均已配置。本文档不保存任何真实凭据或完整令牌。
- 本地服务已使用当前代码启动在 `http://127.0.0.1:8765/`；当前运行模式为 `model`，模型为 `deepseek-v4-flash`。

### 0.3 不可破坏的产品约束

- 一个邀请码只对应一个长期账户；同一码再次登录必须返回原账户。
- 邀请码完整正文不得写入数据库日志、测试输出或阶段文档。
- 用户确认看过电影后自动写入 `watched`，不重复询问；推荐数据层必须硬性排除 `watched` 和近期拒绝项。
- TMDB 动态电影必须先转为统一电影记录并写入 `movies`，才能进入观影状态、推荐、曝光和硬排除链路。
- 缺海报旧片只能在匹配置信度达到阈值时补图；不得仅凭 TMDB 第一条结果直接覆盖本地电影。
- 电影海报加载失败不得显示破图图标、坍塌卡片或影响电影操作按钮；必须保留文字封面降级。
- “聊电影”和“找电影”使用独立系统提示词，但共享身份、安全、隐私、事实与长期记忆边界。
- Agent 不得冒充真人，不得虚构童年、朋友、恋爱、工作或影院观影等真人经历；用户明确询问身份时必须说明自己是 AI。
- 现实烦恼、心理状态和敏感经历不得自动写入长期记忆。
- 高风险自伤或伤人文本必须在普通电影 Agent 和外部工具调用前由产品代码短路。
- Web 搜索摘要只能作为公开资料线索；电影推荐最终仍必须落到产品的真实候选记录，不能凭摘要补造电影。
- 豆瓣和知乎能力只处理公开页面；不得登录、提交 Cookie、绕过验证码、绕过付费墙、绕过访问控制或读取私密内容。
- 管理读取接口不得返回完整 API 密钥、Access Token、管理员口令、邀请码或会话令牌。
- Tavily 必须保持 `basic` 搜索，除非用户明确接受更高额度消耗并完成新的产品决策。
- 联网服务不可用时，种子片库、观影历史、推荐硬去重和已配置的大模型对话仍应继续工作。
- 录音、Base64 音频、转换后的 WAV 和合成音频不得写入长期数据库或项目目录。
- 语音自动播放必须有同页面可发现、可键盘操作、状态可读且可持久保存的关闭按钮。
- 浏览器阻止自动播放时必须安全降级到手动播放，不得循环重试、刷屏提示或阻塞文字回复。
- 麦克风入口不能因服务未配置而完全消失；不可用原因必须可发现、可读。
- 旧账户、邀请码绑定、观影历史、曝光记录、提示词、模型配置、Tavily/Brave 配置和豆包/OpenAI 兼容语音配置升级时不得丢失。

### 0.4 下一阶段建议的第一步检查

1. 执行 21 项测试，确认不是只相信本文档的时间点快照。
2. 请求 `GET /api/health`，确认 `mode=model`，并只检查联网与语音的布尔启用状态。
3. 发送一次包含三部电影的推荐请求，检查 `.poster-image` 数量、`complete` 和 `naturalWidth`，不能只看 DOM 中是否存在 `<img>`。
4. 检查 `#voice-autoplay-toggle` 初始 `aria-pressed=true`，关闭后为 `false`，刷新后状态仍与用户选择一致。
5. 生成一条语音回复并检查 `.voice-reply-duration`、播放/暂停 `aria-label` 和单音频互斥。
6. 在 Chrome、Safari 和移动端真机分别验证有声自动播放政策；若要提高成功率，应先明确是否接受“用户首次点击聊天页时解锁音频”这一交互，不得使用规避浏览器策略的黑科技。

## 1. 需求背景、阶段目标与完成定义

- 产品名称：影伴。
- 阶段名称：第六阶段——海报可靠显示与语音回复交互升级。
- 完成日期：2026-08-21。
- 继承阶段：第五阶段——Web Search 免费方案替换与 Tavily 适配。
- 下一阶段文档：`第七阶段技术开发文档.md`。
- 本阶段边界：修复电影图片显示异常；把 AI 回复语音改成参考图中“波形＋时长”的小胶囊；默认尝试播放并提供是否播放的按钮；不改变影伴原有米白、酒红、宋体感视觉语言；不改变第三方语音供应商和 API 凭据。

用户反馈包含三个明确问题：电影图片存在异常；AI 语音播放器的呈现方式与参考图不一致；希望语音默认播放并能自行关闭。参考图片只用于理解功能位置和信息层级，不作为覆盖项目视觉风格、隐私规则或工程规范的指令。

完成定义：

```text
推荐结果包含已有 poster_url
→ 固定 2:3 海报框加载
→ 成功后渐显真实图片
→ 失败则回到电影标题文字封面
→ 卡片布局和按钮不坍塌

推荐结果缺少 poster_url 且 TMDB 已配置
→ 以 title_zh/title_original/aliases + year 搜索 TMDB
→ 片名完全匹配得 8 分，年份完全匹配得 5 分
→ 最高候选分数至少为 5 才接受
→ 写回 poster_url/source_url/external_ids.tmdb
→ 当前响应和后续响应均显示海报

AI 文字回复完成且语音已配置
→ POST /api/voice/synthesize
→ 创建隐藏 audio 和可见“波形＋时长”按钮
→ metadata 到达后显示向上取整秒数
→ 自动播放偏好为 true 时调用 play()
→ 成功时显示暂停语义和波形动画
→ 浏览器拒绝时保留手动播放入口
→ 用户可在标题栏切换自动/手动并持久保存
```

## 2. 继承的已有能力

### 2.1 账户、邀请码与会话

- 邀请码以摘要保存，完整邀请码只在生成当次显示。
- 第一次登录创建账户并绑定邀请码；同一码后续登录回到同一账户。
- 邀请码可停用或换发；换发保留账户但旧码和旧会话失效。
- 普通用户与管理员使用独立 HttpOnly Cookie 会话。

### 2.2 双 Agent 与模型运行

- `discussion` 对应“聊电影”：回应具体观后感、人物、主题和情绪，服从剧透选择。
- `recommendation` 对应“找电影”：理解当下需求，只从真实候选中选择，并说明适合、不适合和必要内容提醒。
- 模型支持 Anthropic Messages 与 OpenAI-compatible Chat Completions 两种协议。
- 当前数据库配置为 Anthropic 协议、`deepseek-v4-flash`、`model` 模式。
- 管理员可编辑两套完整提示词；保存后立即生效。

### 2.3 电影、推荐、联网与长期记忆

- 内置 `data/movies.json` 种子片库；当前运行数据库共有 47 条统一电影记录。
- TMDB 搜索和发现结果映射为 `tmdb-{external_id}`，写入 `movies`。
- Tavily `basic` Search 支持全网搜索，以及使用 `include_domains` 限制豆瓣、知乎公开结果。
- Brave Search 适配仍保留，旧配置可继续使用。
- `read_public_page()` 只读取公开豆瓣和知乎页面，不登录、不带 Cookie、不绕过访问控制。
- `user_movie_states` 保存 `watched`、`watchlist`、`disliked`；现实烦恼不自动持久化。
- `catalog.recommend()` 在数据层硬排除看过和近期拒绝电影。
- `recommendation_impressions` 记录曝光，降低短期重复。

### 2.4 语音

- 用户输入区始终可看到语音模式入口。
- 浏览器通过 `MediaRecorder` 录音；豆包不支持的 WebM/MP4 可由后端使用 `ffmpeg` 内存转换为单声道 16 kHz WAV。
- 豆包默认 ASR 资源 ID：`volc.bigasr.auc_turbo`。
- 豆包默认 TTS 资源 ID：`seed-tts-2.0`。
- 默认音色：`zh_female_xiaohe_uranus_bigtts`。
- OpenAI 兼容模式调用 `/v1/audio/transcriptions` 与 `/v1/audio/speech`。
- 原始录音、转换音频和合成音频均不落盘。

## 3. 总体架构与本阶段关键决策

影伴仍是 Python 标准库单进程 HTTP 服务、SQLite 持久化和无前端构建步骤的原生 HTML/CSS/JavaScript 应用。

```text
浏览器聊天页
  ├─ 自动播放偏好 localStorage
  ├─ 语音输入 MediaRecorder
  ├─ AI 文字回复
  ├─ 波形＋时长语音胶囊
  └─ 2:3 海报卡片 + 失败文字封面
          │
          ▼
server.py
  ├─ POST /api/chat
  ├─ POST /api/voice/transcribe
  ├─ POST /api/voice/synthesize
  ├─ 推荐硬排除与安全短路
  └─ 推荐候选海报补全调用
          │
          ├─ agent.py：模型和领域工具
          ├─ catalog.py：本地候选与硬排除
          ├─ integrations.py：TMDB 精确补图 + Tavily/Brave
          ├─ voice.py：豆包/OpenAI 兼容 STT/TTS
          └─ storage.py：电影、账户、历史、配置
```

关键决策：

1. 海报修复同时覆盖数据层和呈现层。只修 CSS 无法解决旧电影没有 URL；只补 URL 无法解决 CDN 失败和错误比例。
2. 旧片补图采用惰性策略，只处理本次推荐中最多三部缺图候选。这样无需人工更新，也避免启动时批量访问 TMDB。
3. 片名标准化函数 `_normalized_movie_title()` 去除非字母数字字符并小写；接受候选至少需要片名完全匹配或年份完全匹配。片名匹配优先于单独年份，最终按匹配分和 TMDB `popularity` 排序。
4. `store.upsert_movies()` 仍是电影事实的唯一持久化入口；补图不创建旁路缓存，不引入新表。
5. 语音胶囊使用原生 `<button>` 控制隐藏 `<audio>`，不移除键盘和读屏器语义；`aria-label` 随播放状态改变。
6. 自动播放偏好属于设备/浏览器交互偏好，不是账户长期电影记忆，所以写入 `localStorage`，不写 SQLite。
7. 默认自动播放是明确产品需求，但浏览器政策高于应用代码；失败时降级到手动播放是运行时契约的一部分。
8. 同一时刻只允许一条语音播放，防止多轮回复同时发声。
9. 静态资源引用增加 `?v=6`，让当前浏览器绕过旧的五分钟静态缓存并加载本阶段样式和脚本；服务端仍以 URL path 解析文件，不改变真实文件名。

## 4. 已实现功能与准确代码入口

### 4.1 缺失海报补全

- `integrations.py::_normalized_movie_title(value)`：生成只含字母数字的小写片名，用于中文名、原名和别名比较。
- `integrations.py::InternetRuntime.hydrate_movie_posters(movies, limit=3)`：
  - TMDB 未启用时原样返回；
  - 已有 `poster_url` 的电影不查询；
  - 查询 `search/movie` 时使用电影标题和可用年份；
  - 过滤没有 `poster_path` 的结果；
  - 本地任一标准化片名与 TMDB `title/original_title` 完全匹配加 8 分；
  - 年份完全匹配加 5 分；
  - 最高分低于 5 不接受；
  - 使用 `tmdb_image_base_url + poster_path` 生成图片 URL；
  - 合并 `external_ids.tmdb`、补充 `source_url` 后调用 `store.upsert_movies()`；
  - 重新读取已保存电影，同时保留推荐阶段临时的 `match_tags`、`match_reason` 和 `match_score`。
- `server.py` 推荐响应分支：在候选确定后、发送响应前调用 `self.app.internet.hydrate_movie_posters(candidates, limit=3)`。

### 4.2 图片加载降级

- `web/app.js::renderMovieCard()` 创建 `.poster-image`，设置 `alt`、`loading="lazy"` 和 `decoding="async"`。
- 初始海报容器为 `.is-loading`；`load` 后切为 `.has-image`；`error` 后移除失败 `<img>` 并添加 `.image-failed`。
- `web/styles.css` 中 `.poster` 使用 `aspect-ratio: 2 / 3`、`overflow:hidden`；`.poster-image` 绝对铺满并 `object-fit:cover`。
- 无 URL 或加载失败时，既有电影标题、年份和类型文字仍作为封面内容保留。

### 4.3 语音回复胶囊

- `web/app.js::attachVoiceReply(message, text)` 请求 `/api/voice/synthesize`，创建 `.voice-reply`、`.voice-reply-button`、四条 `.voice-wave span`、`.voice-reply-duration` 和隐藏 `<audio>`。
- `loadedmetadata` 使用 `Math.ceil(audio.duration)` 显示秒数，例如 `122″`。
- `play` 时按钮增加 `.is-playing` 并把可访问名称改为“暂停阿映的语音回复”；`pause/ended` 时恢复“播放阿映的语音回复，N 秒”。
- 播放中的四条波形使用错开的 `voice-wave` 动画；用户启用减少动态效果时，既有全局 `prefers-reduced-motion` 约束仍适用。
- `web/app.js::playVoiceAudio(audio, button, automatic)` 先暂停其他回复，再播放目标；自动播放异常时添加 `.autoplay-blocked` 并安全返回。

### 4.4 自动播放开关

- `web/index.html#voice-autoplay-toggle` 是标题区域的原生按钮，初始 `aria-pressed="true"`。
- `web/app.js::readVoiceAutoPlayPreference()`：无已保存值时返回 `true`。
- `web/app.js::saveVoiceAutoPlayPreference(value)`：保存到 `yingban.voiceAutoPlay`；存储不可用时无异常中断。
- `web/app.js::updateVoiceAutoPlayToggle()`：同步 `aria-pressed`、`aria-label` 和“自动播放/手动播放”文字。
- `web/app.js::setVoiceAutoPlay(enabled)`：关闭时暂停所有回复；开启时尝试播放最近语音。
- 桌面端显示图标和文字；移动端保留 36×36 图标按钮，仍通过 `aria-label` 提供完整状态。

## 5. Agent 行为、运行时上下文与工具限制

本阶段没有改变系统提示词、Agent Loop 次数、工具 schema、长期记忆规则或安全短路顺序。`AgentRuntime.respond()` 继续根据 `mode` 选择独立提示词，并注入当前电影、候选片单、剧透开关、观影记录等运行时上下文。

当前领域工具共七个：

1. `search_movie(query)`：先查本地真实电影；无结果且 TMDB 可用时查 TMDB 并入库。
2. `get_movie_detail(movie_id)`：读取已收录电影的事实、主题、情绪和内容提醒。
3. `list_watched_movies()`：只读取当前账户的看过列表。
4. `mark_movie_watched(movie_id, source)`：只允许已存在电影；`source` 为 `discussion`、`recommendation_feedback` 或 `chat`。
5. `recommend_movies(request, limit)`：可先调用 TMDB discover 补候选，再由本地推荐器硬排除和排序；`limit` 为 1 至 5。
6. `search_web(query, source, limit)`：`source` 只允许 `web`、`douban`、`zhihu`，`limit` 为 1 至 10；当前通过 Tavily `basic` 执行。
7. `read_public_page(url)`：只允许公开的 `douban.com`、其子域名、`zhihu.com` 和其子域名；拒绝其他域名、带用户名 URL 和非 HTTP(S) 地址。

本阶段的海报补全发生在模型/演示 Agent 已选出候选以后，属于产品数据完善，不向 Agent 暴露新工具，也不允许模型提供任意图片 URL覆盖本地事实。

安全降级：

- TMDB 不可用或补图请求失败：继续返回电影，前端显示文字封面。
- 图片 CDN 加载失败：移除失败图片，电影卡片和操作保持可用。
- 豆包合成失败：移除语音占位，文字回复完整保留。
- 浏览器拒绝自动播放：语音胶囊保持手动可播，不重复自动请求。
- Web Search 不可用：本地电影、TMDB、历史和大模型继续工作。
- 模型不可用：切到演示 Agent；账户、片库、历史和管理能力继续工作。
- 高风险文本：在模型和外部工具前短路，记录最小化 `safety_events`，返回安全支持文本。

## 6. 数据结构与迁移

本阶段没有新增数据库表、字段或索引，也没有破坏性迁移。当前 SQLite 表及意义继续为：

- `accounts`：长期账户与状态。
- `invite_codes`：邀请码摘要、提示片段、状态、账户绑定和换发关系。
- `sessions`：普通用户会话摘要、账户与过期时间。
- `admin_sessions`：管理员会话摘要与过期时间。
- `movies`：统一电影事实记录，包含别名、年份、主创、地区、类型、主题、情绪、内容提醒、摘要、来源、海报、来源链接和外部 ID。
- `user_movie_states`：账户级 `watched`、`watchlist`、`disliked` 状态、来源、评分和备注。
- `recommendation_impressions`：推荐曝光，支持短期去重。
- `safety_events`：最小化高风险事件类别，不保存完整敏感对话。
- `app_settings`：运行时配置键值；密钥保存在本机数据库中，因此文件权限必须仅当前系统用户可读写。

数据变化：

- 旧 `movies.poster_url` 为空时，可在该电影进入推荐结果时被补写。
- 可能同时补写 `movies.source_url` 和 `movies.external_ids_json` 中的 `tmdb` ID。
- 旧数据完全兼容；补图是增量更新，不删除片名、摘要、标签、状态或曝光。
- 回滚前端不会丢失补到的海报 URL；若未来认为匹配错误，需要针对具体 `movie_id` 更正，不能批量清空所有海报。
- 自动播放偏好不在数据库中，而在每个浏览器的 `localStorage`；清理站点数据会恢复默认 `true`。

## 7. 接口变化与契约

本阶段没有新增或删除 HTTP 路径。

### 7.1 `POST /api/chat`

既有请求字段、认证和模式不变。推荐模式响应中的 `movies[]` 仍使用统一电影结构，但本阶段在返回前可能补齐：

```json
{
  "poster_url": "https://image.tmdb.org/t/p/w500/...jpg",
  "source_url": "https://www.themoviedb.org/movie/123",
  "external_ids": {"tmdb": 123}
}
```

补图失败不把整个聊天请求变为错误；返回原候选和空 `poster_url`，由前端文字封面降级。

### 7.2 `POST /api/voice/synthesize`

接口和返回格式不变。前端继续提交 AI 回复文字，后端返回可播放的音频 Blob。变化只在消费方式：原生播放器替换为自定义语音胶囊，并根据本地偏好尝试自动播放。

### 7.3 `POST /api/voice/transcribe`

接口和录音格式转换不变。用户语音输入仍需浏览器麦克风授权；本阶段没有自动申请权限或改变录音持久化边界。

### 7.4 管理接口

`/api/admin/internet-config`、`/api/admin/internet-config/test`、`/api/admin/voice-config`、`/api/admin/voice-config/test` 均无契约变化。管理员后台不新增自动播放配置，因为该偏好属于用户设备交互选择。

## 8. 页面与视觉变化

### 8.1 聊天标题区

- 原剧透复选框保留。
- 新增 `.chat-preferences` 容器，统一承载剧透控制和语音自动播放按钮。
- 自动播放开启时使用酒红底、浅色图标和“自动播放”；关闭时使用米白底、酒红描边倾向和“手动播放”。
- 手机窄屏隐藏文字但保留图标和可访问名称，不与标题或返回按钮重叠。

### 8.2 AI 回复气泡

- 有语音的回复气泡增加顶部空间，语音胶囊悬浮在左上角，与参考图的功能布局一致。
- 胶囊为深色圆角形，显示波形和秒数；不照搬参考图的背景、字体、底部导航或二次元视觉风格。
- 播放时波形动画，暂停时静止；焦点样式清晰可见。

### 8.3 电影卡片

- 海报固定 2:3，三列卡片具有一致高度。
- 真实图片加载成功后覆盖文字封面；加载失败仍显示标题、年份、类型和渐变底色。
- 真实浏览器截图确认三张海报并排显示且没有破图。

## 9. 配置变化

本阶段没有新增服务器环境变量或 `app_settings` 键。

继续使用的关键配置：

```text
model_provider=anthropic
model_id=deepseek-v4-flash
tmdb_api_key=<secret>
tmdb_base_url=https://api.themoviedb.org/3
tmdb_image_base_url=https://image.tmdb.org/t/p/w500
web_search_provider=tavily
web_search_base_url=https://api.tavily.com
web_search_api_key=<secret>
voice_provider=doubao
voice_base_url=https://openspeech.bytedance.com
voice_stt_model=volc.bigasr.auc_turbo
voice_tts_model=seed-tts-2.0
voice_name=zh_female_xiaohe_uranus_bigtts
voice_audio_format=mp3
voice_api_key=<secret>
```

浏览器侧新增：

```text
localStorage["yingban.voiceAutoPlay"] = "true" | "false"
```

定义：缺失键代表默认开启；`"false"` 代表手动播放；其他值按开启处理。读写 `localStorage` 失败时前端继续工作，并使用本次页面内存状态。

## 10. 安全、隐私与可访问性边界

- 本阶段没有读取、输出或写入真实 API 密钥到代码、测试、截图或文档。
- 补图只向已配置 TMDB 发送电影标题和年份，不发送账户身份、对话全文、观影历史或邀请码。
- 语音合成仍会把 AI 回复文字发送给管理员配置的语音供应商；这是既有语音功能的数据边界。
- 用户录音、转换音频和合成音频不写 SQLite，不生成项目内永久文件。
- `localStorage` 只保存一个布尔型播放偏好，不保存音频、聊天内容、账户 ID 或敏感信息。
- 自动播放开关是原生按钮，具备 `aria-pressed`、动态 `aria-label`、可见焦点和最小可点击面积。
- 语音胶囊是原生按钮，播放、暂停和时长对读屏器可读；隐藏 `<audio>` 不作为唯一操作入口。
- 自动有声播放可能打扰用户，因此必须保留醒目的关闭开关、持久化选择和立即暂停行为。不得移除开关或把关闭入口藏入管理员页。
- 浏览器阻止自动播放是平台安全策略；产品必须接受降级，不得模拟用户手势、循环调用或诱导绕过。

## 11. 修改文件清单

- `integrations.py`
  - 新增 `_normalized_movie_title()`。
  - 新增 `InternetRuntime.hydrate_movie_posters()`。
- `server.py`
  - 推荐候选返回前调用缺失海报补全。
- `web/index.html`
  - 新增聊天偏好容器和 `#voice-autoplay-toggle`。
  - 静态资源查询版本更新为 `v=6`。
- `web/app.js`
  - 新增自动播放偏好读取、保存和 UI 同步。
  - 新增图片加载成功/失败状态。
  - 重做 `attachVoiceReply()`，新增 `playVoiceAudio()` 和单音频互斥。
- `web/styles.css`
  - 新增自动播放开关、语音胶囊、波形动画和响应式样式。
  - 固定电影海报 2:3 比例并加入加载渐显。
- `tests/test_product.py`
  - 新增旧片缺图 TMDB 补全和持久化测试。
  - 新增语音胶囊与自动播放偏好结构测试。
- `README.md`
  - 更新海报降级、语音胶囊、自动播放边界和 21 项测试基线。
- `第六阶段技术开发文档.md`
  - 本阶段权威交接文档。

## 12. 测试与验证结果

### 12.1 静态与自动化测试

执行命令：

```bash
cd products/yingban
python3 -m py_compile integrations.py server.py tests/test_product.py
node --check web/app.js
python3 -m unittest discover -s tests -v
```

结果：

```text
Ran 21 tests in 4.081s
OK
```

本阶段新增的关键断言：

- 缺海报种子电影调用 TMDB `search/movie`。
- 标题和年份匹配的结果生成正确 `poster_url`。
- `external_ids.tmdb` 被合并并持久化。
- 再次从存储读取时海报仍存在。
- HTML 存在 `#voice-autoplay-toggle` 且初始 `aria-pressed="true"`。
- JavaScript 使用 `yingban.voiceAutoPlay`，包含自定义 `.voice-reply-button` 和自动播放调用。

### 12.2 真实浏览器功能验证

环境：本地服务 `http://127.0.0.1:8765/`，已登录测试账户，当前真实配置启用模型、TMDB、Tavily 和豆包。

步骤与结果：

1. 进入“下一部电影”，标题右侧显示“自动播放”，`aria-pressed=true`。
2. 点击一次变为“手动播放”，`aria-pressed=false`；再次点击恢复 `true`。
3. 输入“推荐三部适合周末看的科幻电影，要显示海报。”
4. 响应返回《流浪地球》《星辰之怒》《Spider-Man: No Way Home》三张卡片。
5. DOM 中 `.poster-image` 数量为 3；三张图片均 `complete=true`、`naturalWidth=500`，来源均为 `https://image.tmdb.org/t/p/w500/...`。
6. 豆包语音合成完成，胶囊显示 `122″`。
7. 本次异步生成后的自动播放被浏览器策略拦截，按钮保持“播放阿映的语音回复，122 秒”。
8. 用户手势点击胶囊后按钮立即变为“暂停阿映的语音回复”，证明音频可播放且播放状态同步成功。
9. 验收结束时已暂停测试音频，自动播放偏好恢复为开启。

结论：三项需求均已实现。自动播放的产品默认值和代码调用正确；是否真正自动发声仍由浏览器的有声媒体策略决定，这一限制已经通过可点击胶囊降级处理。

## 13. 启动与使用方法

### 13.1 启动

```bash
cd products/yingban
python3 app.py serve
```

打开：

- 用户端：`http://127.0.0.1:8765/`
- 管理后台：`http://127.0.0.1:8765/admin`

### 13.2 验证海报

1. 登录用户端并进入“下一部电影”。
2. 请求三部电影，至少包含一部原本缺少海报的种子电影。
3. 检查卡片海报是否保持 2:3。
4. 模拟图片网络失败时，应看到文字封面而不是破图。
5. 再次请求同一电影时，已补海报应直接从 `movies` 读取。

### 13.3 验证语音

1. 管理后台确认“语音交互”显示豆包已接入。
2. 用户端标题右侧应显示“自动播放”。
3. 发送任意消息并等待文字和语音生成。
4. 胶囊应显示波形与秒数；自动播放成功时显示暂停状态。
5. 若浏览器阻止自动播放，点击胶囊应立即播放。
6. 点击标题按钮改为“手动播放”，下一条回复不应自动播放；刷新后仍应保持手动状态。

## 14. 尚未完成事项与已知风险

### 14.1 尚未完成

- 尚未在多台真实手机和 Safari 完成自动播放策略矩阵测试。
- 自动播放偏好目前按浏览器保存，不在同一账户的不同设备之间同步。
- 旧片海报只在进入推荐候选时惰性补全，没有管理员“一键扫描全部缺图电影”的批处理入口。
- 没有后台查看或人工纠正 TMDB 海报匹配的专用页面。
- AI 文字中模型可能输出 Markdown 海报链接；当前对话气泡继续按纯文本显示，真正海报以结构化电影卡片为准。本阶段未引入 Markdown 渲染器。
- 真实豆瓣、知乎页面的可读性仍受目标站点公开访问策略和 Tavily 索引覆盖影响。

### 14.2 已知风险

- 有声自动播放不是 Web 应用可完全保证的能力。尤其在异步 TTS 完成时，最初用户手势的激活窗口通常已经失效。
- 仅按年份匹配可以达到当前阈值 5；极端情况下同年同名信息不足可能错配。下一阶段若扩大批量补图，应提高阈值或增加人工确认。
- TMDB 海报 URL 是外部资源；网络、地区、CDN 或内容下架都会触发文字封面降级。
- 惰性补图会为首次命中的缺图候选增加外部请求延迟；当前最多三部，不能在未评估额度和延迟前无限扩展。
- `localStorage` 可被用户清理或浏览器隐私模式隔离，清理后会恢复自动播放默认开启。
- 当前 HTTP 本地模式未启用 `Secure` Cookie；公开部署必须放在 HTTPS 反向代理后并开启 `YINGBAN_COOKIE_SECURE`。

## 15. 下一阶段建议

1. 做 Chrome、Safari、iOS Safari 和 Android WebView 的语音自动播放矩阵，记录“首次进入、首次点击、录音授权后、连续对话”四种场景。
2. 与用户决定自动播放偏好是否需要账户级跨设备同步；若需要，应新增独立的用户偏好数据结构，而不是混入观影记忆。
3. 为管理后台增加缺图电影列表、候选海报预览和人工确认，避免未来批量补图错配。
4. 把模型正文中的电影引用改为结构化引用或安全 Markdown 子集，避免显示原始 `[海报](URL)` 文本，同时继续以卡片为海报权威呈现。
5. 增加图片代理或缩略图缓存前，先完成版权、TMDB attribution、带宽和缓存失效策略评审。
6. 对语音生成的长回复增加长度上限或摘要 TTS 模式，避免 122 秒以上音频带来的等待和费用；此决策会改变产品体验，需用户确认。
7. 增加真实浏览器端到端回归，至少覆盖：开关持久化、自动播放失败降级、手动播放、单音频互斥、海报成功、海报失败和移动端布局。

## 16. AI 接手入口

下一次迭代的 AI 必须按以下顺序工作：

1. 阅读仓库根目录 `AGENTS.md`。
2. 完整阅读本文件，不删除其中的定义、契约、风险或意义。
3. 只读检查 `integrations.py::InternetRuntime.hydrate_movie_posters()`、`server.py` 推荐分支、`web/app.js::attachVoiceReply()`、`playVoiceAudio()`、`setVoiceAutoPlay()` 和相关 CSS。
4. 执行 21 项测试并记录真实数量；若数量变化，以代码和测试结果为准。
5. 只读确认数据库电影数量和配置启用状态；不得输出完整密钥、令牌、邀请码或会话。
6. 做一次真实推荐和语音播放验证；把浏览器自动播放成功与“代码已调用 play()”区分记录。
7. 若开发新的边界明确阶段，创建 `第七阶段技术开发文档.md`，不得覆盖本文件。
