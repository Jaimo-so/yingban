# 第四阶段技术开发文档

> AI 读取定位：这是当前最新、当前权威的版本迭代交接文档。后续迭代必须先读根目录 `AGENTS.md` 和本文件；只有追溯邀请码、双 Agent、联网电影或 OpenAI 兼容语音方案的设计来源时，才读取第一、第二、第三阶段文档。当前代码和真实运行状态高于任何历史快照。

## 0. AI 快速接手卡

```text
DOCUMENT_ROLE=current_authoritative_handoff
CURRENT_STAGE=4
STATUS=completed_code_and_local_verification_waiting_for_doubao_credentials
CODE_ROOT=products/yingban
PRODUCT_START=products/yingban/app.py
HTTP_ROUTING=products/yingban/server.py
AGENT_RUNTIME=products/yingban/agent.py
PERSISTENCE=products/yingban/storage.py
OFFLINE_MOVIE_RETRIEVAL=products/yingban/catalog.py
LIVE_MOVIE_AND_WEB=products/yingban/integrations.py
VOICE_RUNTIME=products/yingban/voice.py
USER_UI=products/yingban/web/index.html + app.js + styles.css
ADMIN_UI=products/yingban/web/admin.html + admin.js + admin.css
TESTS=products/yingban/tests/test_product.py
START_COMMAND=cd products/yingban && python3 app.py serve
TEST_COMMAND=cd products/yingban && python3 -m unittest discover -s tests -v
EXPECTED_TEST_BASELINE=18 tests passing
CURRENT_DATABASE_MODE=model
CURRENT_MODEL_ID=deepseek-v4-flash
CURRENT_TMDB_MODE=disabled_missing_credential
CURRENT_WEB_SEARCH_MODE=disabled_missing_credential
CURRENT_VOICE_PROVIDER=doubao
CURRENT_VOICE_MODE=disabled_missing_credential
CURRENT_MOVIE_COUNT=24
RUNTIME_EXTRA=ffmpeg required when browser recording is WebM/MP4
NEXT_DOCUMENT=products/yingban/第五阶段技术开发文档.md
```

### 0.1 当前实现一句话

影伴是邀请码制 AI 电影伙伴：保留双 Agent、观影记录、推荐硬去重和高风险短路；可通过 TMDB 自动补充新电影与海报，通过 Web Search 查询公开的全网、豆瓣和知乎资料；用户聊天页始终显示麦克风入口，配置豆包后可以按住说话，经录音文件极速识别转成文字，再由豆包语音合成模型 2.0 为 AI 文字回复生成用户主动播放的语音。

### 0.2 当前完成状态

- 豆包成为代码和新安装环境的默认语音供应商。
- 豆包新版控制台 App Key 和旧版控制台 App ID + Access Token 两种鉴权方式均已实现。
- 豆包 STT 使用一次请求返回结果的录音文件极速识别；TTS 使用 V3 单向流式 HTTP 接口。
- 浏览器录音不受豆包格式支持时，后端通过 `ffmpeg` 内存管道转换为单声道 16 kHz WAV，不写临时文件。
- 用户聊天页的麦克风切换按钮不再因缺少语音密钥而消失。未配置时按钮仍可发现，进入语音模式后显示明确的配置提示。
- 后台语音区域可以选择“豆包语音”或“OpenAI 兼容”，会显示对应字段名称和默认值。
- 18 项自动测试全部通过；前台、后台浏览器实机检查通过；两个页面均无控制台错误。
- 本地服务已经重启并加载当前代码，地址为 `http://127.0.0.1:8765/`，当前大模型运行模式是 `model`。
- 没有真实豆包 App Key，因此只完成协议构造、密钥掩码、模拟响应解析和界面验收，真实 STT/TTS 最终验收仍待用户提供凭据。

### 0.3 不可破坏的产品约束

- 一个邀请码只对应一个长期账户；同一码再次登录必须返回原账户。
- 邀请码完整正文不得写入数据库日志、测试输出或阶段文档。
- 用户在讨论中确认看过某电影后自动写入 `watched`，不重复征求同意。
- 推荐结果必须在数据检索层排除 `watched`；不能只依赖提示词。TMDB 动态电影也必须经过相同硬排除。
- “聊电影”和“找电影”使用独立完整系统提示词，但共享身份、安全、隐私、事实和长期记忆边界。
- Agent 不得冒充真人或虚构真人经历；用户明确询问身份时必须透明说明自己是 AI。
- 现实烦恼、心理状态和敏感经历不得自动写入长期记忆。
- 高风险自伤或伤人文本必须在普通电影 Agent 和外部工具调用前由产品代码短路。
- 管理配置读取接口不得返回完整 API 密钥、Access Token、管理员口令或会话令牌。
- 录音、录音 Base64、转换后的 WAV 和合成音频不得写入长期数据库或磁盘。
- 浏览器不得自动播放有声回复；必须由用户主动点击播放。
- 豆瓣和知乎能力只允许公开页面；不得自动登录、提交 Cookie、绕过验证码、绕过付费墙、绕过访问控制或读取私密内容。
- 外部电影必须转换为统一电影记录并写入 `movies` 后，才能进入观影状态、推荐和曝光链路。
- 旧账户、邀请码绑定、观影历史、推荐曝光、管理员模型配置和旧 OpenAI 语音配置在升级时不得丢失。
- 麦克风入口不能再以“未配置”为理由从页面完全隐藏；不可用原因必须有可发现、可读的状态提示。

### 0.4 下一阶段建议的第一步检查

1. 运行 18 项测试，确认基线没有回归。
2. 访问 `GET /api/health`，确认模型模式和服务可用。
3. 登录 `/admin`，检查语音供应商为 `doubao`，不要读取或输出完整凭据。
4. 请用户提供豆包新版 App Key，或旧版 App ID + Access Token，并确认已经开通 `volc.bigasr.auc_turbo` 与 `seed-tts-2.0`。
5. 先执行“测试语音合成”，再用真实麦克风完成一次 3 至 10 秒中文录音转写。
6. 验证默认音色 `zh_female_xiaohe_uranus_bigtts` 是否与账号开通资源匹配；不匹配时根据控制台音色列表替换，不能猜测 ID。

## 1. 文档信息与阶段编号说明

- 产品名称：影伴。
- 阶段名称：第四阶段——豆包语音正式适配与麦克风入口修复。
- 阶段完成日期：2026-08-21。
- 继承阶段：第三阶段——实时电影知识、公开 Web 检索、真实海报与 OpenAI 兼容双向语音骨架。
- 下一阶段文档：`第五阶段技术开发文档.md`。
- 本阶段边界：解决“已决定使用豆包，但用户前端看不到语音输入按钮”，并把语音运行时从单一 OpenAI 兼容协议扩展为豆包原生协议；不实现全双工实时通话。

## 2. 需求背景与根因

用户确认采用豆包语音后，在聊天页面发现输入区只有文字输入和发送按钮，没有可见的语音入口。

只读检查确认第三阶段已经实现：

- `index.html` 中存在 `#voice-mode-button`、`#voice-composer` 和 `#hold-to-talk`。
- `app.js` 已实现 `MediaRecorder` 录音、松开发送、`Esc` 取消、转写发送和 AI 回复语音播放。
- `server.py` 已实现 `/api/voice/transcribe` 与 `/api/voice/synthesize`。
- `voice.py` 只实现 OpenAI 兼容 `/v1/audio/transcriptions` 和 `/v1/audio/speech`。

实际问题由两个条件共同造成：

1. `GET /api/me` 只有在语音密钥和模型全部配置后才返回 `voice_available=true`。
2. `showAuthenticated()` 使用 `voice_available` 直接设置 `#voice-mode-button.hidden`，导致服务未配置时整个麦克风入口消失。

这不仅是配置问题，也是产品可发现性问题：管理员已经计划接入语音，但用户无法知道功能存在；缺少凭据与浏览器不支持录音也被同一种“隐藏”行为吞掉。

## 3. 阶段目标与完成定义

本阶段闭环：

```text
管理员打开语音设置
→ 选择豆包语音
→ 新版控制台填写 App Key，或旧版填写 App ID + Access Token
→ 使用默认 ASR/TTS 资源 ID 与音色 ID
→ 测试语音合成并保存
→ 用户聊天页始终看见麦克风按钮
→ 点击后切换到“按住说话”
→ 浏览器录音；必要时后端内存转换为 WAV
→ 豆包极速识别返回 result.text
→ 转写文字进入原有 /api/chat
→ AI 文字回复先显示
→ 豆包 TTS 数据帧解码为音频
→ 用户主动点击播放器收听
```

完成定义：

- 未配置语音时，麦克风入口可见，不再隐藏。
- 未配置时进入语音模式，显示“豆包语音服务待管理员填写 App Key 后启用”，录音按钮为禁用状态。
- 浏览器不支持 `MediaRecorder` 或 `getUserMedia` 时，麦克风入口可见但禁用，并有说明。
- 配置后保持第三阶段原有按住录音、松开发送、键盘录音和取消行为。
- 豆包凭据只由后端使用，管理读取接口只返回末四位掩码。
- 豆包请求使用官方资源 ID Header、随机请求 ID 和正确请求体。
- TTS 连续 JSON 数据帧按 JSON 边界解析，不能按换行分割。
- 旧 OpenAI 兼容配置继续可用。
- 所有原有 16 项测试保留，并新增豆包协议与入口可见性测试，最终 18 项通过。

## 4. 当前总体架构

影伴仍是 Python 标准库单进程 HTTP 服务与 SQLite：

1. `web/index.html`、`app.js`、`styles.css`：登录、首页、双聊天入口、麦克风、录音状态、语音播放器、推荐卡、电影海报和历史片单。
2. `web/admin.html`、`admin.js`、`admin.css`：大模型、联网电影、Web Search、语音供应商、双 Agent 提示词和邀请码管理。
3. `server.py`：会话、鉴权、JSON 输入限制、安全短路、外部补全编排、语音用户接口和管理接口。
4. `agent.py`：两套独立系统提示词、最多八轮工具调用和七个领域工具。
5. `catalog.py`：离线种子片库搜索、文本电影识别、情绪/类型匹配和推荐硬去重。
6. `integrations.py`：TMDB、Brave Web Search 和受限公开页面阅读。
7. `voice.py`：`openai_compatible` 与 `doubao` 两个语音适配器、凭据选择、音频格式转换和 TTS 数据帧解码。
8. `storage.py`：SQLite 账户、邀请码、会话、电影、观影状态、推荐曝光和管理员配置。
9. 外部运行时：豆包模式遇到 WebM/MP4 录音时调用系统 `ffmpeg`，只使用标准输入和标准输出。

## 5. 继承的电影、Web Search 与海报能力

### 5.1 本地优先、外部补全

`GET /api/movies?query=...` 和 Agent 工具 `search_movie` 先执行 `MovieCatalog.search()`。本地无结果且 TMDB 已配置时才调用 `InternetRuntime.search_movies()`。

相同外部电影首次查询后写入 SQLite，后续可以本地命中。讨论模式优先识别书名号、刚看完/看完/看了后的片名；推荐模式把自然语言类型、情绪和“新片”等条件映射为 TMDB 发现参数，但否定表达仍由本地推荐层过滤。

动态候选进入 `MovieCatalog.recommend()` 后必须：排除 `Store.watched_ids(account_id)`、默认排除近期曝光、按类型/主题/情绪/内容提醒匹配，并写入 `recommendation_impressions`。

### 5.2 TMDB 统一电影字段

- `id`：`tmdb-{TMDB movie id}`。
- `title_zh`：中文标题。
- `title_original`：原始标题。
- `year`：上映日期年份，缺失为 `0`。
- `regions`：`origin_country`。
- `genres`：TMDB genre ID 转中文类型。
- `summary`：`overview`，最多 4000 字符。
- `poster_url`：`tmdb_image_base_url + poster_path`。
- `source_url`：`https://www.themoviedb.org/movie/{id}`。
- `external_ids`：`{"tmdb": "..."}`。
- `source`：`tmdb`。

搜索/发现响应仍可能缺导演和别名；不能在列表请求中虚构这些字段。

### 5.3 Web Search、豆瓣与知乎

Agent 领域工具保持七个：`search_movie`、`get_movie_detail`、`list_watched_movies`、`mark_movie_watched`、`recommend_movies`、`search_web`、`read_public_page`。

`search_web(query, source, limit)` 的 `source` 只允许 `web`、`douban`、`zhihu`。豆瓣增加 `site:movie.douban.com`，知乎增加 `site:zhihu.com`。返回 `title`、`url`、`snippet`、`source`，摘要不能直接当确定事实。

`read_public_page` 只允许 `douban.com`、其子域名、`zhihu.com` 和其子域名，只允许 HTTP/HTTPS，禁止 URL 用户名，默认最多 12000 字符，硬上限 30000 字符。阅读器不接收 Cookie、不登录、不处理验证码、不绕过访问控制。

Web Search 供应商仍按 Brave `/web/search` + `X-Subscription-Token` 实现；公开页面阅读器默认 `https://r.jina.ai`。两者未配置或失败时，离线电影能力继续工作。

### 5.4 海报

真实海报来自 TMDB `poster_path`，默认根地址 `https://image.tmdb.org/t/p/w500`。`posterNode(movie)` 被推荐卡和历史片单复用；图片带电影标题 `alt`、延迟加载和 `no-referrer`。加载失败时移除图片并恢复年份+片名文字海报，不能显示空白卡。

当前 24 部种子电影仍未批量回填真实海报；只有 TMDB 搜索/发现得到的电影天然带海报。

## 6. 豆包语音架构与关键决策

### 6.1 供应商抽象

`VoiceConfig.provider` 允许：

- `doubao`：本阶段默认。
- `openai_compatible`：保留第三阶段兼容方案。

`VoiceConfig.enabled` 不再只判断单个 API Key：

- OpenAI 兼容：要求 `api_key`、STT 模型、TTS 模型和音色。
- 豆包新版控制台：要求 `api_key`、STT 资源 ID、TTS 资源 ID和音色 ID。
- 豆包旧版控制台：可用 `app_id + access_token` 代替 `api_key`。

保存配置后数据库优先于 `.env`。没有 `voice_provider` 的旧数据库配置，如果 `voice_base_url` 是 `https://api.openai.com`，运行时推断为 `openai_compatible`，避免升级后把旧密钥错误当成豆包 App Key。

### 6.2 豆包 STT 契约

端点：

```text
POST {voice_base_url}/api/v3/auc/bigmodel/recognize/flash
```

默认：

```text
voice_base_url=https://openspeech.bytedance.com
voice_stt_model=volc.bigasr.auc_turbo
```

新版控制台 Header：

```text
Content-Type: application/json
X-Api-Key: {voice_api_key}
X-Api-Resource-Id: {voice_stt_model}
X-Api-Request-Id: {uuid4}
X-Api-Sequence: -1
```

旧版控制台把 `X-Api-Key` 替换为：

```text
X-Api-App-Key: {voice_app_id}
X-Api-Access-Key: {voice_access_token}
```

请求体：

```json
{
  "user": {"uid": "App ID 或 yingban"},
  "audio": {"data": "Base64 音频"},
  "request": {
    "model_name": "bigmodel",
    "enable_itn": true,
    "enable_punc": true
  }
}
```

成功正文从 `result.text` 读取，去除首尾空白后最多返回 6000 字符。没有文本时返回“没有识别到可发送的文字”。

官方参考：`https://www.volcengine.com/docs/6561/1631584?lang=zh`。

### 6.3 浏览器录音兼容

浏览器优先尝试 `audio/webm;codecs=opus`、`audio/webm`、`audio/mp4`、`audio/ogg;codecs=opus`。豆包录音文件识别官方主要支持 WAV、MP3 和 Ogg/Opus，因此：

- `audio/wav`、`audio/x-wav`、`audio/mpeg`、`audio/mp3`、`audio/ogg` 直接发送。
- 其他类型调用 `shutil.which("ffmpeg")` 查找运行时。
- 转换命令参数固定为：`-i pipe:0 -vn -ac 1 -ar 16000 -f wav pipe:1`。
- 使用 `subprocess.run` 的 `input` 和 `stdout`，不创建文件，不使用 shell。
- 转换超时 20 秒，输出仍受 10 MB 限制。
- 缺少 `ffmpeg`、转换失败或超时时返回可理解错误。

该转换只是格式兼容，不写入 SQLite，不写临时音频，不改变长期记忆规则。

### 6.4 豆包 TTS 契约

端点：

```text
POST {voice_base_url}/api/v3/tts/unidirectional
```

默认：

```text
voice_tts_model=seed-tts-2.0
voice_name=zh_female_xiaohe_uranus_bigtts
voice_audio_format=mp3
```

鉴权 Header 与 STT 相同，但不发送 `X-Api-Sequence`。`X-Api-Resource-Id` 使用 TTS 资源 ID。

请求体：

```json
{
  "user": {"uid": "App ID 或 yingban"},
  "req_params": {
    "text": "AI 回复文字",
    "speaker": "zh_female_xiaohe_uranus_bigtts",
    "audio_params": {
      "format": "mp3",
      "sample_rate": 24000
    }
  }
}
```

页面选择 `opus` 时请求值转换为 `ogg_opus`。豆包模式只允许 `mp3` 或 `opus`；`wav` 不用于当前流式协议，`aac` 不受当前豆包契约支持。OpenAI 兼容模式仍允许 MP3、WAV、Opus、AAC。

豆包响应是 `text/plain` 中连续拼接、未必换行的 JSON 对象。`_decode_doubao_tts()` 使用 `json.JSONDecoder.raw_decode()` 从当前位置逐帧解析；每个 `data` 字段 Base64 解码后按顺序拼接。非 `0`、`20000000` 的错误码转为运行时错误。不能改成 `splitlines()`。

TTS 资源 ID 必须与音色系列匹配。默认 `*_uranus_bigtts` 对应 `seed-tts-2.0`；错误组合通常返回资源与音色不匹配，不能自动猜测替换。

官方参考：`https://www.volcengine.com/docs/6561/1598757` 和 `https://www.volcengine.com/docs/6561/1257544`。

### 6.5 OpenAI 兼容回退契约

STT 保留：

```text
POST {voice_base_url}/v1/audio/transcriptions
Authorization: Bearer {voice_api_key}
multipart fields: file, model, language=zh
```

TTS 保留：

```text
POST {voice_base_url}/v1/audio/speech
Authorization: Bearer {voice_api_key}
JSON fields: model, voice, input, response_format
```

旧默认示例为 `whisper-1`、`gpt-4o-mini-tts`、`alloy`。管理员选择 OpenAI 兼容时后台会自动恢复这些建议值。

## 7. 用户页面变化

### 7.1 麦克风入口常显

`index.html` 删除 `#voice-mode-button` 的 `hidden` 属性，并增加：

- `aria-controls="text-composer voice-composer"`。
- `#voice-availability-status`，`role=status`、`aria-live=polite`，视觉隐藏但为辅助技术播报。

`showAuthenticated(me)` 分开维护：

- `state.voiceConfigured`：后端语音是否已经配置。
- `state.voiceSupported`：浏览器是否同时支持 `MediaRecorder` 与 `navigator.mediaDevices.getUserMedia`。

麦克风按钮始终存在。浏览器不支持录音时按钮禁用，`title` 为“当前浏览器不支持录音”。

### 7.2 未配置状态

浏览器支持录音但后端未配置时：

1. 用户仍能点击麦克风切换按钮。
2. 文字输入区隐藏，语音输入区显示。
3. “按住说话”按钮禁用。
4. 可见状态为“豆包语音服务待管理员填写 App Key 后启用”。
5. 辅助技术播报“语音输入入口已显示，但豆包语音服务尚未配置”。
6. 再次点击麦克风可以返回文字输入。

这样既不请求无效服务，也不会让用户误以为产品没有语音功能。

### 7.3 已配置录音与回复

- 鼠标、触摸或手写笔按下开始录音，松开结束并发送。
- 键盘按住空格或 Enter 录音，松开结束；`Esc` 取消。
- 状态每 0.2 秒更新，最长 60 秒。
- 小于 0.5 秒或没有数据块不发送。
- 音频以 Base64 JSON 发送 `/api/voice/transcribe`。
- 转写文字作为真实用户消息进入 `/api/chat`，用户气泡显示时长与“语音已转写”。
- AI 文字回复立即显示；语音配置有效时异步调用 `/api/voice/synthesize`。
- 合成成功后显示原生 `<audio controls>`，不自动播放；失败时移除语音区，文字回复不受影响。

## 8. 管理页面变化

“语音交互”新增 `#voice-provider`：

- `doubao`：豆包语音（推荐）。
- `openai_compatible`：OpenAI 兼容。

豆包默认字段：

- 根地址：`https://openspeech.bytedance.com`。
- 识别资源 ID：`volc.bigasr.auc_turbo`。
- 合成资源 ID：`seed-tts-2.0`。
- 音色：`zh_female_xiaohe_uranus_bigtts`。
- 格式：MP3。

新版凭据使用 App Key；旧版凭据放在可展开区域，包含 App ID 与 Access Token。Access Token 读取时只返回末四位掩码，留空表示保留，复选框可以显式清除。

供应商切换时：

- 更新字段标签和帮助文案。
- 更新对应默认根地址、模型/资源 ID 和音色。
- 豆包模式禁用 AAC 选项，不受支持的已选格式自动回到 MP3。
- 不提交表单，只改变当前页面草稿；只有“保存语音配置”才写数据库。

“测试语音合成”继续只合成“连接成功”，不会保存测试音频。保存后配置立即生效，不需要重启。

## 9. Agent 行为、提示词与工具边界

本阶段没有修改双 Agent 系统提示词、工具数量或长期记忆逻辑。

“聊电影 Agent”继续负责接住具体观后感、遵守剧透设置、在资料不足时调用电影或公开搜索工具，并在用户确认看完电影时调用 `mark_movie_watched`。

“找电影 Agent”继续只从产品候选推荐，候选已硬排除看过与近期拒绝；Web 摘要不能直接变成推荐电影。

运行时上下文继续注入：当前模式、剧透状态、确认电影、是否已自动写入看过、去重候选片单和避免重复工具调用。工具循环最多八轮。

语音只改变输入输出媒介：STT 得到的文字进入原聊天链路；TTS 只读取最终 AI 回复文字。语音服务无权修改工具限制、事实边界、长期记忆或高风险降级规则。

## 10. 数据结构与配置变化

### 10.1 业务数据表

本阶段没有新增或修改业务表、字段、索引或外键，不需要 SQL 数据迁移。

当前关键表语义保持：

- 邀请码/账户/会话表维护邀请码绑定与登录状态。
- `movies` 保存统一电影记录。
- 观影状态表保存 `watched`、`watchlist`、`disliked`。
- `recommendation_impressions` 保存推荐曝光，用于近期去重。
- `app_settings` 以键值方式保存管理员运行配置。

`movies` 第三阶段已有字段继续保留：

```sql
poster_url TEXT NOT NULL DEFAULT ''
source_url TEXT NOT NULL DEFAULT ''
external_ids_json TEXT NOT NULL DEFAULT '{}'
```

`Store.initialize()` 仍通过 `PRAGMA table_info(movies)` 检查缺失字段并单列 `ALTER TABLE`，不重建表、不删除旧数据。`upsert_movies()` 对空海报、空来源和空外部 ID 保留数据库原值。

### 10.2 新增语音配置键

`app_settings` 新增逻辑键：

- `voice_provider`：`doubao` 或 `openai_compatible`。
- `voice_app_id`：豆包旧版控制台 App ID。
- `voice_access_token`：豆包旧版控制台 Access Token，敏感。

第三阶段键继续保留：

- `voice_api_key`：豆包新版 App Key 或 OpenAI 兼容 API Key，敏感。
- `voice_base_url`。
- `voice_stt_model`：豆包模式表示 STT Resource ID，OpenAI 模式表示模型 ID。
- `voice_tts_model`：豆包模式表示 TTS Resource ID，OpenAI 模式表示模型 ID。
- `voice_name`：豆包 speaker ID 或 OpenAI voice。
- `voice_audio_format`。
- `voice_timeout_seconds`。

### 10.3 其他现有配置键

模型配置继续包括 provider、API Key、model ID、base URL、timeout、max tokens、temperature；提示词配置继续独立保存 discussion 和 recommendation。

联网配置继续包括：`tmdb_api_key`、`tmdb_base_url`、`tmdb_image_base_url`、`web_search_api_key`、`web_search_base_url`、`web_reader_enabled`、`web_reader_base_url`。

### 10.4 旧数据兼容与回滚

- 数据库不存在 `voice_provider` 时，旧 OpenAI 官方根地址会推断为 `openai_compatible`。
- 其他没有供应商字段的环境使用新的豆包默认值。
- 新代码继续识别旧语音键，不要求用户迁移密钥。
- 回滚到第三阶段代码会忽略新增的 provider/App ID/Access Token 键，不破坏业务数据；但第三阶段代码不能调用豆包原生协议。
- 如果旧配置只有豆包旧版 App ID/Access Token，回滚后语音会不可用，因为旧代码只读取 `voice_api_key`。

## 11. 接口变化

### 11.1 `GET /api/me`

现有返回继续包含：

```json
{
  "authenticated": true,
  "account": {"id_hint": "...", "watched_count": 0},
  "demo_mode": false,
  "voice_available": false
}
```

`voice_available` 仍表示后端语音服务已经配置，不表示前端是否渲染麦克风。前端浏览器能力自行检测。

### 11.2 用户语音接口

`POST /api/voice/transcribe`：要求用户登录；JSON 输入 `audio_base64`、`mime_type`；请求体最多 12 MB，解码音频最多 10 MB；成功返回 `{"text":"..."}`。

`POST /api/voice/synthesize`：要求用户登录；JSON 输入 `text`；文字非空且最多 3000 字符；成功返回 `audio_base64` 和 `content_type`。

原始音频与合成音频不落库。供应商错误不会返回密钥；普通异常统一降级为可理解的语音不可用信息。

### 11.3 管理员语音接口

- `POST /api/admin/voice-config`：保存 provider、新旧凭据、根地址、资源/模型、音色、格式和超时。
- `POST /api/admin/voice-config/test`：使用当前表单草稿合成“连接成功”，返回字节数和 MIME。
- `GET /api/admin/agent-config`：`voice` 对象增加 provider、app_id、has_access_token、access_token_hint；不返回完整 API Key 或 Access Token。

其他认证、聊天、历史、电影、联网配置、提示词和邀请码接口没有改变。

## 12. 配置变化

`.env.example` 的新默认语音变量：

```text
VOICE_PROVIDER=doubao
VOICE_API_KEY=
VOICE_APP_ID=
VOICE_ACCESS_TOKEN=
VOICE_BASE_URL=https://openspeech.bytedance.com
VOICE_STT_MODEL=volc.bigasr.auc_turbo
VOICE_TTS_MODEL=seed-tts-2.0
VOICE_NAME=zh_female_xiaohe_uranus_bigtts
VOICE_AUDIO_FORMAT=mp3
VOICE_TIMEOUT_SECONDS=60
```

`.env` 只作为首次启动和故障恢复默认值。管理员保存后数据库优先。真实密钥不得写入 `.env.example`、README、测试或阶段文档。

部署豆包模式还必须满足：系统命令路径可找到 `ffmpeg`。当前开发机路径为 `/opt/homebrew/bin/ffmpeg`，但代码使用 `shutil.which`，不硬编码该路径。

## 13. 安全与隐私边界

### 13.1 凭据

- 豆包 App Key、Access Token、OpenAI API Key 只在服务器端配置和请求中使用。
- 管理读取只返回末四位掩码；输入框留空表示保留，显式复选框才清除。
- 错误日志只记录异常类型，不打印请求 Header、请求体、Base64 或真实密钥。
- App ID 不是按密钥掩码处理，但不得把它误当用户账户身份或传入公开页面。

### 13.2 音频

- 原始录音只存在于浏览器内存、影伴当前 HTTP 请求、可选 ffmpeg 进程内存和豆包请求中。
- `ffmpeg` 通过管道工作，不建立临时文件。
- 合成音频只存在于供应商响应、服务器内存和浏览器 data URL。
- 影伴不把音频或转写正文写入 SQLite；长期数据仍只有观影状态等产品数据。
- 外部语音供应商能够收到当次录音或 AI 回复文字，公开部署前必须在隐私政策中说明供应商、用途、保留政策和跨境边界。
- 浏览器不会自动播放有声回复。

### 13.3 外部请求与站点访问

语音、TMDB、Web Search 和页面阅读根地址都必须是 HTTP/HTTPS 且禁止 URL 用户名。公开页阅读仍执行豆瓣/知乎域名白名单。管理员配置 URL 属于高权限操作，生产环境应限制管理入口并增加审计。

### 13.4 Agent 与高风险文本

语音输入的转写文字进入与键盘输入完全相同的安全短路，不可绕过自伤/伤人检测。语音输出不改变身份透明、事实查证、记忆和推荐去重边界。

## 14. 修改文件清单

### 修改

- `settings.py`：增加语音 provider、App ID、Access Token 默认配置；默认供应商切换为豆包。
- `voice.py`：增加豆包新旧鉴权、极速录音识别、V3 TTS、连续 JSON 帧解码、浏览器音频 ffmpeg 内存转换、旧 OpenAI 配置推断。
- `web/index.html`：麦克风按钮常显，增加 `aria-controls` 和语音可用状态播报。
- `web/app.js`：分离浏览器支持与后端配置状态；未配置时显示可发现的禁用语音模式；只有配置成功才生成 AI 语音回复。
- `web/admin.html`：增加供应商、新版 App Key、旧版 App ID/Access Token、资源 ID 和说明。
- `web/admin.js`：增加供应商切换、默认值、旧配置推断、密钥掩码和豆包格式限制。
- `web/admin.css`：增加旧版凭据折叠区布局与移动端适配。
- `.env.example`：增加豆包变量与默认资源。
- `README.md`：更新豆包配置、入口常显、ffmpeg 格式兼容和测试说明。
- `tests/test_product.py`：增加豆包协议/数据流与麦克风常显测试，旧 OpenAI 测试显式声明 provider。

### 新增

- `第四阶段技术开发文档.md`：当前权威交接文档。

本阶段没有修改 `agent.py`、`catalog.py`、`integrations.py`、`storage.py`、`server.py` 的业务逻辑，也没有修改真实 `.env`、邀请码、数据库业务记录或真实密钥。

## 15. 测试与验证结果

### 15.1 语法检查

```text
python3 -m py_compile products/yingban/voice.py products/yingban/settings.py products/yingban/tests/test_product.py
node --check products/yingban/web/app.js
node --check products/yingban/web/admin.js
```

全部通过。

### 15.2 自动测试

命令：

```text
cd products/yingban
python3 -m unittest discover -s tests -v
```

结果：18 项全部通过。

覆盖包括：

1. 同邀请码回到同账户。
2. 停用邀请码阻止登录。
3. 换发邀请码保留账户并使旧码失效。
4. 推荐硬排除看过电影。
5. 账户片单隔离。
6. 模型配置持久化与密钥掩码。
7. 双 Agent 提示词独立。
8. 讨论自动标记看过。
9. 推荐不返回看过电影。
10. 换片保留上一轮意图。
11. 身份询问时透明说明 AI。
12. 管理员生成邀请码。
13. 管理员管理模型和提示词。
14. 管理员管理联网与语音配置。
15. TMDB 新电影与真实海报入库。
16. 联网和语音密钥持久化、掩码且不泄漏。
17. 豆包 STT/TTS Header、资源 ID、请求体和连续音频帧解码。
18. 未配置语音时 HTML 麦克风入口没有 `hidden`。

最初在受限沙箱运行 HTTP 测试时，7 项因系统不允许绑定 `127.0.0.1` 随机端口而报 `PermissionError`；允许本地测试端口后 18 项全部通过。这不是产品缺陷，不能把受限环境错误记录为测试失败。

### 15.3 浏览器验证

在现有已登录本地前台和后台刷新验证：

- 首页和聊天入口正常。
- 聊天页麦克风按钮以可访问名称“切换到语音输入”显示。
- 未配置语音时点击后显示“按住说话”禁用按钮和明确配置提示。
- 切回文字输入正常。
- 后台默认选择“豆包语音（推荐）”。
- 重启服务后根地址为 `https://openspeech.bytedance.com`。
- STT 为 `volc.bigasr.auc_turbo`。
- TTS 为 `seed-tts-2.0`。
- 音色为 `zh_female_xiaohe_uranus_bigtts`。
- MP3 为默认格式，AAC 在豆包模式禁用。
- 前台与后台控制台 error/warning 均为空。

没有点击或接受麦克风权限，因为真实豆包凭据尚未配置，当前阶段不能完成有效端到端录音验收。

## 16. 启动与使用方法

### 16.1 启动

```text
cd products/yingban
python3 app.py serve
```

用户页：`http://127.0.0.1:8765/`。

管理页：`http://127.0.0.1:8765/admin`。

当前服务已重启并运行，启动输出显示：模型 `deepseek-v4-flash`，联网电影未配置，语音交互未配置。

### 16.2 配置豆包新版控制台

1. 在豆包语音控制台开通录音文件极速识别和语音合成模型 2.0。
2. 创建 App Key。
3. 管理后台进入“语音”。
4. 选择“豆包语音（推荐）”。
5. 在“App Key（新版控制台）”填写凭据。
6. 保留默认根地址、`volc.bigasr.auc_turbo`、`seed-tts-2.0` 和匹配音色，除非控制台明确分配其他值。
7. 点击“测试语音合成”。
8. 测试成功后点击“保存语音配置”。
9. 回到用户聊天页刷新，点击麦克风并允许当前本地站点使用麦克风。

### 16.3 配置豆包旧版控制台

1. 选择豆包。
2. App Key 留空。
3. 展开“使用豆包旧版控制台凭据”。
4. 填写 App ID 和 Access Token。
5. 测试并保存。

不能同时随意混用新版 App Key 与旧版 Access Token；如果 App Key 非空，运行时优先使用 `X-Api-Key`。

### 16.4 使用语音

1. 进入“聊电影”或“找电影”。
2. 点击输入框左侧麦克风。
3. 按住“按住说话”并讲话。
4. 松开后等待转写和 AI 文字回复。
5. 在 AI 回复气泡里主动点击播放语音。
6. 录音中按 `Esc` 可以取消。

## 17. 尚未完成事项与已知风险

### 17.1 必须由用户提供或决定

- 豆包 App Key，或旧版 App ID + Access Token。
- 确认账号已经开通 `volc.bigasr.auc_turbo` 和 `seed-tts-2.0`。
- 确认默认音色是否符合“阿映”人设，或从控制台选择另一个与资源 ID 匹配的 speaker。
- TMDB 凭据、Brave Web Search 凭据仍未提供。
- 公开部署前的语音隐私告知与供应商数据处理说明。

### 17.2 真实服务未验收

当前没有真实豆包凭据，因此没有验证：配额、计费、区域限制、账号音色权限、真实网络延迟、服务错误 Header 和中文转写质量。模拟协议通过不等于真实供应商验收完成。

### 17.3 ffmpeg 依赖

开发机存在 `ffmpeg`。生产容器或服务器如果没有安装，Chrome WebM 录音无法转换，豆包 STT 会返回明确错误。部署清单必须安装 ffmpeg，或未来改成浏览器端 PCM/WAV 采集。

### 17.4 非实时通话

当前是按住录音、松开发送、等待 Agent、再生成语音的半双工交互。没有使用 Doubao-实时语音交互，不支持：边说边出字幕、AI 说话时打断、连续免按键对话、全双工降噪和低时延流式 Agent 输出。

若下一阶段升级实时通话，需要单独设计 WebSocket/RTC 会话、打断状态机、并发计费、音频播放队列、回声消除、字幕同步和断线恢复，不能把 `Doubao-实时语音交互` 名称直接填入现有字段。

### 17.5 性能与内存

当前浏览器把完整录音转 Base64 JSON，后端和供应商请求也一次持有完整音频。单条限制 10 MB 可以控制风险，但并发用户增加时需要改 multipart 上传、流式传输、连接池和并发限流。

TTS 音频通过 Base64 JSON 返回浏览器，长回复会增加约三分之一编码体积。单次合成文字限制 3000 字符，但下一阶段仍应按句分段和缓存。

### 17.6 供应商错误诊断

豆包常见错误是 speaker 与 `X-Api-Resource-Id` 不匹配。当前 `_request()` 会返回 HTTP 错误正文，但没有把 `X-Tt-Logid` 单独保存为结构化诊断字段。下一阶段应在不记录密钥和正文的前提下记录 log ID、状态码与资源 ID。

### 17.7 电影与搜索继承风险

- 24 部离线种子电影尚未回填海报。
- TMDB 搜索结果可能缺导演、别名和完整分级。
- Brave Search 与 Jina Reader 尚未用真实凭据/真实站点最终验收。
- 豆瓣/知乎页面结构和访问策略可能变化，必须继续遵守公开访问边界。
- 推荐候选池仍较小，严禁为了丰富结果取消看过硬排除或让模型虚构影片。

### 17.8 生产安全

- 本地启动提示 `YINGBAN_COOKIE_SECURE` 关闭；HTTPS 部署必须开启。
- 管理入口当前依赖管理员令牌，生产环境应增加限速、失败审计、CSRF 评估和更强身份验证。
- 外部 URL 由管理员配置，生产环境应考虑出口域名白名单和私网地址阻断。
- `ffmpeg` 处理不受信任音频，应保持版本更新、资源限制和非特权运行。

## 18. 下一阶段建议

按优先级：

1. 获取真实豆包凭据，完成 TTS 测试、3 至 10 秒中文录音、长句、静音、噪声、权限拒绝和移动端 Safari 验收。
2. 增加完整“测试语音识别”管理动作，而不只测试 TTS；测试音频必须由用户主动录制且不落盘。
3. 在错误日志中安全记录豆包 `X-Tt-Logid`、状态码和资源 ID，禁止记录凭据或音频。
4. 为音色增加从官方列表选择/试听能力，选项必须由 API 或维护表提供，避免手填错误。
5. 给合成结果增加短时内存缓存或对象存储策略，并定义删除周期；默认仍不长期保存。
6. 评估全双工实时语音交互，但作为独立架构阶段实施，不与当前按住说话混在一个配置字段中。
7. 配置 TMDB 并为种子电影按标题+年份回填海报，保留同名人工确认。
8. 配置 Web Search，真实验证新电影、豆瓣和知乎公开检索。
9. 增加推荐解释结构化数据、内容提醒标准化和用户可控记忆管理。

## 19. AI 接手入口

下一次 AI 开始开发时：

1. 阅读根目录 `AGENTS.md`。
2. 阅读本文件，不先把第三阶段时间点状态当当前事实。
3. 运行 18 项测试。
4. 读取 `/api/health`、管理公开配置和 `VoiceRuntime.public_config()`，绝不输出完整密钥。
5. 检查 `ffmpeg` 是否存在。
6. 如果用户提供凭据，只在管理后台或本地安全配置入口输入；不得写入代码、测试、README 或阶段文档。
7. 真实验收先测试 TTS，再测试 STT，最后验证 AI 回复播放器。
8. 如果真实接口与文档不一致，优先保留本地/文字核心能力，记录状态码、log ID 和差异，不通过猜测参数绕过。
9. 完成新的边界明确开发后，创建 `第五阶段技术开发文档.md`，不得覆盖本文件。
